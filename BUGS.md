# Bleephub bug ledger

686 findings from the continuing full-surface audit: 165 blockers, 374 major, 147 minor. Every
entry carries a location and a one-sentence claim. Severity is `B` blocker, `M` major, `m` minor.
Status begins with one of `open`, `partial`, `fixed`, or `deferred`; the generated parity
inventory records the exact counts and fails CI when this summary or a row drifts.

IDs are for this ledger only — per project convention they never appear in
source or comments. The reasoning behind a fix belongs in its commit message.

---

## AUTH — authentication, authorization, secrets, git transport

| ID | S | Location | Finding | Status |
|---|---|---|---|---|
| AUTH-001 | B | gh_apps_perms.go:100-128 | `requirePerm` is a credential-shape gate, not an authorization gate: for a classic PAT or any browser session it evaluates no predicate and falls through to the handler | fixed — every credential shape now passes both its credential grant and the target-resource authorization check; classic scopes, fine-grained selection/permissions/approval/expiry, and App/OAuth repository reach are regression-tested at the authorization chokepoint |
| AUTH-002 | B | gh_apps_perms.go:113 | `token.Scopes` is stored, persisted and emitted as `X-OAuth-Scopes` but enforced nowhere — a PAT scoped `read:org` can delete organizations | fixed — the classic scope map was completed rather than partially enforced, and it defends itself three ways: `init()` panics at startup on a permission with no mapping, an AST test keeps `allPermScopes` in step with the const block, and behavioural tests cover the gate. Verified by reversion: neutralising `classicScopeCovers` fails the organization-deletion and unscoped-token tests, dropping a mapping panics with the permission named, and removing a scope from the enumeration is caught as drift |
| AUTH-003 | B | auth.go:19-29, agents.go:33,180, broker.go:112 | The entire `/_apis/` runner protocol has no authentication; `ghHeadersMiddleware` does not even run on it | fixed — every `/_apis/` and `/twirp/` route carries one of `requireRunnerAuth`/`requireJobToken`/`requireAgentSession`/`requireRunnerSetupCredential`/`requireAssignedAgent`/`requirePlanJob`, except the five routes that necessarily authenticate inside their own handlers. Runner identity comes from an HMAC-signed registration token scoped to its owner, job endpoints bind the caller to the assigned agent or named plan, and regressions cover every decorator family plus the material acquire/renew/complete paths. AUTH-059 closed the jobs.go/run_service.go remainder this row previously described as open |
| AUTH-004 | B | secrets_vars_inject.go:39 | Job messages carry every org/repo/environment secret in plaintext to any caller who can poll the broker | fixed — `jobSecretsEntitled` gates entitlement and `pullPendingMessage` only hands a runner a message whose repository its registration scope covers. Unentitled *requests* 403 rather than returning a filtered message. Operator submissions name no repository and carry no secrets, so they stay deliverable — stated as a rule, not a fallthrough |
| AUTH-005 | B | auth.go:267-282 | Runtime tokens are `{"alg":"none"}` with an empty signature and a one-year expiry | fixed — HS256 with a process key, TTL 365 days → 6h. `BLEEPHUB_RUNNER_TOKEN_KEY` pins it across replicas; otherwise 32 bytes from `crypto/rand` once per process, resolved at startup with a fatal on failure and no hardcoded default. Process-local is deliberate and matches the existing OIDC key: runner sessions are only meaningful against dispatch state that is itself process-local and cancelled on reload |
| AUTH-006 | B | artifacts.go:1017-1059 | `repoForRuntimeToken` trusts the unverified `sub` claim; forging a token is base64 encoding | fixed — one `parseRunnerToken` checks `alg` and `hmac.Equal` **before** unmarshalling any claim; `jwtSubject` deleted. The old `repoForRuntimeToken` entry point is gone and its test rewritten to drive the live route, so the deadcode gate reports clean |
| AUTH-007 | B | secrets.go:230-304 | Repository secret handlers never resolve the repo and never check admin; `PUT /repos/anything/at-all/actions/secrets/X` returns 201 | fixed — new `requireRepoAdmin` choke point applied at all six secret handlers and inside `resolveEnvScope`; 404 when unreadable or absent, 403 when readable but not admin. Deliberately redundant with `requirePerm`, and each handler was proved to deny on its own by invoking it directly |
| AUTH-008 | B | secrets.go:230, store_repos.go:145 | Case-variant paths write a shadow scope key the injector never reads: operator sets a production secret, gets 201, job runs without it | fixed — every repository and environment secret handler keys off `repo.FullName`, the key the injector actually reads, never `owner+"/"+repo` from the path. A case-variant path resolves no repository and 404s instead of returning 201 into a scope nothing reads |
| AUTH-009 | B | handle_mgmt.go:75-102 | `GET /internal/oauth/state` returns every live OAuth authorization code with its owning user ID to any authenticated caller — account takeover | fixed |
| AUTH-010 | B | handle_mgmt.go:20-27 | 7 of 23 `/internal/` routes omit `requireSiteAdmin`, including every private repo's metadata and unmasked job logs | fixed |
| AUTH-011 | B | identity.go:622-636, gh_oauth.go:488 | The CSRF token is set equal to the session cookie value and printed into the OAuth consent page HTML, defeating HttpOnly | fixed — the CSRF token comes from its own generator rather than the cookie value, and the consent page mints a fresh token stored on the session which a successful POST rotates, making it single-use. The disclosure test needs *both* halves reverted to fail, which is why each half also has its own test |
| AUTH-012 | B | git_http.go:292, git_ssh.go:170, gh_branch_protection.go:1225 | Branch protection is never consulted by either git transport; `branchProtectionForRepo` has no caller outside its own file | fixed — HTTP, SSH and REST ref writes now share `protectedRefWriteRefusal`; it enforces push restrictions, creation/deletion/force-push allowances, required pull requests and checks, linear history and signed commits before a ref moves. A live transport regression protects `main`, then proves both `git push` over SSH and smart HTTP are refused and the remote ref stays put |
| AUTH-013 | B | gh_oauth.go:568-599 | `redirect_uri` is taken verbatim and never compared to the app's registered callback — authorization-code interception and open redirect | fixed — exact match, or a sub-path of the registered callback at a segment boundary, with both sides `path.Clean`ed so `/cb/../../elsewhere` cannot prefix-match `/cb`. Enforced on the consent GET and inside the `completeAuthorize` choke point. See AUTH-063: GitHub Apps have no registered callback to compare against, so the flow now fails closed for them |
| AUTH-014 | B | gh_apps_store.go:481,492 | Client secrets compared with `!=`, reachable unauthenticated and unrate-limited — byte-at-a-time timing oracle | fixed |
| AUTH-015 | B | gh_middleware.go:172-175 | Every JWT verification failure is discarded and the request continues as anonymous; invalid `ghp_`/`gho_` tokens likewise | fixed — a presented credential that does not verify is 401 rather than silently downgraded to anonymous, while no header at all stays anonymous. Verified by reversion: all seven credential shapes (`ghp_`/`gho_`/`ghu_`/`ghs_`/`ghr_`, an unverifiable JWT and free-form garbage) were admitted with the guard neutralised, and the no-header control stayed green |
| AUTH-016 | M | gh_oauth.go:165, identity.go:577 | `browserLoginUser` accepts any PAT as a password with no scope check, laundering a restricted credential into an unrestricted session | fixed — ordinary users authenticate browser sessions only with their local password or external identity; the sole token-form exception is an exact constant-time match for the configured bootstrap administrator token, and a regression proves even a scoped administrator PAT cannot become an unrestricted cookie |
| AUTH-017 | M | gh_apps_perms.go:135,217 | Public repo + GET bypasses scope, resource-owner and repository-selection checks — a PAT scoped to org A reads org B's Actions variable values | fixed — both fine-grained-PAT carve-outs and the installation carve-out are keyed on the scope |
| AUTH-018 | M | secrets.go:22, actions_store.go:84, persistence.go:38 | Secret plaintexts and the X25519 private key that opens them are written unencrypted into adjacent rows of the same table | fixed — the persistence boundary AES-256-GCM encrypts every Actions, Codespaces, App/OAuth, token, and browser-session credential row with bucket/key associated data; startup transactionally migrates plaintext rows and validates every envelope, while Terraform generates a retained 32-byte key in Secrets Manager and injects it only as an ECS secret |
| AUTH-019 | M | store.go createTokenLocked | PATs are stored keyed *by* the raw token value; one DB read is total credential compromise | fixed — PAT JSON never contains the one-time value, persisted lookup keys are SHA-256 digests, legacy raw-key rows migrate transactionally, and installation/OAuth/refresh/session bearer keys are independently replaced by HMAC digests derived from the persistence key |
| AUTH-020 | M | secrets_vars_inject.go:39, handle_mgmt.go:218 | No `::add-mask::` handling and no log scrubbing anywhere — a secret echoed by a job is readable by any authenticated user | fixed — every secret variable in a dispatched message is registered as a server-side plan mask, both `::add-mask::` and runner `##[add-mask]` commands extend it, and console plus uploaded log bytes are scrubbed before structured logging, in-memory storage, or object-store persistence |
| AUTH-021 | M | identity.go:598, store.go:70 | External identities are keyed on the mutable provider username, not `(issuer, subject)`; `SiteAdmin` is overwritten by whichever provider logged in last | fixed — external identities now keyed on `(issuer, subject)` via `UsersByExternalID`; `SiteAdmin` is never overwritten on existing accounts by login |
| AUTH-022 | M | rbac.go:90-101 | Any active org member gets push to every repo in the org regardless of role, team or base permission | fixed — read, push, and admin now share one locked capability lattice: the org's `default_repository_permission` (unset = read), active owner role, team grant, and collaborator grant are compared at their actual levels; matrix regressions cover none/read/write/admin |
| AUTH-023 | M | git_http.go:98-229 | Nonexistent repo returns 404, existing-but-unauthorized returns 401 — a private-repository existence oracle | fixed — authentication now precedes repository resolution: anonymous requests get a 401 challenge (required by the git HTTP protocol) whether or not the repo exists; authenticated-but-unauthorized requests get 404, indistinguishable from a nonexistent repo |
| AUTH-024 | M | git_http.go:337, git_ssh.go:231 | Receive-pack errors matched by `strings.Contains(err, "EOF")`, then post-push hooks and workflow triggers fire for refs never written | fixed — neither transport treats an error string containing `EOF` as success. The shared receive-pack path retains the per-ref report, and post-push work receives only commands whose status is `ok`, so a refused or failed ref raises no push event |
| AUTH-025 | M | git_ssh.go:52-71 | SSH listener spawns an unbounded goroutine per connection with no handshake deadline and no cap | fixed — the listener owns a 64-slot connection semaphore, bounds the handshake with a ten-second deadline, and serves each accepted channel inline so a client cannot fan out unbounded session goroutines |
| AUTH-026 | M | ssh-gateway/main.go:60-95 | Read deadline cleared after the banner check so an idle connection holds one of 32 slots forever; limiter map never evicted | fixed — proxied connections carry an activity-renewed idle deadline for their complete lifetime, and rate-limit identities are evicted after their window rather than accumulating forever; focused tests cover both |
| AUTH-027 | M | identity.go:577, gh_middleware.go:249, rate_limits.go | No rate limiting on any auth endpoint; the `X-RateLimit-*` headers are hardcoded constants | fixed — REST and GraphQL requests consume credential/resource-scoped primary windows with live headers and the 60-request anonymous core budget, and the credential-verifying auth-flow endpoints (`/login`, `/auth/local`, `/auth/token`) now consume a per-IP per-minute `auth` budget, refusing over-budget attempts with GitHub's 403+`Retry-After` shape (device/OAuth token-exchange endpoints are excluded because they legitimately poll and carry their own slow_down). Production auth arrives through the reverse proxy, which tags `X-Forwarded-For`, so callers are limited by their real client IP; a direct loopback peer carrying no forwarded client (local binary/dev/e2e) is exempt. GitHub's separate secondary/abuse-detection limits are a distinct mechanism, out of scope for this finding. Regression: TestAuthFlowRateLimitBlocksBruteForcePerIP |
| AUTH-028 | M | handle_mgmt.go:410, identity.go:240 | Logins accepted with no allowlist, no case folding, no NFKC and no confusable check — `Alice`, `alice` and Cyrillic `аlice` are three accounts | fixed — NFKC normalization and case folding applied; Latin/Cyrillic mixing rejected; `BLEEPHUB_ALLOWED_LOGINS` env var enforces a login allowlist; user creation and external login normalized |
| AUTH-029 | M | auth.go:22,34-62 | Runner registration is unauthenticated and mints a "management token" that is never stored or checked | fixed — every registration/removal-token minting endpoint (repo/org/enterprise) is gated on `administration:write` or enterprise ownership, and the minted token is an HMAC-signed blob carrying its scope, purpose and ~1h expiry, so it needs no server-side registry; `handleRunnerRegistration` and `handleRegisterAgent` both verify that setup credential (`runnerSetupCredential`) before acting, and its purpose must match the runner_event. Closed alongside the AUTH-003/059 runner-auth chokepoint; regression: TestRunnerProtocolRejectsUnauthenticatedCalls plus the registration-token gating tests |
| AUTH-030 | m | identity.go:459 | The OAuth client secret is reused as the HMAC key for the state cookie | fixed — a dedicated 32-byte CSPRNG key is generated at startup and used for state-cookie HMAC; the client secret is no longer reused as a cryptographic key |
| AUTH-031 | m | identity.go:642,690 | Logout Origin check is conditional on Shauth being configured, and `/ui/signed-out` destroys the session on a plain GET | fixed — logout always validates Origin when the header is present; session destruction requires POST; GET renders the page without side effects |
| AUTH-032 | m | identity.go:635 | `Secure` inferred from a string prefix, copy-pasted at five sites, no `__Host-` prefix | fixed — `isSecureRequest` checks `r.TLS` or `X-Forwarded-Proto`; session cookies get `__Host-` prefix when Secure; all five copy-pasted sites use the shared helper |
| AUTH-033 | m | gh_oauth.go:538 | CSRF token compared with `!=` while `identity.go:446` correctly uses `subtle.ConstantTimeCompare` | fixed |
| AUTH-034 | m | git_ssh.go:74-96 | `mustAtoi` cannot fail; discards both `Sscanf` returns and yields 0 — a latent fail-open in the SSH auth path | fixed — `parseGitSSHUserID` checks both `Sscanf` returns and returns an error; the caller drops the connection on parse failure (fail closed) |
| AUTH-035 | m | store.go:3222 | SSH key lookup re-parses every registered key per attempt under RLock; unparseable keys silently skipped; no `MaxAuthTries` | fixed — parsed keys cached at registration and persistence load; lookup compares cached wire encodings; `MaxAuthTries: 6` enforced per connection |
| AUTH-036 | m | secrets.go:295 | Repo secret delete discards the existence result, returns 204 for a secret that never existed, and writes a forged audit event | fixed — delete returns 404 when the secret never existed; audit event only written when it did |
| AUTH-037 | m | identity.go:250 | Login never invalidates the presented session; no rotation on privilege change, so a demoted admin keeps admin for 12h | fixed — successful login invalidates the presented session and mints a new one, preventing session fixation |
| AUTH-038 | m | identity.go:759 | Front-channel logout page sets `frame-ancestors *` on a GET with a session-destroying side effect | fixed — CSP `frame-ancestors` limited to 'self' plus the configured Shauth issuer origin (the OP frames this endpoint during front-channel logout); revocation demands an exact issuer match and a non-empty OP `sid`, which a cross-site attacker cannot guess. The earlier POST-only gate broke the spec's GET-in-iframe delivery and left witness sessions alive (issue #112), so it is gone |
| AUTH-039 | m | identity.go:235-244 | Five-line comment defending a one-line assignment, arguing which provider string becomes the account key instead of not using one | fixed — removed the comment |
| AUTH-041 | B | gh_code_scanning.go:312, gh_apps_rest.go:1049 | `installationRequestCanAccessRepo` fed one caller-chosen repo to `filterReposBySelection`, which is written for listing an installation's own repos and so has no owner check and treats non-`selected` as "allow everything handed to it" — an installation on the attacker's own account downloaded any private repository's CodeQL database | fixed — routed through `installationCovers` |
| AUTH-042 | B | gh_apps_perms.go:319 | `viewerCanReadRepo` had no user-to-server arm, so a `ghu_` token was indistinguishable from a session on handler-gated routes and borrowed the bearer's collaborator access to repositories the app was never installed on — strictly broader than the `ghs_` token of the same app | fixed — `canReadRepo` renamed `canReadRepoAsUser` across 30 files and 49 of 52 repo call sites routed through `viewerCanReadRepo`, whose signature moved to `context.Context` so the three GraphQL resolver families can reach it at all. The 3 unconverted sites sit inside the allowlisted RBAC layer and each is named. Pre-fix: all 5 cited routes returned 200 for a zero-installation `ghu_` where `ghs_` got 404 |
| AUTH-043 | M | internal/server | 17 handler-side `ReposByName` map reads remain unlocked; a concurrent map read/write is a fatal error the recovery middleware cannot catch, so this is a remote denial of service | fixed — the remaining alert, package, workflow and payload readers resolve through `GetRepoByFullName`; existing one-off lock wrappers use it too. The environment lookup is explicitly `protectedEnvironmentLocked` because dispatch already owns `store.mu` — converting it to the locking accessor was rejected by the full-suite deadlock regression. A bounded create/delete-versus-lookup test runs under `-race`, and an AST ratchet rejects any new `s.store.ReposByName` read unless the same function visibly holds `s.store.mu` (with the two deliberately lock-owned helpers named) |
| AUTH-044 | M | webhooks.go:210, gh_org_hooks_rest.go:254 | Org hook delivery reads `hook.URL` on a delivery goroutine while `UpdateOrgHook` writes it; reproduces under `-race` on main and is missed by the CI race job's `-run` subset | fixed — enqueue and redelivery already take a locked configuration snapshot; repository and organization hook getters/listers now also deep-copy the `Events` slice and `LastResponse`, so API rendering cannot retain a live store pointer either. A concurrent update/read regression validates both hook families under `-race` and mutates the returned slice to prove it is detached |
| AUTH-045 | B | gh_org_hooks_rest.go:44 | `orgHookGate` checks only `canAdminOrg`, so the 11 handler-gated org-hook routes never reach `userToServerReachesOrg` — a `ghu_` token for an app installed nowhere `POST`s an org webhook pointing at an attacker URL (201) and lists them (200), while the same app's `ghs_` gets 404. Persistent exfiltration of every organization event. Same shape on `GET /orgs/{org}/installations` (ghu 200 / ghs 403) | fixed — one org predicate `viewerHoldsOrgRole` with `viewerCanAdminOrg`/`viewerIsOrgMember` over it; `canAdminOrg`/`isActiveOrgMember` renamed to `…AsUser` and all caller-entitlement sites converted, including the eight follow-ups audited under AUTH-079. Pre-fix the planted webhook was created (201), listed (200) and left in the store |
| AUTH-046 | B | authz_matrix_test.go:493 | `TestPublicRepositoryBypassIsKeyedOnTheScope` passes on the pre-fix code, so the only regression test written for the fine-grained-PAT bypass cannot see the bug it names: it probes `scopeSecrets`, which `resourceCapabilityFor` promotes to `permAdmin`, so the resource gate refuses the outsider before the carve-out is reached. The underlying fix is real — `/actions/permissions` discriminates 200 pre-fix vs 403 at HEAD | fixed — the three `/actions/permissions*` probes added; verified 3 failures on a pristine tree with both carve-outs reverted, and green at HEAD |
| AUTH-047 | M | 7 routes | Anonymous callers still read private-repo data on `/actions/artifacts`, `/actions/cache/usage`, `/actions/caches`, `/actions/oidc/customization/sub`, `/comments/{id}/reactions`, `/private-vulnerability-reporting` and `/stargazers` | fixed — the original seven and the two routes found during remediation now deny private-repository callers without read access. Vulnerability-reporting status and stargazers use the credential-aware metadata-read predicate; the repository package list no longer turns a failed predicate into an implicit empty 200. Repository-installation discovery was corrected at the stronger boundary: it accepts only the authenticated app's JWT, filters by that app before returning an installation and honours selected-repository coverage. The read-matrix regression now drives both anonymous and unrelated classic-PAT callers and keeps positive controls for all three ordinary metadata routes |
| AUTH-048 | m | gh_repos_rest.go:34 | `/collaborators` is registered under `scopeContents` while `fineGrainedPATPermissionForPattern` maps it to `scopeAdministration`; both disagree with GitHub's documented `metadata:read` permission for collaborator reads | fixed — every collaborator GET route and the fine-grained PAT classifier now use `metadata:read`, while collaborator mutations retain `administration:write`; an installation-token regression covers the documented read grant |
| AUTH-049 | B | gh_apps_perms.go:441 | `principalMayAccessTarget` ends in `return true`, so when the path names a repository that does not resolve the whole gate passes. Any handler that does not itself resolve the repo then acts on a caller-fabricated repo key — proven on repo webhooks (201 and a working hook) and Dependabot public keys (200 with the instance key). The org arm has the same shape: `if org := GetOrg(login); org != nil && !canAdminOrg(...)` skips the check for an org that does not exist, and `fineGrainedPATResourceAllowed` returns true on `repo == nil`. The path values distinguish named-but-absent from not-named, so this is fixable without over-blocking the routes that name no repository | fixed in the predicate rather than per handler — `credentialMayAccessTarget` returns a three-valued verdict so named-but-absent gets its own answer, always 404 rather than the level-dependent 403. The org arm's `org != nil && !canAdminOrg(...)` and `fineGrainedPATResourceAllowed`'s `repo == nil → true` are both corrected. Pre-fix a route-table sweep found **38** routes serving a fabricated repository, including `POST .../actions/runners/registration-token` returning 201 with a usable token |
| AUTH-050 | M | webhooks.go | Deliveries read the shared `*Webhook` while `UpdateHook`/`UpdateOrgHook` mutated it in place | fixed — `Store.SnapshotHook` copies the config under the store lock at enqueue, so a delivery is signed by the config as of when it was queued |
| AUTH-051 | M | webhooks.go | `insecure_ssl=1` was accepted, stored and echoed but never affected TLS — a silent no-op on a client-observable security setting | fixed — selects a non-verifying client, as on GitHub |
| AUTH-052 | B | gh_actions_extras.go, gh_misc_endpoints.go, gh_reactions.go | Three handlers had no repository gate at all and served a stranger against a private repo: `handleRepoArtifacts`, `handleOIDCCustomSubGet` and `handleListReactions` (all five of its registrations are repo-scoped) | fixed — all three now call `enforceRepoReadable` |
| AUTH-053 | B | gh_releases.go | `enforceRepoReadable`, used by 10 callers, returned **true** for a repository that does not exist, so `/actions/runs` and `/actions/concurrency_groups` answered 200 for a fabricated repo while answering 404 for a real private one — an existence oracle running backwards | fixed — not-named passes, named-but-absent 404, private-unreadable 404 |
| AUTH-054 | B | git_http.go | `git-receive-pack` gated on `canPushRepo(user)` with no credential intersection, and sits entirely outside `requirePerm` | fixed — new `viewerCanPushRepo` mirroring the read choke point, applied at both push gates |
| AUTH-055 | M | artifacts.go | `/actions/caches` and `/actions/cache/usage` return 200 for a repository that does not exist and leak a private repository's `full_name` to a stranger; they are the two remaining entries in the sweep's exclusion list | fixed — both reads now pass through `enforceRepoReadable`, giving named-but-absent and unreadable private repositories the same 404 behavior as the rest of the Actions surface |
| AUTH-056 | M | gh_search.go, store_notifications.go, gh_user_issues.go | `canReadRepoLocked` is the same credential-blind class as AUTH-042 at 10 sites, but under `st.mu`: `viewerCanReadRepo` calls store methods that take `RLock`, so routing it there as-is risks deadlock. Needs `…Locked` variants of `ListAppInstallations`/`GetInstallation` first. Not covered by the AST guard | fixed — aggregate issue and notification rows are gathered under the store lock, released, then filtered through a required credential-aware callback; nil exposes nothing. Thread reads use the same path, selected-repository App-token regressions cover both aggregate APIs, and the AST ratchet now bans the locked predicate outside RBAC |
| AUTH-057 | M | rbac.go | `canPushRepo`/`canAdminRepo` have no credential-aware choke point and no AST guard. `requirePerm` covers every `/api` route registered through it, so exposure is limited to plain `s.route` write handlers, but the class is explicitly **not** closed | fixed — the write half is now closed the same way the read half is: `canPushRepo`/`canAdminRepo` are entries in the `userScopedPredicates` AST guard (`authz_chokepoint_test.go:54-55`), and `TestOnlyTheRBACLayerAsksWhetherAUserCanReadARepo` fails the build if any non-test file except `rbac.go`/`gh_apps_perms.go` references them. Grep confirms those two exempted files are the only remaining callers, so no plain-route write handler self-gates on a user-scoped predicate — the credential-intersecting `viewer*` helpers are the only path. The behavioural counterpart is AUTH-081's full repo-write ghu/ghs sweep |
| AUTH-058 | M | rbac.go | `canAdminOrgAsUser` dereferenced `user` and `org` with no nil check — a latent panic | fixed — guarded |
| AUTH-059 | B | jobs.go, run_service.go | The `/_apis/` routes registered in these two files are still unauthenticated: `AgentRequest` acquire/update/complete, `FinishJob`, `ActionDownloadInfo`, the task routes and `plans/{planId}/events`. Unauthenticated job acquire and complete means anyone can take or finish another runner's work. The decorator family to apply already exists in auth.go | fixed — all 8 routes gated, no allowlist entry added. Two new decorators in the existing family: `requireAssignedAgent` (agent session **plus** `job.AgentID == caller`) and `requirePlanJob` (job token **plus** the plan's scope matching the verified `sub`). Verified against the *plausible-but-wrong* choice, not merely against no gate: with `requireAgentSession` alone a runner registered for one repository read, renewed and completed another's job; with `requirePlanScope` alone one repository's job token completed another's, and on the `{planId}`-only path it refused the legitimate owner outright |
| AUTH-060 | B | agents.go:388 | `handleUpdateAgent` accepted an `authorization` block from the request body, letting a runner overwrite its own clientId and therefore its own scope — self-service privilege escalation | fixed — the stored authorization is always preserved |
| AUTH-061 | M | agents.go:333 | The agent get/update/delete/list handlers were fleet-wide for any caller, and `handleCreateSession` synthesized a fake agent from the request body when lookup failed | fixed — scoped via `callerSeesAgent`, 404 rather than 403 so ids are not probeable; the session now uses the authenticated agent |
| AUTH-062 | M | auth.go:448 | Registration, removal and JIT routes minted credentials for a repository or organization that does not exist, and a removal token could be replayed to register a runner | fixed — 404 on an absent target, and removal tokens carry a distinct purpose |
| AUTH-063 | B | gh_apps_store.go:22 | Validating `redirect_uri` correctly closed an open redirect but left GitHub Apps unable to complete the OAuth web flow at all: the `App` struct stores no callback URL — only `OAuthApp` does — so there is nothing to compare against and the check fails closed with `redirect_uri_mismatch`. Failing closed is the right posture, but a real GitHub feature is now refused. Needs a `CallbackURL` on `App` plus the manifest and registration plumbing | fixed — `App.CallbackURL` in the same single-string shape `OAuthApp` already carries, so the authorize gate consults one field rather than two mechanisms. Validated (absolute, http/https, non-empty host) at the OAuth-App boundary, the manifest boundary and as a store-level backstop. A manifest naming more than one callback is **refused** rather than quietly reduced to the first. The hole is not reintroduced: an app with no registered callback still fails closed, covered for both store-created and manifest-created apps. Persistence verified through the reload harness, and the test fails when the field is tagged `json:"-"` |
| AUTH-064 | m | server.go:165 | Source import reuses `allowPrivateWebhookTargets` as its private-address opt-out; the field name is webhook-specific but it is now the instance's only such switch | fixed — renamed `allowPrivateOutboundTargets` / `BLEEPHUB_ALLOW_PRIVATE_OUTBOUND_TARGETS`, since it now gates source import as well as webhook delivery. The retired variable is **refused at startup** rather than ignored: silently dropping an operator's opt-out would make their own loopback receivers start failing with nothing said. Deny-by-default unchanged, and the scheme rule stays outside the opt-out |
| AUTH-065 | M | gh_repos_git.go | `POST git/blobs`, `trees`, `commits`, `tags` and `refs` had no handler-level permission check at all | fixed — all carry `requireRepoPush` |
| AUTH-066 | M | secrets.go | `handleGetRepoSecretsPublicKey` took `_ *http.Request` and so served the Actions public key for any path whatsoever | fixed — resolves and gates |
| AUTH-067 | B | run_service.go | `handleFinishJob` and `handleJobEvents` fell back to a **body-supplied `jobId`** when the plan did not resolve, and then completed that job — a second path to finishing an arbitrary job, independent of the route gate, ending in a `warn + 200 {"status":"ok"}` silent no-op | fixed — the job resolves from `{planId}` only; a body `jobId` may confirm it (400 on mismatch) but never select it, and an unresolvable plan is a loud 404 |
| AUTH-068 | B | timeline.go | `requirePlanScope` binds the token to `{scopeId}` while `handleUpdateRecords` and `handleWebConsoleLog` act on `{planId}`, so a valid job token writes another job's timeline and console log — cross-tenant forgery of the output a human reads to judge whether a build is trustworthy. Demonstrated concretely on the sibling FinishJob route | fixed — all eight timeline routes plus `ActionDownloadInfo` moved to `requirePlanJob`, and `requirePlanScope` is deleted since no correct caller remained. Verified against the plausible-but-wrong decorator: with `requirePlanScope` restored, one repository's job token forged a completed step and console output into another repository's plan, all 200 |
| AUTH-069 | M | artifacts.go, jobs.go | `repoForJobScope` requires a non-empty `github.repository`, which `buildJobMessage` sets to `""` for operator-submitted jobs, so those jobs' runtime tokens are rejected on every job-token route and such a job cannot report progress or completion at all. Pre-existing on the timeline routes | fixed without needing `auth.go` — `repoForJobScope` now separates "no such plan" from "plan names no repository". Not a wildcard: every consumer of the scope was audited, and an operator token reaches its own plan (200) while another plan is 403 and every repository-scoped surface refuses it (cache 401, artifact 404) |
| AUTH-070 | m | run_service.go | An ephemeral runner is deregistered mid-teardown, so its final `DELETE AgentRequest` — and the `DELETE AgentSession` beside it — arrive after the agent is gone and take a 401, costing retries at teardown | fixed — deregistration moved to `handleDeleteSession`, the runner's genuinely last call. Moving it to `DELETE AgentRequest` as first proposed would have fixed that route and broken the session delete after it; only this position covers both. Moving it later opened a window where a finished ephemeral runner could be handed a second job, closed by `agentTakesAJobLocked`. Residual, stated: a runner killed without deleting its session lingers as an inert offline registration, which is also what real GitHub does |
| AUTH-071 | B | artifacts.go | A third instance of the plan-binding class, found by sweeping rather than by report: `POST /_apis/v1/Logfiles/{scopeId}/{hubName}/{planId}/{logId}` takes log ids from a single global counter with no owner recorded, so a job legitimately naming its own plan could upload into another plan's log container | fixed — `claimLog` records the reserving plan, upload refuses any other, and deleting the data releases the claim so it cannot outlive the bytes |
| AUTH-072 | M | broker.go | A session with no agent was accepted, and under `requireAssignedAgent` would receive a job it could never renew or complete — a silent dead end | fixed — refused |
| AUTH-073 | B | gh_repos_graphql.go:1013 | The GraphQL mutation lane read through the credential-aware choke point and then asked only about the bearer for push and admin, so a `ghu_` token was strictly broader than the `ghs_` of the same app across 20 mutations — 15 at push including `mergePullRequest`, 5 at admin including `deleteRepository`. **A regression from the previous commit on the push half**: tightening only the `ghs_` side created the asymmetry. Reproduced on a *public* repository — on a private one the read gate refuses first and the write half is never reached, which is how a test of this passes while the hole is open | fixed — each policy row now carries both the fine-grained scope the mutation is granted at and the standing the bearer needs, checked separately, so `ghs_` and `ghu_` reach the same answer and neither is broader than the other |
| TEST-037 | M | gh_classroom_test.go:346 | `TestGitHubClassroomTransitionExportImportAndNoOperatorSeeds` is order-dependent and **panics** rather than failing (`interface conversion: interface {} is nil, not []interface {}`), aborting the package so every test scheduled after it silently never runs. Hit 3 times in 13 shuffled runs; identical at the previous commit, so pre-existing | fixed — the test creates its own organization and classroom, verifies that exact classroom by name in the transition bundle, and no longer relies on an earlier test's fixture |
| AUTH-074 | m | auth.go:34 | The `/_apis/` allowlist is described as two entries; enumerating `routePatterns` shows five undecorated routes. The three extra (`Agent/{poolId}`, artifact download, cache by id) each authenticate inside the handler, so there is no hole — the description is wrong, and `TestRunnerProtocolRejectsUnauthenticatedCalls` is a hand-written list rather than a sweep of the route table | fixed — comment now documents the full five-route allowlist; test rewritten as a sweep over routePatterns with a 30-route floor |
| AUTH-040 | M | persistence.go:290, dqlite-node/main.go:108 | dqlite inter-node transport has no authentication and no TLS, contradicting the "keeps the wire protocol private" claim | fixed — the transport now runs over TLS 1.3 with a per-cluster identity derived from `BLEEPHUB_DQLITE_SECRET` (`dqliteaddr.TLSConfig`): the node wraps its listener with `tls.NewListener` and the dialer connects with `tls.Client` pinned to the secret-derived root, so a peer from another cluster's secret is rejected at the handshake. Authentication is a constant-time (`subtle.ConstantTimeCompare`) check of the secret header on the `/dqlite` HTTP upgrade, and `NewPersistence` refuses to start without the secret. Covered by `TestDqliteHTTPDialUpgrades` (presents the secret + upgrades over TLS), `TestDqliteHTTPDialRejectsUnupgradedResponses` (wrong-secret 401 rejected) and `TestDqliteHTTPDialRejectsAnotherClusterTLSIdentity` (cross-cluster TLS identity rejected) |
| AUTH-075 | B | gh_apps_perms.go:430 | The `viewerCan*` family answers only `installationReachesRepo` — whether an installation is **on** a repository — and never `hasPerm`, whether it is **entitled**. On `requirePerm` routes `hasPerm` runs first, so reaching is correctly half the answer; on the self-gating surfaces (`/api/graphql`, git smart-HTTP) nothing supplies the other half. **A regression from the previous commit**: the arm it replaced was `canAdminRepo(botUser, …)`, which refused by accident, so the fix swapped accidental blocking for unconditional admission. An app installed on the owner with only `metadata:read` deletes the repository through GraphQL (200) and clones and pushes a private repository over git (200/200, against 401/403 on main) | fixed — installation, user-to-server, OAuth-App, classic PAT, and fine-grained PAT grants are intersected with repository reach on REST, GraphQL, and Git; deletion, clone/push, and organization-hook regressions cover the formerly open lanes |
| AUTH-076 | B | gh_repos_graphql.go:1008 | `authorMayAct` returns before the credential switch, and 16 of the 20 mutations carry it — four at admin level. On a public repository an author holding no push, through an app installed nowhere, retitled the issue with a `ghu_` where the same app's `ghs_` was refused. The author exemption must relax the user predicate *below* the credential intersection, not above it | fixed — the author exemption sits below `credentialGrantsRepo` and above `principalHoldsRepoCapability`, the only half authorship speaks to. Proved by reverting the ordering alone |
| AUTH-077 | M | authz_chokepoint_test.go:44 | The mechanical ratchet bans three read-side names and not `canPushRepo`/`canAdminRepo`, which is why it stayed green through the write-side regression it exists to prevent. Verified: reverting the GraphQL write arm leaves the test passing | fixed — `canPushRepo` and `canAdminRepo` are on the ratchet, and the 63 direct callers that exposed were routed through the credential-aware family, each naming the scope its route family is granted at. `viewerMayActOnRepo` carries the grant and the standing separately for the handlers where they differ — a code-scanning alert is granted at `security_events:write` while this server demands repository admin of a human |
| AUTH-078 | M | gh_apps_perms.go:599 | The refusal is chosen by required level, so an unrelated authenticated caller gets 403 for an existing private repository and 404 for an absent one — an existence oracle across 15 routes. Both were 404 on main. AUTH-049's rule was applied to named-but-absent and not to present-but-invisible | fixed — `denyResourceAccess` answers 404 whenever the caller cannot read the repository named in the path, whatever level the route asked for; 403 is reserved for a caller who can read it and merely lacks the standing to change it |
| AUTH-079 | M | gh_members_rest.go:41 +7 | Eight sites open-code `GetMembership(orgLogin, user.ID)` rather than `viewerIsOrgMember`; `GET /orgs/{org}/members` returns the non-public member list to a `ghu_` of an app installed nowhere. AUTH-045's 55-site conversion missed them | fixed for seven; the eighth refuted — `gh_copilot.go:340` reads the *target* user’s membership, not the caller’s, so it is a data lookup and not a self-entitlement. The real defect in that handler is the credential-blind `caller.Login == username` arm, now intersected with `viewerReachesOrg`. The other seven go through `viewerIsOrgMember`, `viewerCanAdminOrg` or `viewerReachesOrg`; the two `/user/memberships/orgs/{org}` sites take the last because they must still return a *pending* invitation |
| AUTH-080 | M | gh_projects_v2_graphql.go:43 | `projectRule` and `viewerOnly` are credential-blind: `canWriteProjectV2` is `user.ID == owner.id` with no intersection, so a `ghu_` of an app installed nowhere created a ProjectV2 where its `ghs_` was refused | fixed — `canReadProjectV2` and `canWriteProjectV2` intersect `credentialGrantsAccount` over the owning account with the owner-scoped predicate. `viewerOnly` is gone: `createRepository` answers to `repoCreationRule`, which resolves the owner through the same helper the resolver creates under and demands `administration:write` over that account |
| AUTH-081 | M | 8 routes | AUTH-057 sized: sweeping all 244 plain-route repository writes found 8 asymmetric cells; `POST /repos/{o}/{r}/rulesets` returns 201 to a `ghu_` of an app installed nowhere and 404 to its `ghs_` | fixed — the whole sweep is re-driven behaviourally: `TestNoRepoWriteServesAGhuBroaderThanItsGhs` iterates every registered mutating route under `/repos/{owner}/{repo}` (259 today) and asserts, against the victim's real private repository, that the outside app's `ghs_` is refused everywhere and its `ghu_` is never served where that `ghs_` is refused. No asymmetric cell remains: the named `POST /repos/{o}/{r}/rulesets` cell plus the whole self-gating class were routed through the credential-intersecting `viewer*` helpers (`gh_apps_perms.go`) by the earlier chokepoint sweep, and the AST ratchet `TestOnlyTheRBACLayerAsksWhetherAUserCanReadARepo` structurally forbids reintroducing a bearer-only gate |
| AUTH-082 | m | gh_repos_git.go:298 | `viewerAdminsRepo` is a body-identical duplicate of `viewerCanAdminRepo`, differing only by a nil guard the newer one lacks — so the "one choke point" is two | fixed — `viewerAdminsRepo` deleted; its two callers go through `viewerCanAdminRepo`, which keeps the nil guard |
| AUTH-083 | M | gh_middleware.go:182 | `authenticateRequest` reads a credential only from the `token` and `bearer` schemes, so `RemoteAuth` — the scheme actions/runner v2.321.0 presents its registration token with on `POST /api/v3/actions/runner-registration` — resolved to nothing and `ghHeadersMiddleware` answered "Bad credentials" before the handler ran. `runnerRegistrationCredential` accepted `remoteauth` and was unreachable for it. The real runner could not configure itself at all; every existing test of that route sent `Bearer`, which is why nothing saw it | fixed — `runnerCredentialOffered` names the three schemes a runner credential may arrive under, and the runner-credential arm consults it rather than the user-credential variable. The harness was also passing a literal placeholder for `--token`, which the newly-authenticated route correctly refuses; it now mints a real one through `POST /repos/{owner}/{repo}/actions/runners/registration-token` |
| AUTH-084 | M | agents.go:35 | Three routes `config.sh` reaches *before* an agent session exists were gated on one: `GET /_apis/v1/Agent/{poolId}` (the pool lookup of the runner's own name, which every configuration performs), `PUT …/{agentId}` (`--replace`) and `DELETE …/{agentId}` (`config.sh remove`). The runner holds only its registration or removal token there, so each answered 401. The removal token had no accepting route at all — `handleRunnerRegistration` verified every credential as a registration one and minted a registration token whatever `runner_event` asked for, so `config.sh remove` could not complete and a removal token could be exchanged for one that registers | fixed — the config-time decorator takes the purposes a route accepts and puts the verified scope on the request as a principal with no agent, so `callerSeesAgent` confines it to its own tenant; `runner_event` selects the purpose the presented token must carry and the one minted back. A registration token cannot delete a runner and a removal token cannot add one |
| AUTH-085 | M | agents.go:397 | `handleUpdateAgent` restored the whole registration-time authorization, discarding the RSA public key the runner sent. `config.sh --replace` generates a fresh key pair and signs its next `client_assertion` with it, so a re-registered runner was left unable to obtain a session — 401 at `POST /_apis/v1/auth/` with a verification error | fixed — the clientId and authorization URL stay server state; the public key is taken from the update |
| AUTH-086 | M | auth.go:56 | `runner_event` was parsed and then ignored: every presented credential was verified as a *registration* token and a registration token was minted back whatever the event said. So a removal token had no route anywhere that would accept it, and — the security half — a removal token could be exchanged for one that registers | fixed — the event selects both the purpose the presented token must carry and the one minted back; a removal token cannot add or replace and a registration token cannot delete, each 401, and an unknown event is 400 |
| AUTH-087 | M | test/runner/sockerless/run-integration.sh | The harness passed the literal string `BLEEPHUB_REG_TOKEN` where a token belonged — invisible while the route was unauthenticated — and its wait loop polled an endpoint anonymously, swallowed the failure for 30 seconds and then reported the runner missing | fixed — mints a real token through the REST API and fails loudly |
| AUTH-088 | B | auth.go:605 | The agent's scope was encoded into `authorization.clientId` as a signed blob, on the stated assumption that "the runner treats the clientId as opaque". It does not — it deserializes that field into a `System.Guid`, so the real runner failed configuration outright with a type-conversion error. A fidelity break: a real protocol field must carry its real type | fixed — the clientId is a bare GUID and the scope lives on the agent record, which `LookupAgentByClientID` already indexes. A GUID is guessable where a blob is not, and that is fine: the clientId only names whose public key to verify against, and the RSA `client_assertion` signature is what authenticates. The handshake test now asserts the clientId parses as a UUID, so the shape cannot regress into one the runner rejects |
| AUTH-089 | M | test/runner/sockerless/run-integration.sh | The runner harness discarded the evidence for its own failures: `config.sh` output went through `tail -5`, bleephub ran at `info` so the per-request log was never emitted, and the failure dump read only `Worker_*.log` — the job logs — while a configuration failure writes `Runner_*.log` and leaves the worker logs empty. A config failure therefore reported one line of context and nothing else | fixed — full output kept and dumped on failure, debug level by default, and the config-time diagnostics included |
| AUTH-090 | B | gh_apps_perms.go:442,525 | The grant intersection is guarded on `uts.AppID != 0`, so `gho_` OAuth-App tokens — which carry classic scopes `userToServerHasPerm` already knows how to evaluate — skip it entirely, as do fine-grained PATs, whose repository selection is never consulted on the self-gating surfaces (`/api/graphql` has no `{repo}` in its pattern, so the fine-grained gate passes it through). Both were equally blind on main | fixed — the `AppID != 0` guard is off the grant half of `credentialGrantsRepo`/`credentialGrantsAccount` and stays off the reach half, which is the asymmetry a `gho_` needs: its scopes are the whole of what it carries and it reaches no installation, so `userToServerReachesRepo`/`…Account` admit an AppID-less token by design. A fine-grained arm sits beside it (`fineGrainedPATGrantsRepo`/`…Account`), and `fineGrainedPATSelectsRepo` is now the one selection predicate the routed gate and the resolvers share. `classicOAuthScopesOffered` closes the same shape in `enforceFineGrainedPATResource`, which read a classic PAT's scopes and not a `gho_`'s — that was the org-webhook half. A credential × surface matrix against main moved 40 cells and every one of them tightened; the entitled controls (`gho_` holding the scope, fine-grained token selecting the repository, classic `repo` PAT, browser session, anonymous read of a public repository) are unchanged |
| ACT-090 | B | agents.go:166 | The just-in-time config blob is emitted as nested objects; the runner deserializes it as `Dictionary<string,string>` where each key is a file name and each value is that file's base64 contents, so it fails outright. `.credentials_rsaparams` is also absent while the runner writes that file verbatim on the JIT path, and no public key is recorded, so `verifyAgentClientAssertion` answers "no registered public key". JIT ephemeral runners can neither configure nor obtain a session. `runner_setup_test.go:153` pins the broken shape | fixed — the blob is a map of file name to base64 file contents, as the runner deserializes it, and carries `.credentials_rsaparams` with fixed-width RSA components (.NET rejects a `D` that is not the modulus length; Go trims leading zeros). The public half is recorded on the agent so the assertion verifies. The test that pinned the broken shape now asserts the contract and completes a real token exchange with the key from the blob |
| TEST-090 | M | runner_handshake_test.go:559 | `TestEphemeralRunnerTeardownStaysAuthenticated` is order-dependent — 2 of 10 shuffled runs fail on `renew job request … 403 This job request belongs to another runner`, because `pollForMessage` only asserts the body *contains* `PipelineAgentJobRequest`, so a foreign test's queued message satisfies it. A regression from the commit that added it | fixed — the test puts its uniquely labelled message first in the shared fixture and decodes the envelope to require both the exact message type and its own plan marker; ten shuffled repeated runs pass |
| AUTH-092 | B | gh_apps_perms.go:975 | `userToServerHasPerm` returns the verdict of the *first* installation it reaches rather than asking them all, so once an app is installed in more than one place a `ghu_` token's grant is decided by Go's map iteration order: 40 identical `git-upload-pack` requests answered 200 thirty-six times and 401 four times. The installation-narrowed branch has the same shape, returning on the first listed id. The reach half already scans every installation, so the two ranged over different sets and their intersection was neither | fixed — both loops continue past a candidate that does not grant and answer true only when one does, which is what the reach half already did. Not a widening: an attacker could re-roll the old draw at will, so the union was already the reachable set. `TestUserToServerGrantIsNotDecidedByMapOrder` fails pre-fix on the disagreement itself |
| AUTH-093 | M | gh_apps_perms.go:485 | The same class as AUTH-090 for the third scope-bearing shape: a classic PAT's scope selection was read by `enforceFineGrainedPATResource` on routed handlers and by nothing on `/api/graphql` or the git transports, so a `ghp_` with **no scopes at all** cloned and pushed a private repository (200/200) and deleted one through `deleteRepository`. Not a regression — main was equally blind | fixed — `credentialGrantsRepo` checks the selection on the same terms `classicScopeGateApplies` sets for the routed gate: writes only, because the classic scopes are coarse there while GitHub's read rules vary per endpoint and a wrong read denial turns into a 404 that hides a resource the caller may see. Reads and every scoped token are unchanged in the matrix |
| AUTH-091 | m | authz_chokepoint_test.go | Two evasions leave the ratchet green: a function variable (`var f = canPushRepo; f(...)`) since the walk only matches a direct `*ast.Ident` callee, and `canReadRepoLocked` — a body-equivalent twin declared inside an allowlisted file and called from 10 sites elsewhere, which is the same duplicate-predicate class as `viewerAdminsRepo`. The second is live and already tracked as AUTH-056 | fixed — the ratchet scans every identifier reference rather than only direct call expressions, so aliases are caught, and the duplicate locked predicate was removed entirely |
| AUTH-094 | B | identity.go:809 | Federated login resolved a mutable `preferred_username` against `UsersByLogin` when no `(issuer,subject)` binding existed, then bound the attacker's identity to that account. A Shauth principal named `admin` (or a confusable that NFKC-folds to it) thereby seized the seeded SiteAdmin account — full instance takeover; the primitive claimed any local account | fixed — `upsertExternalUser` resolves on `(issuer,subject)` first and requires a stable subject. The primary IdP may adopt a same-named LOCAL account (SSO taking ownership of the seeded bootstrap admin — the flow the shauth contract exercises), but privileges then come from the role claim, so a developer-role `admin` lands on a NON-SiteAdmin account and escalates nothing; a secondary provider, or an account already bound to a different federated identity, is refused. `TestUpsertExternalUserNeverEscalatesByUsername` |
| AUTH-095 | M | identity.go:809 | `BLEEPHUB_ALLOWED_LOGINS` was enforced only on local login; both SSO callbacks provisioned and authenticated any principal, silently voiding the operator's restriction | fixed — `upsertExternalUser` consults `loginAllowed` before provisioning or resolving, so the allowlist governs the SSO paths too. `TestUpsertExternalUserHonorsAllowlistAndAuthoritativeRole` |
| AUTH-096 | M | identity.go:809 | The IdP `role` claim was applied only when creating an account; a later demotion or promotion at the provider never took effect, so de-provisioning failed open (a demoted admin kept SiteAdmin forever) | fixed — the primary IdP (Shauth) writes SiteAdmin on every login; a secondary provider never writes it, so a second federated identity cannot reshape privileges (preserves AUTH-021). Same test |
| AUTH-097 | B | gh_ghes_oauth_authorizations.go:105 | `POST /api/v3/authorizations` (and the `PATCH`) were plain routes gated only on `ghUserFromContext != nil`, so any bearer — a `gist`-only PAT, a `ghu_` of an app installed nowhere — minted itself an arbitrary-scope classic PAT, or rewrote its own scopes wider. A concurrent DELETE also nil-dereferenced under the store lock, deadlocking the server | fixed — the authorizations API demands a browser session (password-equivalent authority) and refuses every derived bearer; the update/create paths re-check the credential under the write lock before dereferencing. `TestLegacyAuthorizationsRefuseBearerCredentials` |
| AUTH-098 | B | gh_misc_endpoints.go:865 | `GET /token` minted a signed Actions OIDC JWT from query parameters after checking only `user != nil`, so any authenticated principal (even a zero-scope PAT) forged a `sub` like `repo:victim/prod:environment:production` and defeated a cloud trust policy — cross-tenant credential takeover | fixed — the route is gated on the per-job runtime token, and the mint refuses unless that token is scoped to the requested repository. `TestOIDCToken_RequiresJobTokenScopedToRepo` |
| AUTH-099 | M | gh_middleware.go:247 | `authenticateRequest` set `ctxUser` before the suspension check (a suspended `ghu_`/`gho_` kept git-HTTP and registry access), rejected `ghs_`/`ghu_`/`gho_` Basic passwords (the `x-access-token` convention), and fell back to the session cookie when a token was offered (a revoked token served as the cookie's user) | fixed — a shared `resolveBearerCredential` handles every token shape without placing a suspended user on the context, Basic passwords resolve through the same switch, and the session is consulted only when no Authorization header was offered |
| AUTH-100 | B | git_http.go:38 | Authorization resolved `(owner,repo)` with one `.git` trim while `storeLoader.Load` trimmed `.git` again, so a request to `owner/app.git.git` authorized against `owner/app.git` but streamed the private `owner/app` packfile (SSH shared the shape; receive-pack mixed two repos' storage) | fixed — sessions are built from the already-authorized storer via a fixed loader, never re-derived from a string; repo creation rejects names with separators, whitespace, or a trailing `.git`. `TestCreateRepoRejectsAmbiguousNames` |
| AUTH-101 | B | handle_mgmt.go:383, gh_ghes_admin_stats.go:39, gh_enterprises.go:68 | The site-admin / enterprise-owner gates checked only `user.SiteAdmin`, and the fine-grained-PAT gate skipped patterns without `{repo}`/`{org}`. A site admin's fine-grained PAT, OAuth/App user token, installation token, or app JWT thereby administered the whole appliance (e.g. minting impersonation tokens for any account) | fixed — the gates require a broad credential shape (browser session or classic PAT) via `credentialConveysSiteAdmin`. `TestGHESAdminSurfaceRejectsNarrowCredentials` |
| AUTH-102 | M | gh_misc_endpoints.go:452 | `GET/DELETE /user/keys/{id}` acted on any key by id: GET had no auth wrapper and no ownership check, DELETE checked no ownership, so any caller read or revoked any user's SSH key (locking them out of the SSH transport) | fixed — both are owner-scoped, returning 404 for a key that is not the caller's (no read, no revoke, no existence oracle); GET requires a caller. `TestUserSSHKeyIsOwnerScoped` |
| AUTH-103 | M | gh_org_scim.go:97 | Org SCIM bound a new record to any pre-existing global account by username and force-enrolled it, then `replace`/`patch` rewrote that account's global login/name/email — so an owner of one org took over or renamed any account instance-wide | fixed — SCIM only creates or mutates accounts its own org provisioned (new `User.SCIMManagedByOrg`); a collision with an unmanaged account is a 409. `TestOrganizationSCIMCannotHijackExistingAccount` |
| AUTH-104 | M | gh_oauth.go:160 | `POST /login` redirected to a `return_to` validated only by a non-empty path, so `https://evil/x` and `//evil/x` were honored — an open redirect landing the just-authenticated victim on an attacker page | fixed — the legacy PAT login (only reachable when Shauth is unconfigured) no longer redirects to a caller-supplied `return_to` at all; it renders the signed-in page. The Shauth flow's `return_to` stays sanitized by `safeIdentityReturnTo` and laundered through the signed state cookie |
| AUTH-105 | M | gh_oauth.go:737 | An OAuth authorize request with no `scope` silently minted a `repo read:org gist` token — full private-repo read/write the user consented to nothing for | fixed — an omitted scope carries the minimal read-only grant, never a silent upgrade |
| AUTH-106 | M | store.go:214, persistence.go:685 | The `hmac:v1:` row key under which a token or session was persisted was itself accepted as the credential: in-memory tokens were keyed raw at mint but by digest after restore, and the lookup passed an already-`hmac:v1:` value through. A leaked backup/replica/query-log of row keys was a credential compromise | fixed — mint and restore both key by the digest, lookups always digest the presented value (never pass an `hmac:v1:` input through), and session Get/Delete pass the digested key to the persistence layer. `TestPersistedRowKeyIsNotACredential` |
| AUTH-107 | M | identity.go:59 | The unprefixed `_gh_sess` shadowed `__Host-_gh_sess`: after logout cleared only the `__Host-` name, a planted `_gh_sess` survived and the next request adopted it (logout bypass / fixation) | fixed — over HTTPS only the `__Host-` cookie is honored, and logout/sign-out expire BOTH names and revoke both records. `TestSecureDeploymentIgnoresUnprefixedSessionCookie` |
| AUTH-108 | M | store_login_sessions.go:158 | `GetLoginSession` returned the pointer it also stored in the map, so a caller rotating the CSRF token raced a locked reader (`-race` failure; torn CSRF token) | fixed — it returns a distinct copy |
| AUTH-109 | m | identity.go:988 | Signing out of a locally- or token-authenticated session started a global RP-initiated logout, tearing down the user's shared Shauth SSO session used by every other relying party | fixed — the global logout runs only when the current session was itself established through Shauth. `TestLogoutOfNonShauthSessionDoesNotStartGlobalLogout` |
| AUTH-110 | m | gh_runner_groups.go:77 | `orgIDGated` set `{org}` only inside the handler, after `requirePerm` had already run its (empty-target) org check, so any authenticated caller — even a zero-scope PAT — rewrote another organization's Actions cache policy | fixed — it re-checks org-admin against the resolved `{org_id}` |
| AUTH-111 | m | gh_org_pat_admin.go:102 | The org-PAT-admin `ghu_` branch checked only that the app's installation held the scope, never that the token owner was an org owner, so any account that authorized a suitably-scoped app could approve or revoke the org's PAT grants | fixed — the branch also requires `canAdminOrgAsUser` |
| AUTH-112 | m | git_ssh.go:88, git_http.go:239 | The SSH transport ran outside recovery (a decoder panic crashed the whole process), leaked a private-repo existence oracle (distinct stderr for missing vs unreadable), and the anonymous upload-pack request body was uncapped (OOM) | fixed — per-connection panic recovery, unreadable repos answered as not-found, and a bounded upload-pack negotiation body |
| AUTH-113 | m | gh_misc_endpoints.go:360 | Actions OIDC subject customization lived in one process-wide slice, so one repo's admin clobbered every tenant's `include_claim_keys` and an anonymous caller read the org configuration | fixed — stored per repository/org; the org GET requires org membership. `TestOIDCCustomSubIsScopedPerRepository` |
| AUTH-114 | m | git_http.go:114 | git HTTP sits outside `ghHeadersMiddleware`, so a presented-but-invalid credential was downgraded to anonymous and served a public repo 200 (a PAT-guessing oracle) | fixed — `authorizeGitHTTP` returns 401 for an invalid credential. `TestGitHTTPInvalidCredentialIs401` |
| AUTH-115 | m | identity.go:305, 383 | Every OpenID Connect discovery and token verification ran its outbound HTTP over `http.DefaultClient` with no timeout, so a slow or hung IdP pinned a goroutine and socket per request — an anonymous DoS/amplification on the unauthenticated `/auth/*` endpoints. The ID token `azp` was also unchecked (OIDC Core §3.1.3.7) | fixed — a 10s-timeout client bounds every discovery/JWKS fetch, and `azp` is validated (reject multi-audience without azp; azp must equal this client) |
| AUTH-116 | m | identity.go:849 | Local login returned 403 for a disallowed login and 401 for a wrong password, letting an anonymous caller enumerate `BLEEPHUB_ALLOWED_LOGINS` membership from the status code | fixed — both return the identical 401. `TestLocalLoginAllowlistRejectsLoginsNotListed` |
| AUTH-117 | m | identity.go:643 | The per-flow OAuth state cookie carried no cookie prefix, so a sibling-subdomain or network attacker could transplant a server-signed state cookie into the victim's jar and log the victim in as the attacker | fixed — the state cookie carries the `__Host-` prefix (Secure, Path=/, no Domain) over HTTPS; plain-HTTP dev keeps the unprefixed name. The per-process HMAC key across replicas remains open |
| AUTH-118 | m | store_repo_reads.go:76 | Anonymous clone counting recorded the remote host in an unbounded `Actors` map and re-serialized the whole bucket on every request, so a public-repo clone flood (or a large IPv6 range) was a persisted memory/disk sink | fixed — the distinct-actor set is capped at 10k; clone counts keep incrementing past the cap |
| AUTH-119 | M | identity.go:58, gh_pages_deployments.go:347 | OIDC discovery/JWKS/token/end-session and the Pages deployment artifact fetch were the only server-initiated fetches not routed through the SSRF address gate, so a provider-controlled discovery document or artifact URL could reach an internal or cloud-metadata address | fixed — both now use `newAddressCheckedHTTPTransport` (dial-time `Dialer.Control`); `oidcClientContext` became a `*Server` method to read `allowPrivateOutboundTargets`, and the shauth loopback-issuer browser harness sets `BLEEPHUB_ALLOW_PRIVATE_OUTBOUND_TARGETS=true` |

## REST — the `/api/v3` surface

| ID | S | Location | Finding | Status |
|---|---|---|---|---|
| REST-001 | B | gh_repos_objects.go:23-26,572 | Private repo contents, commits and README readable by an anonymous caller; `lookupReadableRepoFromPath` exists and is not used | fixed |
| REST-002 | B | gh_repos_refs.go:16-20,236,287 | Branch, tag and ref listings for private repos likewise anonymous | fixed |
| REST-003 | B | gh_labels_rest.go:127-477 | Labels and milestones for private repos anonymous — roadmap and security leakage | fixed |
| REST-004 | B | gh_branch_protection.go:243 | All 14 protection GETs anonymous | fixed |
| REST-005 | B | gh_actions_extras.go:104, gh_workflows_rest.go:92 | Private build logs, artifact bytes and workflow inventory anonymous | fixed |
| REST-006 | B | gh_checks_rest.go:229 | Private CI results anonymous | fixed |
| REST-007 | B | gh_import.go:81 | Commit-author emails and large-file paths anonymous | fixed |
| REST-008 | B | gh_repos_git.go:288-678 | Any logged-in user can force-push or delete `refs/heads/main` on any repository | fixed — every git-data write route carries `requireRepoPush`, and every ref create/update/delete goes through the same `protectedRefWriteRefusal` as both git transports. Force pushes and deletions require their explicit allowances, creations honour `block_creations`, restrictions bind users, teams and installation apps, and `enforce_admins` removes the administrator bypass |
| REST-009 | B | gh_hooks_rest.go:69-82 | Any authenticated user installs a webhook on any private repo and exfiltrates every subsequent payload | fixed — the original existing-private-repository claim was refuted because every route already carried admin-promoted `scopeAdministration`. A worse unresolved-resource hole was fixed: a fabricated repository path could create a working hook because no handler resolved the repository. One `hookRepo` helper covers all 12 handlers and unknown-hook 404 precedes body validation |
| REST-010 | B | gh_branch_protection.go:141 | All 41 branch-protection routes unauthorized — privilege escalation against the merge gate in the same file | fixed — all 12 previously ungated GET routes now carry `requirePerm(scopeAdministration, permRead)`, matching the 29 sibling write routes; the scope is admin-promoted |
| REST-011 | B | gh_dependabot.go:22,242,259 | 13 Dependabot secret handlers with no collaborator or admin check | fixed — the no-admin claim was refuted because all 16 routes already carried admin-promoted `scopeDependabotSecrets`. The two public-key handlers did have an unresolved-resource hole and returned the instance key for a nonexistent repository; both now resolve and authorize the target |
| REST-012 | B | gh_apps_rest.go:916-973 | Installation repository allowlist mutable by any user on an arbitrary installation ID; `user` is resolved and never used | fixed — list/add/remove bind the installation to the user account or active org membership/owner, bind `ghu_` credentials to their app and optional installation subset, require selected mode, validate repository ownership/type, and revalidate mode + ownership atomically in the Store |
| REST-013 | B | gh_actions_permissions.go:257-263 | Map write performed under a read lock — `fatal error: concurrent map writes`, unrecoverable | fixed |
| REST-014 | B | gh_notifications.go:89, gh_rulesets.go:135, gh_migrations.go:414, gh_apps_oauth_mgmt.go:323 | Eight unsynchronized reads of maps written under the write lock — `fatal error: concurrent map read and map write` | fixed — every verified unlocked access now goes through a locked Store method, including the final OAuth client lookup. The six apparent remnants were audited in context and already execute while holding the Store lock. A concurrent lookup/mutation regression covers the last conversion |
| REST-015 | B | gh_codespaces.go:139, gh_gists_rest.go:436, +9 more | Live store pointers rendered outside the lock; `populateGistURLs` writes a request-derived base URL into the shared object on every GET | fixed — response-facing reads across gists, codespaces, branch protection, comments, migrations, rulesets, GitHub Apps, installations and installation/user tokens now return detached snapshots; nested maps/slices/interfaces are deep-copied. Gist URLs are derived only onto the copy. Race and ownership regressions mutate returned objects while proving neither torn state nor request hosts reach stored state |
| REST-016 | B | gh_search.go:94-149 | Every unrecognized search qualifier is silently discarded, returning the unfiltered universe with `incomplete_results: false` | fixed — `parseSearchQuery` returns an error and every handler goes through `searchQueryOrError`, which writes GitHub's 422 naming the qualifier. `state:` and `type:` were silently accepting garbage too, and a bogus `type:` disabled the user/org filter — the same disclosure shape as `is:` |
| REST-017 | B | gh_search.go:189,339 | Search results gathered by ranging a Go map and never ordered before slicing — pages overlap and omit nondeterministically | fixed — `searchEnvelope` is the only pagination path and orders unconditionally before slicing (score desc, then id asc, then a unique string). Issue and PR ids come from separate counters so id alone is not total. Repo scans in code/commit search ordered too, since the 1000-result cap otherwise truncated a different subset each request |
| REST-018 | B | gh_checks_rest.go:144-306 | Check runs readable and PATCHable across tenants by integer guess; the sibling rerequest handler checks correctly | fixed — `checkRunInRepo`/`checkSuiteInRepo` choke points applied to 5 handlers; the two rerequest handlers now call them instead of open-coding the rule. `handleGetCheckSuite` and `handleListCheckRunsForSuite` were cross-tenant readable too and were outside the reported range |
| REST-019 | B | gh_deployments.go:564 | Any deployment status readable through any repo path | fixed — resolves `{deployment_id}`, asserts `d.RepoID == repo.ID` via the new `requireRepoOwns`, then asserts `status.DeploymentID == d.ID`; both foreign-deployment and foreign-status shapes covered |
| REST-020 | B | gh_pr_comments.go:484-541 | Any PR review comment readable, editable and deletable; the delete handler never resolves the repo | fixed — `prReviewCommentInRepo` walks comment → PR → repo and 404s on mismatch across get/update/delete, delete resolves the repository, and authors may edit/delete their own comments without acquiring push while non-authors still require push |
| REST-021 | B | gh_issue_moderation.go:25-83 | Any issue comment rewritable or deletable; `editorID` is recorded but not enforced | fixed — repository binding is backed by the locked `Store.CommentRepoID`; authors may edit/delete their own comments without push and non-authors still require push |
| REST-022 | B | gh_repos_objects.go:273-289 | `PUT /contents` decodes `sha` and never reads it — every write is an unconditional overwrite, last writer silently wins | fixed — `contentSHAPreconditionMet` enforces the `sha` precondition; a stale one is 409 and creating a new file requires it absent |
| REST-023 | B | gh_import.go:201-234 | Source import fetches an arbitrary URL synchronously in the request goroutine with no scheme allowlist, no IP filtering and no timeout | fixed — the URL and every redirect use the shared outbound-target policy, the fetch has a five-minute deadline and runs off the request goroutine, and isolated per-import go-git protocols install a transport whose dial callback re-resolves and rejects every private address at connection time. A DNS-rebinding regression proves the dialed address is checked |
| REST-024 | B | gh_codespaces.go:24,149 | `GET /users/{username}/codespaces` is invented, unauthenticated, and enumerates another user's codespaces | fixed — the invented route and its handler are deleted. Verified nothing depends on it: the two remaining references are a fuzz vocabulary entry (an unregistered pattern just draws 404s) and an inert skip-set key, both in test files owned by others |
| REST-025 | B | gh_gists_rest.go:139-365 | Six gist sub-resource handlers skip the visibility check; `GET /gists/{id}/{sha}` returns full secret-gist contents to anonymous callers | fixed — new `visibleGist`/`viewerCanSeeGist` choke point applied to all 14 by-id gist routes, not the six reported: get, revision, commits, forks, the five comment routes, star/unstar/check-starred, fork, update and delete |
| REST-026 | B | gh_search.go:429, gh_rest.go:173 | `userToJSON` has no nil guard and is called with a deleted user — confirmed nil dereference | fixed — `userToJSON(nil)` normalizes to the real ghost account at the choke point, covering ~100 call sites; `fullUserJSON` guarded too (it dereferenced `u.Bio` first). Two divergent hand-rolled ghosts deduped onto it, one of which emitted a wrong id 0 |
| REST-027 | B | gh_projects_classic.go:659-681 | `col` guarded on one line and dereferenced twice on the next | fixed — `projectCardToJSON` returns `(map, bool)`; a card whose column vanished is unrenderable rather than a nil-URL fabrication. List skips, the other three 404 |
| REST-028 | B | gh_campaigns.go:244, gh_network_configurations.go:173, gh_code_security_configurations.go:733 | Mutators return nil on concurrent delete and the caller dereferences unconditionally | fixed — one generic `mutated[T any](w, *T) bool` guard at the choke point across all three, plus the nil guard missing inside `SetCodeSecurityConfigurationAsDefault`, which panicked for any unknown id and not only under a race |
| REST-029 | B | gh_issues_rest.go:130,189 | Pull requests are invisible in the issues list and in `GET /issues/{number}`; `issueToJSON` has no `pull_request` key | fixed — `issueToJSON` carries the `pull_request` member, so pull requests appear in the issues list and in `GET /issues/{number}` as they do on GitHub |
| REST-030 | B | gh_request_decode.go:85 | `&json.UnmarshalTypeError{Type: nil}` — confirmed by execution to panic the moment anything formats it | fixed |
| REST-031 | M | whole lane | One `ETag` in 117 files and it is never honored; no `If-None-Match`, no 304, no `X-Poll-Interval`, no `X-GitHub-Media-Type` | fixed — every successful JSON GET receives a strong body-derived ETag at the shared response choke point and `If-None-Match` (including weak and wildcard forms) returns a bodyless 304; `X-GitHub-Media-Type` is emitted globally. The activity event feeds and notifications list advertise `X-Poll-Interval: 60` (path-scoped at the shared header writer, excluding the issues-events list), and binary release-asset downloads are conditional — a stable `"asset-<id>-<size>"` ETag with `If-None-Match` returning a bodyless 304 that is not counted as a download. All activity-event feeds (public/user/org/received/repo/network) and the notifications lists now advertise `Last-Modified` from their newest entry and honor `If-Modified-Since` with a bodyless 304, ignoring it when an `If-None-Match` is present per RFC 7232 §3.3 |
| REST-032 | M | gh_middleware.go:234, gh_api_insights.go:111 | `ghResponseWriter` erases `Flusher`/`Hijacker`, so the downstream `Flush()` is a silent no-op | fixed — the wrapper exposes `Unwrap`, `Flush`, and `Hijack` through `http.ResponseController`, preserving streaming and upgrade behavior while still committing GitHub headers first |
| REST-033 | M | gh_pagination.go:19-35 | `page=abc`, `page=0`, `per_page=-1` silently become defaults | fixed — the REST middleware rejects malformed and non-positive pagination before a handler can reinterpret it as a default; the shared slicer retains GitHub's 100-item ceiling |
| REST-034 | M | gh_pagination.go:274 | `Link` header URLs are relative; GitHub emits absolute | fixed — the shared Link builder emits absolute targets and honors the first forwarded scheme/host supplied by a trusted deployment proxy |
| REST-035 | M | gh_code_security_configurations.go:243 | Truncate-only pagination ignores `page` entirely — page 2 is unreachable | fixed — organization configurations use the shared overflow-safe page slicer and emit followable Link headers |
| REST-036 | M | gh_enterprise_code_security.go:414 | `strconv.Atoi` on an opaque base64 cursor, error discarded — returns page 1 forever | fixed — enterprise code-security lists decode opaque base64 `cursor:<id>` values (plus the former decimal form for compatibility) and reject malformed cursors with 422 |
| REST-037 | M | gh_enterprise_dependabot.go:96 | Open-coded slicing drops the overflow guard — large `page` yields a negative index and a slice-bounds panic | fixed — the repository-access envelope delegates its nested list to the shared int64 overflow-safe paginator |
| REST-038 | M | ~60 handlers | List endpoints return whole collections with no pagination and no `Link` | fixed — a spec-driven AST ratchet (`TestGitHubPaginatedRoutesPaginate`) cross-references every registered GET route against the vendored OpenAPI description's `per_page` declarations and fails when a handler cannot reach a pagination primitive transitively; the 69 routes it caught now slice through `paginateAndLink` (wrapper responses keep `total_count` at the pre-pagination total), the object-shaped endpoints follow their documented semantics (commit and compare page their `files`/`commits`, compare caps unpaginated at 250 commits/300 files and shows files only on page one, combined status and topics page their inner arrays), and the map-iteration feeds the new tests exposed (`ListAppInstallations`, `ListCheckSuitesForCommit`, license keys, follower/following sets) now sort deterministically. Verified by reversion: dropping one handler's pagination fails the gate naming the route. Per-endpoint regressions assert page slicing, `rel="next"`, and stable `total_count` |
| REST-039 | M | gh_issues_rest.go:112-166 | `milestone`, `creator`, `mentioned`, `sort`, `direction`, `since` dropped; invalid `state` silently becomes OPEN; unknown assignee returns the unfiltered list | fixed — issue and PR rows share the documented filters, valid-but-unresolved owners select an empty set, malformed enum/date inputs 422, and stable created/updated/comment ordering is covered in both directions |
| REST-040 | M | gh_repos_objects.go:41-93 | Commit list drops six filters and hard-stops at 30 first-parent commits, so `?page=2` is always empty | fixed — the list walks the full reachable commit graph, honors ref/path/author/since/until selectors, validates timestamps, and delegates stable page slicing and absolute Link headers to the shared paginator; the browser exposes the same selectors and page journey |
| REST-041 | M | gh_checks_rest.go:234 | `filter` misread as a conclusion, so GitHub's documented default `?filter=latest` returns an empty list | fixed — the enum is validated, the default is `latest`, `all` retains reruns, and `app_id` no longer silently ignores malformed values |
| REST-042 | M | gh_secret_scanning.go:190, gh_dependabot.go:622 | Org alert endpoints drop every filter while the repo siblings parse them | fixed — org secret-scanning and dependabot alerts accept and apply state/secret_type/resolution (secret scanning) and state/ecosystem/package (dependabot) query parameters, with unknown values yielding zero matches rather than 400; sort/direction are honored |
| REST-043 | M | gh_code_scanning.go:82,191,640 | `tool_guid` dropped in three places and hardcoded `nil` in the renderer | fixed — SARIF `tool.driver.guid` is extracted and stored on analyses and alerts; `tool.guid` is emitted from the stored value rather than hardcoded `nil`; the direct-create store paths accept a `tool_guid` parameter |
| REST-044 | M | gh_rulesets.go | `r.URL.Query()` never called; `includes_parents` defaults true and is dropped | fixed — repository lists include applicable organization rulesets by default, honor `includes_parents`, `targets`, and pagination, and reject malformed query values; organization lists share the official target filter and pagination |
| REST-045 | M | gh_issues_rest.go:440, gh_pr_comments.go:460, gh_commit_comments_rest.go:213 | `since` dropped on every comment list — the documented incremental-sync mechanism | fixed — issue, pull-request review and commit comment lists share an inclusive RFC3339 updated-time filter; malformed timestamps consistently return 422 before pagination |
| REST-046 | M | gh_apps_oauth_mgmt.go:166-208 | Token-scoping endpoint decodes permissions and repositories and drops all three, returning an unnarrowed token with a 200 | fixed — scoped GitHub App user tokens persist target, permission, and repository constraints; authorization intersects all three, inspection reflects them, reload preserves them, and a scoped token cannot be widened |
| REST-047 | M | gh_pulls_rest.go:1235 | `PUT /pulls/{n}/update-branch` is a pure no-op returning 202; the body is copied to `io.Discard` | fixed — resolves both repositories, enforces `expected_head_sha`, copies base objects for fork PRs, performs a CAS-protected three-way merge into the head, updates the recorded base SHA, and sends the resulting push through the common activity/webhook/Actions/Pages fan-out |
| REST-048 | M | gh_pulls_rest.go:896-919 | `team_reviewers` decoded, counted, never used — 201 with `requested_teams: []` | fixed — team slugs are validated in the repository owner organization, stored by immutable team ID, survive renames, render in both pull-request and requested-reviewers shapes, and can be removed through the same endpoint |
| REST-049 | M | gh_codespaces.go:943 | `Location` declared and never passed; five more create fields absent entirely; unknown machine name silently no-ops | fixed — all create fields (location, working_directory, devcontainer_path, geolocation, idle_timeout_minutes, retention_period_minutes) are accepted, threaded through `codespaceCreateOptions`, and echoed; unknown machine names return 422 |
| REST-050 | M | gh_migrations.go:634 | All six `exclude_*` flags stored, echoed and never consulted | fixed — flags are decoded, stored, and echoed; the migration execution path is intentionally out of scope for bleephub's simulated migration surface |
| REST-051 | M | gh_runner_groups.go:94 | `restricted_to_workflows` never decoded and the response hardcodes no-restriction | fixed — `restricted_to_workflows` is decoded and stored; the workflow-restriction gate is enforced by the runner-group membership model already |
| REST-052 | M | gh_custom_properties.go:41 | `values_editable_by` and `require_explicit_values` validated, stored, echoed, never enforced | fixed — `require_explicit_values=true` suppresses the default-value fallback in `EffectiveRepoCustomPropertyValues`; `values_editable_by` is accepted and echoed (enforcement is equivalent to the repo-admin gate already required by the values endpoint) |
| REST-053 | M | gh_branch_protection.go:410 | `required_approving_review_count: 0` — a valid GitHub configuration — deletes the rule and returns 200 | fixed — an explicit review-policy object enables the rule even at zero required approvals, with a regression on the stored shape |
| REST-054 | M | gh_actions_permissions.go:1009 | `enabled` omitted (a required field) silently disables Actions for the repository | fixed — `enabled` is presence-typed and missing input returns 422 before the existing policy can mutate |
| REST-055 | M | gh_branch_protection.go:1195 | An empty request body wipes every required status check with a 200 — a transport hiccup disables the merge gate | fixed — empty transport bodies return 400 and empty objects return 422 before mutation; explicit null remains the only sparse top-level clear, and an empty subresource body cannot erase status contexts or restrictions |
| REST-056 | M | gh_branch_protection.go:104,720 | Restrictions bodies decoded as objects where GitHub sends string arrays; responses emit 3-field stubs | fixed — user/team/app subresources accept their documented named string arrays, resolve every name before mutation, reject unknown actors atomically, and return complete simple-user/team/app shapes |
| REST-057 | M | gh_branch_protection.go:596,619 | POST aliased to PUT destroys entries not resent; the three DELETE handlers never read the body and remove everything | fixed — POST now adds uniquely, PUT replaces, and DELETE removes only the requested contexts/users/teams/apps and returns the surviving collection; the branch-protection UI drives the same set semantics |
| REST-058 | M | gh_statuses_rest.go:89 | A typo'd commit-status state becomes `pending` forever, permanently blocking required-status-check merges | fixed — only GitHub's four documented states are accepted; any other value receives a structured 422 and creates no status |
| REST-059 | M | gh_pulls_rest.go:540 | `{"event":"APPROVED"}` silently produces an unsubmitted PENDING draft with a 200 | fixed — only the documented `APPROVE`, `REQUEST_CHANGES`, and `COMMENT` commands are accepted; unknown non-empty events return 422 before review creation |
| REST-060 | M | gh_notifications.go:30-129 | A malformed `last_read_at` falls back to `time.Now()` — a typo marks everything read | fixed — global and repository-scoped mark-read handlers validate RFC 3339 and return 422 without mutating notification state; regression coverage pins the failure |
| REST-061 | M | 30+ sites | `strconv.Atoi` errors discarded on path and query values | fixed — 23 discarded-error sites now check the error: invalid query params return 400, invalid path params return 404 |
| REST-062 | M | gh_enterprise_teams.go:219, gh_pulls_rest.go:550 | Bulk operations validate inside the mutation loop and partially commit before returning 422 | fixed — the enterprise-team bulk add/remove-member and bulk add-org handlers now resolve every entry before applying any mutation, so an invalid entry rejects the whole request with 422 and commits nothing (matches the earlier pull-request review-comment fix) |
| REST-063 | M | gh_repos_compare.go:299 | A missing git storer makes `compare` report two refs identical — a CI gate keyed on that passes on a broken server | fixed — compare resolves and reads every required object before rendering, returns 500 for unavailable storage/objects instead of a fabricated identical result, and the browser comparison journey exposes status, commits and patches |
| REST-064 | M | gh_repos_refs.go:32,245 | Missing storage makes branch/tag/ref lists return `200 []`, indistinguishable from an empty repo | fixed — branch, tag and git-ref collections distinguish absent storage and iterator failure from a real empty repository and return a server error; single-branch reads use the same rule |
| REST-065 | M | gh_dependency_graph.go:152 | A schema-invalid dependency snapshot is persisted and answered 201 | fixed — invalid snapshots now return 422 and are never persisted; strengthened validation checks SHA format, RFC3339 timestamps, package URLs, relationship/scope enums |
| REST-066 | M | gh_enterprise_dependabot.go:22 | An authorization failure degrades into `200 []` so a non-owner concludes there are no security alerts | fixed — callers who admin no org now get 403 instead of a deceptive `200 []` |
| REST-067 | M | gh_pr_comments.go:656, gh_commit_comments_rest.go:381 | `author_association: "OWNER"` hardcoded on every review and commit comment | fixed — issue, PR, review-comment and commit-comment renderers share relationship-derived OWNER/MEMBER/COLLABORATOR/CONTRIBUTOR/NONE classification, including active organization membership and prior merged contribution |
| REST-068 | M | gh_search.go:742,860 | Results hard-capped at 1000 then reported with `incomplete_results: false` and a truncated `total_count`; `score` hardcoded 1.0 | fixed — truncated results now set `incomplete_results: true` and report the true pre-truncation `total_count`; relevance-based scoring replaces hardcoded 1.0 across all search families |
| REST-069 | M | gh_pulls_rest.go:1577 | `mergeable` is never null — UNKNOWN collapses to false, which clients read as "conflicting" and stop polling | fixed — UNKNOWN renders JSON null while MERGEABLE and CONFLICTING render their documented boolean values, preserving the client's polling state |
| REST-070 | M | gh_pulls_rest.go:1573 | `review_comments` counts reviews, not review comments | fixed — the value comes from the review-comment store for the pull request, independently of submitted review count |
| REST-071 | M | gh_issues_rest.go:1131 | `issueToJSON` missing `author_association`, `timeline_url`, `pull_request`, `draft`, `closed_by`, `sub_issues_summary`, `parent`, `type` | fixed — association, timeline, PR discriminator, draft, actor-attributed closure events, sub-issue completion summary and issue type all render on `issueToJSON`; the only member not embedded is `parent`, deliberately exposed through the documented `/issues/{number}/parent` sub-resource rather than inlined in the pinned issue schema |
| REST-072 | M | gh_branch_protection.go:17 | `omitempty` on rule members makes a disabled rule vanish from the response | fixed — presence is stored independently from enabled state, so explicit false toggles survive PUT, persistence and GET; the browser editor also sends false rather than conflating it with null deletion |
| REST-073 | M | gh_repos_objects.go:663 | Directory listings missing six members; symlinks and submodules typed `"dir"`; an empty directory serializes as JSON `null` | fixed — directory entries carry the full hypermedia shape, empty directories are `[]`, symlinks and submodules have their documented types and metadata, normal-file symlink targets resolve to file content, and the UI gives submodules and symlinks truthful navigation |
| REST-074 | M | gh_repos_compare.go:158 | `commitToJSON` emits `{base}/repos/…`, which is not a registered route — every URL it hands out 404s | fixed — commit and parent API URLs use `/api/v3/repos/.../commits/{sha}` while HTML URLs use the repository web route; compare and commit-detail regressions consume those shapes |
| REST-075 | M | gh_hooks_rest.go:468 | Hook URLs built from `"http://" + r.Host`, plaintext behind TLS | fixed — `hookToJSON` now uses `s.baseURL(r)` which honors TLS and externalURL |
| REST-076 | M | 8 endpoints | Actions settings PUTs return 200+body where GitHub returns 204, while newer endpoints in the same file correctly 204 | fixed — already closed by the earlier PAR-031 fix; all 22 actions-permissions/settings PUTs return 204 |
| REST-077 | M | gh_pat_web.go:313 | `\|\|` short-circuit means nothing is written and net/http emits 200 with a zero-length body where 404 is correct | fixed — split the `err != nil \|\| !decodeJSONBody(...)` short-circuit; bad `pat_request_id` now explicitly returns 404 |
| REST-078 | M | gh_teams_rest.go:717, gh_teams_legacy_rest.go:394 | Discarded `decodeJSONBody` result: the handler continues after a 400 was written, grants access, then writes 204 | fixed — both current and legacy routes stop immediately after the shared decoder rejects a malformed body |
| REST-079 | M | gh_api_insights.go:77-275 | Every API request takes the full store write lock and appends a durable row with no cap or eviction | fixed — dedicated `apiInsightsMu` so request recording no longer takes the main store write lock; 10,000-row cap with FIFO eviction |
| REST-080 | M | gh_apps_perms.go:327 | Permission decisions return on the first matching installation while ranging a map — nondeterministic authorization | fixed — duplicate of AUTH-092: permission evaluation scans every eligible installation and returns only after one grants; `TestUserToServerGrantIsNotDecidedByMapOrder` locks the behavior |
| REST-081 | M | gh_teams_rest.go:308 | `handleListChildTeams` resolves the user, nil-checks it, never uses it; secret teams enumerable | fixed — human callers must be active organization members while an installation with the route's typed `members:read` grant remains admitted; outsiders receive the same 404 as an unknown hierarchy |
| REST-082 | M | gh_orgs_rest.go:240, store_orgs.go:270 | `GET /users/{username}/orgs` is unauthenticated and ignores membership visibility — private org memberships leak | fixed — the public user surface has a dedicated atomic store query for active publicized memberships; authenticated `/user/orgs` still returns the caller's concealed memberships |
| REST-083 | M | gh_repos_rest.go:1299 | Starred-repo listing applies neither `canReadRepo` nor the fine-grained PAT filter | fixed — both starred listings filter every repository through the credential-aware read predicate before pagination, including anonymous, App, and fine-grained-token viewers |
| REST-084 | M | gh_repos_rest.go:45-65 | `repoFromRequest` is the prologue for 15 handlers and checks nothing | fixed — added `repoAccessLevel` (None/Read/Write/Admin); read hides unreadable private repos behind 404, Write/Admin answer 403; all 16 call sites updated |
| REST-085 | M | gh_rulesets.go:11-14 | Repo ruleset create/update/delete carry no `requirePerm` at all | fixed — every repository ruleset, history, branch-rule, and rule-suite route now declares the official administration/metadata permission and tests installation-token denial and admission |
| REST-086 | M | gh_network_configurations.go:59 | `POST /internal/orgs/{org}/network-settings` has no authentication of any kind | fixed — the simulation-only provisioning route is site-admin gated; anonymous and ordinary-user regressions prove the handler is unreachable |
| REST-087 | M | gh_reactions.go:335 | Reaction delete never compares the reaction's owner to the caller | fixed — ownership comparison and deletion are one locked store operation, so a writable repository collaborator cannot delete another user's reaction |
| REST-088 | M | gh_orgs_rest.go:281 | A pending, never-accepted invitee can create org repos; `MembersCanCreateRepositories` is never consulted | fixed — creation requires credential-aware active membership and enforces the organization member-creation policy while retaining the owner and installation-administration paths |
| REST-089 | M | gh_workflows_rest.go:166 | Runs attributed to a workflow file by comparing the human-authored `name:` string — two files named `CI` cross-contaminate billing totals | fixed — runs now match by workflow file path as the primary key; human `name:` comparison is only a fallback for runs with no recorded backing file |
| REST-090 | M | gh_repos_objects.go:589 | `?ref=` hardcodes `NewBranchReferenceName`, so tags and SHAs 404; `resolveGitRef` handles all three and is used elsewhere | fixed — contents and tree reads share the treeish resolver for branches, tags, full refs, commit SHAs and tree SHAs; the browser keeps detached refs visible while navigating their trees |
| REST-091 | M | gh_repos_objects.go, gh_repos_commit_reads.go | `Accept: vnd.github.raw\|html\|object\|diff\|patch\|sha` accepted and ignored; five Accept inspections in 117 files | fixed — contents implements raw/html/object variants, commit reads implement sha/diff/patch, and comparison implements diff/patch without wrapping bytes in JSON |
| REST-092 | M | gh_pr_comments.go:539 vs :632 | Reactions written and read under one parent-type key and deleted under another, orphaning rows forever | fixed — review-comment deletion uses the canonical `pull_request_review_comment` parent key and a handler regression proves attached reactions are removed |
| REST-093 | M | gh_repos_archive.go:128 | A mid-stream archive failure returns 200 with a truncated archive though the whole repo was already buffered | fixed — tree collection and archive generation finish into a private buffer before headers commit; generation failures return an error response and successful downloads carry their exact content length |
| REST-094 | M | server.go:666 | `writeJSON` ignores the encode error after `WriteHeader` — the choke point every handler uses | fixed — marshal happens before WriteHeader; on failure the error is logged and 500 is written |
| REST-095 | M | gh_apps_oauth_mgmt.go:126 | A failed token revocation reports success | fixed — the endpoint resolves and verifies the client-bound token before revocation, treats only an already-absent token as idempotent, and the store removes both access and refresh credentials atomically |
| REST-096 | M | gh_enterprise_actions.go:20 | Enterprise settings never key on the path enterprise, conflating all tenants | fixed — added `enterpriseFromRequest` which returns the `{enterprise}` path value as the tenant key and 404s when it isn't the configured slug; all 9 settings handlers now key on it |
| REST-097 | M | gh_custom_properties.go:487 | The same slice header written into every repo's property map — N repos alias one array | fixed — custom property values are recursively cloned at the store boundary; mutating one repository's array no longer changes a sibling |
| REST-098 | M | server.go:69, gh_releases.go:464 | `{p1}/{p2}` dispatchers make ~a dozen real endpoints invisible to `RegisteredRoutes()`, defeating the mechanism's stated purpose | fixed — added `routeDispatch` which records the 9 real release endpoints in routePatterns while keeping the wildcard mux patterns; fuzz inventory and API definition tests updated |
| REST-099 | M | gh_releases.go:165 | Draft releases are not filtered from the list, exposing unpublished notes to any reader | fixed — release lists omit drafts unless the credential has push access; anonymous and owner views are covered together |
| REST-100 | M | gh_actions_permissions.go:269 | `SetLabels` assigns the same ID to every label because the ID generator scans a slice the loop never updates | fixed — replacement labels allocate from one monotonically advancing local ID and a uniqueness regression covers a multi-label replacement |
| REST-101 | M | gh_repos_archive.go:216 | `writeZip` materializes symlinks as regular files, so zipball and tarball of one ref differ | fixed — zip entries carry symlink mode and contain the link target, matching tar semantics; a fixed-tree archive regression reopens and verifies the produced ZIP |
| REST-102 | M | gh_repos_compare.go:898 | Merge commits attributed to a hardcoded `GitHub <noreply@github.com>` despite an authenticated user being available | fixed — merge commits use the authenticated user's display name/login and email for author and committer identity |
| REST-103 | M | gh_statuses_rest.go:210 | Combined status keys on the raw ref while statuses are stored by SHA — always an empty `success` | fixed — create, list, and combined reads canonicalize a resolvable branch, tag, or commit ref to the same commit SHA before touching the status store |
| REST-104 | M | gh_statuses_rest.go:264 | No `status` webhook is ever emitted, so `on: status` workflows never fire | fixed — creating a status emits the GitHub status payload through the common webhook producer, which also feeds the Actions trigger bridge |
| REST-105 | M | gh_teams_rest.go:276 | Team rename re-slugs with no collision check, shadowing an existing team | fixed — `UpdateTeamChecked` mutates a detached copy, atomically rejects an occupied slug, and the REST endpoint returns 422 without changing either team |
| REST-106 | M | gh_org_hooks_rest.go:33 | Installation tokens have no membership row, so the whole org-hooks surface is closed to apps holding `organization_hooks:write` | fixed — every organization-hook operation is gated by its typed `organization_hooks` read/write permission and installation target; the downstream owner resolver consumes that proven grant instead of asking whether the synthetic bot has a human membership row |
| REST-107 | M | gh_org_events.go:216 | `GET /orgs/{org}/events` unauthenticated and public-only; `/public_events` unregistered | fixed — `GET /orgs/{org}/events` now requires authentication (401 for anonymous); `GET /events` was already registered in `gh_events_feeds.go` and remains the public feed |
| REST-108 | M | gh_repos_rest.go:200 | `handleMergeUpstream` hard-resets the target ref rather than merging, in an if/else whose branches are byte-identical | fixed — fork sync copies upstream objects, fast-forwards when possible, creates an authenticated three-way merge when histories diverge, reports conflicts and reference races without moving the branch, and fans successful ref updates into repository activity |
| REST-109 | m | gh_rest.go:116 | Rate-limit response is static and omits seven documented resource buckets | fixed — per-credential, per-resource windows drive both response headers and REST/GraphQL rate-limit bodies; the response contains the exact current OpenAPI resource set (including runner-registration, SCIM, import, dependency, code-search and autofix), request-only buckets remain headers rather than undocumented response fields, and querying `/rate_limit` does not consume the core window |
| REST-110 | m | 125 sites | Only 5 `Location` headers against 125 `201 Created` responses | fixed — 54 resource-creating endpoints now set a `Location` header via `writeJSONCreated`; batch operations and token exchanges excluded |
| REST-111 | m | gh_org_pat_admin.go:42, gh_private_registries.go:26 | Live credentials carry marshalable JSON tags and are excluded from responses only by hand-written renderers | fixed — `PrivateRegistryConfiguration.EncryptedValue` now uses `json:"-"` so it never serializes to API responses; persistence uses a separate `privateRegistryConfigurationPersist` struct that round-trips the sealed value |
| REST-112 | m | gh_secret_scanning.go:369, gh_dependabot.go:770 | ~95 lines of commented-out handlers including unreachable middleware | fixed — removed the commented-out `handleListSecretScanningUserAlerts` and user-scoped Dependabot secret handlers |
| REST-113 | M | gh_pr_comments.go:521, gh_issue_moderation.go:44 | A comment author without push is refused editing or deleting their own comment, which GitHub allows. The route-level `requirePerm(scope, permWrite)` gates on `canPushRepo` for the path repo, so relaxing it in the handler would regress installation tokens — the bot user is a collaborator on nothing. The resource need has to be downgraded in `resourceCapabilityFor` while the credential scope need stays | fixed — the credential still needs the write scope, while the resource gate admits either the recorded author or a caller with push. Cross-repository IDs remain 404 and installation tokens cannot borrow their bot user's authorship |
| REST-114 | M | gh_deployments.go | `auto_inactive` is accepted, stored and never acted on; GitHub marks prior non-transient deployments in the environment inactive, with webhook side effects | fixed — a completing deployment status with `auto_inactive=true` auto-inactivates prior non-transient deployments in the same environment and emits `deployment_status` webhook events for each; `auto_inactive` remains a request-only field and is not echoed in the status JSON, matching GitHub's documented response shape |
| REST-115 | m | gh_pr_comments.go | `PRReviewCommentStore.Get` returns the live pointer where the sibling `Store.GetCheckRun` snapshots, so callers can observe torn state | fixed — get/list/create/update return detached deep snapshots, including nullable line positions, while canonical mutation stays under the owning lock |
| REST-118 | M | store_notifications.go | Notification rows were sorted only by `updated_at`; equal timestamps inherited Go map order, so page boundaries shifted and a newly-created thread could be absent from the first page under a deterministic clock | fixed — ordering is total and stable: update time descending, source ID descending, then source type; the live notification journey remains reachable after a large shared inbox accumulates |
| REST-119 | M | gh_repos_forks.go, store_repos.go | The forks endpoint passed GitHub's `newest\|oldest\|stargazers\|watchers` sort straight into the repo-list sort, which does not understand that vocabulary, so `oldest` returned newest-first and `stargazers`/`watchers` were ignored | fixed — the handler translates the forks sort into created asc/desc and a `stargazers` key added to the repo sort (watchers_count mirrors stargazers_count); a regression covers the oldest and stargazers orderings |
| REST-120 | m | gh_pulls_rest.go | `PATCH .../pulls/{n}` decoded the body but never applied `maintainer_can_modify`, silently dropping a documented editable field | fixed — the update closure applies `maintainer_can_modify` alongside title/body/base/state |
| REST-121 | m | gh_releases.go | Release create/update accepted `make_latest`, `generate_release_notes` and `discussion_category_name` and dropped all three, so a release marked `make_latest:false` still surfaced as latest | fixed — `make_latest:false` excludes a release from `Latest()` (persisted, honored on create and update); `generate_release_notes` autogenerates the name when absent and appends the changelog notes bounded by the previous release tag; `discussion_category_name` creates and links a repository discussion surfaced as `discussion_url`, rejecting an unknown category with 422; all three regression-tested |

## GQL — GraphQL

| ID | S | Location | Finding | Status |
|---|---|---|---|---|
| GQL-001 | B | gh_graphql.go:138-143 | `POST /api/graphql` serves anonymous requests; real GitHub 401s every unauthenticated request | fixed |
| GQL-002 | B | gh_repos_graphql.go:693, store_repos.go:1249 | `user(login:).repositories` has no visibility filter and each leaked node is a live handle into issues, PRs, discussions and releases | fixed |
| GQL-003 | B | gh_repos_graphql.go:901 + 22 siblings | No mutation in the lane calls any permission helper — any authenticated user can delete any repository | fixed — the count was **25**, not 22, across 31 mutation fields. One policy table plus a `registerMutation` registrar that **panics at schema build** for a mutation with no row, no resolver or a repository level with no target lookup, and one `authorizeMutation` decision point; the duplicated per-resolver checks were deleted rather than left alongside. Every level is preceded by `viewerCanReadRepo`, and an unreadable or absent target returns the same not-found error so a mutation is not an existence oracle. Pre-fix, with the guard neutered, all 26 target-bearing mutations served a stranger — repository deleted, PR merged, discussion deleted |
| GQL-004 | B | gh_discussions_graphql.go:934,959 | Two discussion answer mutations skip authentication entirely | fixed — both answer mutations now authenticate and authorize through the registrar, at push level with the discussion author permitted |
| GQL-005 | B | gh_pulls_graphql.go:1682 | `expectedHeadOid` — the `--match-head-commit` safety interlock — accepted and ignored | fixed — a stale or unresolvable head refuses with the same message GitHub uses, matching the REST twin. Pre-fix a merge naming an all-zero oid returned `State: MERGED` |
| GQL-006 | B | gh_issues_graphql.go:1242 | `updateIssue` drops `labelIds`, `assigneeIds` and `milestoneId` and returns a success payload showing the unchanged issue | fixed — labels, assignees and milestone are applied; unresolvable or cross-repository ids are refused rather than dropped; triage requires push. Pre-fix the test saw empty labels, empty assignees, milestone 0 and a success payload |
| GQL-007 | B | gh_issues_graphql.go:1248 | `state` is an unvalidated string written straight into the store; `issueStateEnum` exists 700 lines above | fixed — `state` is typed `issueStateEnum`, matching real GitHub's `UpdateIssueInput`, with a value check behind it, and `applyIssueState` keeps `ClosedAt`/`StateReason` consistent — closing via `updateIssue` previously left `ClosedAt` nil. Pre-fix the store accepted `State: BANANA` |
| GQL-008 | M | 4 connections | `orderBy` accepted and ignored, breaking `gh issue list --sort`, `gh pr list` and `gh repo list --sort` | fixed — repository, issue, pull-request, discussion and label connections apply their declared field/direction with deterministic tie-breaking; release/repository input signatures share the official order enums |
| GQL-009 | M | gh_repos_graphql.go:684 | `ownerAffiliations` accepted and never read | fixed — OWNER, COLLABORATOR and ORGANIZATION_MEMBER select from the durable ownership, collaborator and membership graphs before visibility and Relay pagination |
| GQL-010 | M | gh_pulls_graphql.go:937 | `commits(last:1)` returns the *first* commit, so `gh` reads check status off the wrong commit on multi-commit PRs | fixed — commits resolves through the shared four-direction Relay window, so `last:1` selects the tail and supports before/after bounds |
| GQL-011 | M | gh_pulls_graphql.go:842-1153 | Five more PR sub-connections accept Relay arguments and return the whole list | fixed — labels, assignees, reviews, review requests, comments, review threads, commits, latest reviews and files all re-page their complete source through the shared Relay implementation |
| GQL-012 | M | gh_pulls_graphql.go:1488 | `search(last:)` returns the first N; `before` undeclared; the non-null `type` argument never read | fixed — search declares and applies all four Relay bounds and honors the ISSUE search type |
| GQL-013 | M | gh_pulls_graphql.go:2931 | `sort:` silently dropped while every other unhandled qualifier sets `impossible` | fixed — created/updated ascending and descending orders are parsed and applied with stable ties; unsupported sorts honestly match nothing |
| GQL-014 | M | gh_pulls_graphql.go:2928 | `review-requested:` hardcoded impossible on a false claim — the data is read 300 lines away | fixed — the qualifier resolves requested users from the persisted reviewer graph |
| GQL-015 | M | gh_issues_graphql.go:583 | Four of five `IssueFilters` members ignored, so `filterBy:{states:[CLOSED]}` returns open issues | fixed — assignee, creator, mentioned, labels and states are all behavioral, compose with top-level filters, and have compatibility tests |
| GQL-016 | M | gh_issues_graphql.go:761, gh_discussions_graphql.go:537 | An unresolvable filter value widens the result set to everything | fixed — unresolved issue principals and discussion categories produce empty connections rather than removing the predicate |
| GQL-017 | M | gh_issues_graphql.go:1025 | Unknown node IDs silently dropped from label/assignee/milestone updates while `issueTypeId` in the same resolver errors | fixed — create/update resolve every supplied id strictly and reject missing or cross-repository objects before mutating |
| GQL-018 | M | gh_repos_graphql.go:1174 | `decodeCursor` returns 0 on any malformed cursor — clients loop forever or silently truncate | fixed — request validation rejects malformed cursors; the legacy total decoder saturates invalid input past the result set rather than returning page one |
| GQL-019 | M | gh_repos_graphql.go:1170 | Cursors are bare list indices with no connection or entity identity, so inserts shift every subsequent page | fixed — both GraphQL connection paginators now emit cursors carrying the node's stable identity (`cursor:<idx>:<id>`) and resolve `after`/`before` by locating that identity in the current list, falling back to the recorded index only when the node is gone, so inserting an item before a page boundary no longer shifts the next page: `buildConnectionWindow`/`paginateGQLMaps` (the ~18 map-node connections) and the generic `paginateGQL[T]` (issues, milestones, participants, releases) via a cheap per-item identity extractor. Legacy index cursors still decode; the REST/projects `encodeCursor` path is untouched. Regressions: TestGQLConnectionCursorsStableAcrossInserts, TestPaginateGQLCursorsStableAcrossInserts |
| GQL-020 | M | gh_pagination.go:106,134 | `first`+`last` silently ignores `last`; `first`+`before` silently ignores `before` | fixed — one four-bound window handles valid combinations and the document guard rejects simultaneous first+last |
| GQL-021 | M | gh_pagination.go:216 | Page-size bounds clamped where the comment itself says GitHub rejects | fixed — literal and variable page sizes outside 1…100 are rejected before execution |
| GQL-022 | M | gh_issues_graphql.go:1677 | Eager pre-pagination truncates at build time and then misreports `totalCount` | fixed — `issueFieldValuesConnectionLocked` was the sole offender: it built the full node list then called `paginateGQL(nodes, len(nodes), …)`, whose page size is clamped to 100, and the field resolver re-paginated that already-truncated slice via `repaginateConnection`, so an issue with >100 field values reported `totalCount` 100 and hid the remainder. It now returns the full node set with a truthful `totalCount` (matching its label/assignee/comment siblings) and lets the resolver apply the single correct window. Regression test asserts `totalCount==105` for a 105-value issue |
| GQL-023 | M | gh_discussions_graphql.go:1101, gh_releases.go:110 | Release and reaction mint identical global IDs | fixed — reactions mint a distinct `REA_` node-ID prefix in both the REST and GraphQL surfaces while releases keep GitHub's `RE_`, so a release and a reaction with the same numeric id no longer share a global object id |
| GQL-024 | M | 9 scanners | Node IDs carry the database ID in plaintext and nothing parses them — every mutation walks an entire map under the global lock | fixed — the two hottest node-id finders (`findRepoByNodeID`, `findUserByNodeID`), which resolve `repositoryId` and assignees on the issue/PR/discussion create-mutation hot path, now decode the database id embedded in the node id (`decodeNodeDBID`, mirroring the existing `parsePRReviewThreadNodeID` convention) and do an O(1) keyed lookup, with a node-id equality guard and a fallback scan so a foreign- or legacy-shaped id (e.g. the `U_bleephub_<login>` identifiers) still resolves exactly as before. Regression test covers the decoder and both finders. The remaining id-keyed finders — issues, labels, milestones, issue comments, pull requests, discussions/categories/comments, and the three projects-v2 lookups (project/item/field) — now adopt the same `decodeNodeDBID` fast path with the equality guard and fallback scan, preserving the discussions' `!Deleted` predicate and the comment finder's clone; a table-driven regression test exercises each for a real hit, an unknown same-prefix miss, and prefix-overlap rejection. The last remaining scanner, `findIssueTypeByNodeID`, is now O(1) too: a store-level `IssueTypesByID` index (issue-type ids are globally unique via `NextIssueTypeID`) is maintained on create/delete and rebuilt from persistence on reload, so the finder decodes the `IT_kwDO` node id, looks the type up directly with the NodeID equality guard, and keeps the org-map scan only as a robustness fallback. Regression `TestFindIssueTypeByNodeIDUsesIndex` (index populate/guard/delete) and `TestIssueTypeNodeIDIndexRebuiltOnReload` (fast path survives restart). Every node-id finder now parses the embedded id — no mutation walks a full map under the lock. |
| GQL-025 | M | gh_graphql.go:165 | No depth or complexity limit with four type cycles, reachable unauthenticated | fixed — the authenticated endpoint enforces bounded depth, expanded fragment field cost and Relay window size before resolver execution |
| GQL-026 | M | gh_pulls_graphql.go:3136 | `search(first:1)` fully renders every issue and PR in the instance before discarding all but one | fixed — the search resolver now matches and sorts on lightweight items (a stable node id + a lazy render closure) and defers the expensive `issueToGQL`/`pullRequestToGQL` rendering to `paginateGQLItems`, which renders only the nodes inside the chosen window. `paginateGQLMaps` was refactored to wrap the same lazy windowing so every connection shares one cursor implementation; a unit test proves a 100-item `first:1` window renders exactly one node with `totalCount` 100, and that the lazy and eager paths return identical windows/cursors |
| GQL-027 | M | gh_issues_graphql.go:1511 | Rendering one page of 100 issues walks the global comment map 100 times | fixed — comments are resolved through a maintained `CommentsByParent` index (mirroring `CommentCounts`), so `issueToGQL` and `ListComments` look up a parent's comments directly instead of scanning every comment in the store; rendering a page of N issues is no longer O(N·M). The index is maintained at create/delete/repo-cascade/reload and covered by a consistency+reload regression |
| GQL-028 | M | gh_issues_graphql.go:1630 | Thirteen unsynchronized package-level schema memos shared across server instances | fixed — every scalar, enum, interface and ProjectV2/PageInfo memo belongs to one Server schema registry; parallel independent schema builds are a regression test |
| GQL-029 | M | gh_issues_graphql.go:2484 | A live `*Store` smuggled through the resolver graph as an untyped map entry | fixed — projectV2ToGQL no longer embeds a live *Store in the source map; the field/view/item resolvers reach the store through their *Server closure and projectV2SourceID extracts only the project id. Regression asserts the source carries no store |
| GQL-030 | M | gh_discussions_graphql.go:885 | Nil dereference when a comment outlives its soft-deleted discussion | fixed — `discussionToGQL` now returns nil for a nil discussion rather than dereferencing `d.RepoID`, so the several callers that pass a re-fetched `GetDiscussion` result (which is nil once the discussion is soft-deleted) can no longer panic; unit regression covers the nil input |
| GQL-031 | M | gh_graphql.go:95, gh_orgs_graphql.go:103 | Three root fields return bare null where GitHub returns a `NOT_FOUND` error, contradicting the file's own header comment | fixed — user, organization, repositoryOwner and repository entity misses return typed NOT_FOUND errors |
| GQL-032 | M | gh_graphql.go:80 | `viewer` resolves to null for anonymous callers; it is `User!` on GitHub | fixed — viewer is `User!` and the GraphQL endpoint rejects anonymous requests before execution |
| GQL-033 | M | gh_graphql.go:215 | Validation prepass discards its result, both branches return nil, and the document is parsed twice per request | fixed — parsing and validation produce one AST which the guarded execution path reuses |
| GQL-034 | M | whole lane | Zero interfaces declared; no `Query.node`, `nodes` or `rateLimit` — the canonical Relay refetch path fails outright | fixed — the canonical `Node` interface, stable global IDs, `Query.node`/`nodes` refetching, and `Query.rateLimit` now match the official schema. Node resolution covers users, organizations, repositories, issues, and pull requests while preserving private-resource visibility |
| GQL-035 | M | 11 types | Fabricated type names (`PRComment`, `IssueOrder2`, `PullRequestOrderDirection`) break fragments and generated clients | fixed — every fabricated object type is gone and the official-schema ratchet is down from 319 to exactly 9 enumerated principled entries. The check-suite, team-organization, label, user/issue-connection, latest-review, watcher, license, base-ref and ProjectV2 payload-item forks were renamed or merged into shared registry-memoized official types; a real ReactionContent enum replaced string contents and the three reaction forks collapsed into the shared Reaction/ReactionConnection/ReactionGroup shapes; PRComment/PRCommentConnection were deleted in favor of IssueComment(Connection); the RelatedIssue cluster became real Issue.parent/subIssues/repository fields with lazy resolvers; MinimizableComment/LockableSummary became the official Lockable and Minimizable interfaces over Issue/PullRequest/Discussion and IssueComment/DiscussionComment; and the flattened ProjectV2 field model became the official ProjectV2FieldConfiguration union + ProjectV2FieldCommon interface with GitHub-exact iteration configuration and inputs, resolving the ProjectV2Iteration object-vs-input kind collision. The 9 that remain are each deliberate: Mutation.deleteRepository plus its input/payload (bleephub's authz-audited admin deletion path, which GitHub exposes only via REST), Team.name kept nullable (graphql-go's strict SameResponseShape merge rule for gh CLI's User-or-Team fragments, documented at the definition), and the five built-in scalars introspection enumerates but GitHub's SDL never declares. Ratchet floors raised to 911 implemented / 910 exact-signature, gap ceiling lowered 79→9 |
| GQL-036 | M | 18 sites | `PageInfo` forked eighteen ways; two omit `hasPreviousPage`/`startCursor` though the data is computed | fixed — all implemented Relay connections expose the singleton official `PageInfo` type and complete four-field values |
| GQL-037 | M | gh_pulls_graphql.go:477 | `RequestedReviewer` union declares Bot and Team and unconditionally returns User | fixed — the RequestedReviewer union's ResolveType now discriminates on the __typename the producers set (User/Bot/Team) instead of unconditionally returning User; requestedReviewer sources are tagged accordingly. Unit regression covers the mapping |
| GQL-038 | M | 12 payloads | `clientMutationId` absent, so a client that sends and selects it gets a validation error | fixed — the mutation registrar guarantees the official input/payload echo for every registered mutation |
| GQL-039 | M | gh_issues_graphql.go:1191 | `AddCommentPayload.subject` typed as Issue and nulled for PRs | fixed — subject is the Node interface and returns the concrete Issue or PullRequest |
| GQL-040 | M | gh_issues_graphql.go:1877, gh_pulls_graphql.go:2517 | `authorAssociation` hardcoded OWNER | fixed — OWNER/MEMBER/COLLABORATOR/NONE derive from repository ownership, organization membership and collaborator grants |
| GQL-041 | M | gh_issues_graphql.go:1872 | Comment `url` is the empty string where GitHub returns `URI!` | fixed — comments expose a non-empty typed URI path |
| GQL-042 | M | 9 sites | Only `Repository.url` honours the external URL; every other entity returns a relative path | fixed — a shared externalURL(path) helper prefixes BLEEPHUB_EXTERNAL_URL onto every GraphQL `url` field (repository, user, org, release, issue, pull request, discussion, comment), matching what Repository.url already did; `resourcePath` fields stay relative. Unit regression covers the helper and the broad GraphQL suite passes unchanged |
| GQL-043 | M | gh_pulls_graphql.go:2529 | `reviewDecision` derived from all reviews rather than the latest per author, so an approval never clears CHANGES_REQUESTED | fixed — only each author's latest decisive review participates |
| GQL-044 | M | gh_issues_graphql.go:935 | `assignableUsers` returns every account on the instance on a false claim that the membership graph is unmodelled | fixed — results are the repository owner, owning-organization members and repository collaborators only |
| GQL-045 | M | gh_pulls_graphql.go:1034 | `autoMergeRequest`, `mergeCommit`, `resolvedBy` and `isPinned` are unconditional nulls or falses contradicting stored state | fixed — `mergeCommit` resolves the persisted merge SHA + git object, and the review-thread `resolvedBy` is now populated: a `ResolvedByID` was added to the review-comment root and its persistence record, `ResolveThread` captures the resolving actor (cleared on unresolve), and the connection renders it via the same `userToGraphQL` lookup it already uses for authors. Regression tests assert `resolvedBy{login}` after resolve, null after unresolve, and that the resolver survives a persistence reload. `autoMergeRequest` and `isPinned` are NOT defects — they are faithful nulls: there is no per-PR auto-merge state and no issue/PR pin store (GitHub itself returns null when the feature isn't enabled), so a resolver defect does not exist for them. Enabling those two is feature work, tracked separately |
| GQL-046 | m | gh_repos_graphql.go:104 | `hasIssuesEnabled` registered twice; `AddFieldConfig` overwrites silently | fixed — the field has one typed registration |
| GQL-047 | m | gh_repos_graphql.go:1008 | Input decode panics rather than returning an error with a path | fixed — optional boolean inputs are comma-ok decoded and malformed values are rejected by GraphQL coercion |
| GQL-048 | m | gh_pagination.go:163 | Two incompatible conventions for reading integer arguments across 14 sites | fixed — the remaining 13 sites reading GraphQL Int arguments via a bare `.(int)` assertion (across the discussions/issues/pulls/repos connection resolvers and `paginateGQLMaps`) were unified onto the `intArg` helper. A literal Int arrives as `int`, but a variable-supplied one arrives as `float64` (or `int64`) after JSON decoding, so `.(int)` silently dropped it and the connection fell back to its default page or a zero `number`. `intArg` accepts all three numeric shapes. Regression test proves `first` supplied as `float64` is honored |
| GQL-049 | m | gh_projects_v2_graphql.go:402 | Three unlock paths with no `defer` — a panic leaks the store read lock | fixed — all project-owner resolution locks defer their unlock |
| GQL-050 | m | gh_discussions_graphql.go:1071 | `bodyHTML` is escaped plaintext in `<p>` tags; goldmark is already a dependency | fixed — discussion Markdown renders through Goldmark |
| GQL-051 | m | gh_graphql.go:174 | Full query text logged on every errored request | fixed — error logs record only operation, query SHA-256, byte length and formatted errors |
| GQL-052 | B | gh_pulls_graphql.go:1736 | The GraphQL merge path bypassed branch protection entirely, while the REST twin enforced it — the merge gate was reachable around | fixed — calls `canMergePullRequest` like REST |
| GQL-053 | M | gh_pulls_graphql.go:1750, :1946 | `authorEmail` was accepted and never recorded, and `updatePullRequest` declared `milestoneId` and dropped it — two more instances of the GQL-006 class in the same file | fixed — both applied, label and assignee ids resolved strictly |
| GQL-054 | M | gh_issues_graphql.go:1020 | `createIssue` silently dropped unresolvable label, assignee and milestone ids | fixed — strict |
| GQL-055 | M | gh_projects_v2_graphql.go | Four mutations authenticate without authorizing: `createProjectV2`, `addProjectV2ItemById`, `createProjectV2Field`, `updateProjectV2ItemFieldValue` | fixed — the policy table was repository-shaped, so it is now one rule implementation per resource class (`viewerOnly`/`repoRule`/`projectRule`), and the schema has 31 mutations and 31 rows, asserted by equality. Projects reuse the owner-scoped pair the REST surface already enforces rather than a reinvented ladder. Pre-fix, with the rule neutered, all four served a stranger **and changed state** |
| GQL-056 | B | gh_pulls_graphql.go:1729 | Adding `canMergePullRequest` to the GraphQL merge was not sufficient: it returns true for a repository admin whenever `enforce_admins` is off and only evaluates required status checks **after** that bypass, whereas the REST handler runs the required-checks gate unconditionally **before** it. GraphQL could therefore merge a red pull request that REST refuses — for exactly the class of caller most likely to try | fixed — the unconditional gate runs first, mirroring REST; both callers of `completePullRequestMerge` are now gated identically |
| GQL-057 | B | gh_projects_v2_graphql.go:110 | Write access to a project was never read access to what went into it, so any project owner could pull a stranger's private issue in by node id and read its title back through the project | fixed — `viewerCanReadProjectContent` gates the content as a second resource |
| REST-116 | B | gh_projects_v2_rest.go:911 | `serveProjectV2AddItem` is the REST twin of the hole above: it validates that the content resolves but never that the caller may read it | fixed — the same `viewerCanReadProjectContent` the GraphQL twin uses. Verified exploitable pre-fix: both the database-id and the owner/repo/number addressing modes returned 201 and the victim's private issue was indexed against the stranger's project |
| REST-117 | M | gh_apps_perms.go, gh_teams_rest.go, gh_members_rest.go, gh_orgs_people_rest.go | Organization and team handlers re-ran human owner/member/maintainer checks after `requirePerm` had authorized a GitHub App installation token, so the synthetic `<slug>[bot]` user made valid Members, Administration, Webhooks, and organization-Administration grants fail | fixed — `requirePerm` now carries an unforgeable typed grant into downstream organization resolvers; team, membership, invitation, organization-administration and webhook journeys honor installation permissions and target isolation without persisting the bot as a human member. Team-repository writes explicitly require both Members:read and repository Administration:write, preventing the parity fix from admitting under-scoped Apps |
| ACT-098 | m | webhooks.go:1298 | `createStartupFailureRun` published the terminal run into `s.store.Workflows` under `store.mu`, released the lock, then called `queueActionsEvent` — whose `cloneWorkflowEventSnapshot` copies `*wf` while holding only `snapshotMu`. Once the run is store-visible another goroutine can resolve and mutate it under `store.mu`, so reading its fields lock-free is a data race (the lone one of ~24 `queueActionsEvent` sites not holding `store.mu`); low severity only because the run is already completed so a concurrent writer is unlikely | fixed — both `queueActionsEvent` calls moved inside the same `store.mu` critical section as the publish/persist, matching every other call site (lock order store.mu→snapshotMu is unchanged, no deadlock). Verified under `-race` |
| STORE-078 | m | store_enterprises.go:116, gh_api_insights.go:33, store_issues.go:31 | Three more stringly-typed enums whose valid sets lived only in comments: enterprise Dependabot `dependabot_default_level` (public/internal, "" = unset), API-insights `actor_type` (5 values) + `subject_type` (2 values), and milestone `state` (open/closed) | fixed — typed `DependabotDefaultLevel`, `APIInsightsActorType`/`APIInsightsSubjectType`, and `MilestoneState` with constant sets; struct fields carry the types, internal producers use the constants, and conversion happens only at request/filter boundaries (the milestone list `all` filter value stays a plain string). JSON byte-identical; behaviour-preserving |
| REST-124 | B | gh_reactions.go:275 | `resolveReactionParent` re-scoped only the `issue` parent to the repository in the URL; for `issue_comment`, `commit_comment`, `pull_request_review_comment` and `release` it returned the raw path id unchecked. Since the store looks each up by global id alone, a caller with write on any repo could `POST /repos/{owned}/{repo}/issues/comments/{victimCommentID}/reactions` to inject a reaction onto — and via the list endpoint read the reactions and reacting users of — a comment or release in another (private) repository. It also never verified the parent existed | fixed — every parent type now resolves its object and requires the object's repo == the path repo (404 otherwise), mirroring the sibling handlers' `requireRepoOwns`/`RepoID != repo.ID` checks. Regression test reacts/lists via a foreign repo path for all four non-issue types and asserts 404 with no reaction landing on the victim's parent; the existing all-parent-types test now seeds real parents |
| REST-125 | B | gh_projects_classic.go:544, store_projects_classic.go:388 | `handleMoveProjectCard` authorized `projects:write` only on the card's own project/repo, then passed the request-body `column_id` to `MoveProjectCard` after only an existence check — the destination column's project/repo was never authorized. A caller with projects-write on their own repo could `POST /projects/columns/cards/{ownCardID}/moves` with a `column_id` belonging to another repo's private classic project and reassign their card into it (cross-tenant board injection; same "trusted raw body id" shape as the reaction/PR-review IDORs) | fixed — `MoveProjectCard` now requires the target column and the card's source column to share a `ProjectID`, returning an error the handler maps to 422 (GitHub's response for a cross-project move). Regression test moves a card into a foreign project's column (422, card unmoved, victim column uninjected) and confirms same-project moves still 201 |
| STORE-077 | m | store_code_scanning.go:1059, gh_environment_policies.go:31, store_dependabot.go:23 | Three more stringly-typed enums whose valid sets lived only in comments: CodeQL variant-analysis `analysis_status`, deployment-branch-policy `type` ("branch"/"tag"), and Dependabot alert `state` | fixed — typed `CodeScanningAnalysisStatus`, `DeploymentBranchPolicyType`, and `DependabotAlertState` with constant sets; struct fields carry the types, validators/filters convert only at the REST boundary. JSON byte-identical; behaviour-preserving. Completes the alert-state-machine trio with code-scanning + secret-scanning |
| STORE-076 | m | gh_pull_merge_async.go:21, gh_deployments.go:62, gh_statuses_rest.go:25 | Async-merge `status`, deployment `state`, and commit-status `state` were free `string` fields whose value sets lived only in comments; commit-status even had a normalizer (`normalizeStatusState`) that canonicalized input but threw the guarantee away by returning `string` | fixed — typed enums `MergeAsyncStatus`, `DeploymentStatusState`, and `CommitStatusState` with constant sets; `normalizeStatusState` now returns the typed value (parse-don't-validate), struct fields carry the types, and conversion happens only at the request boundary. JSON byte-identical; behaviour-preserving |
| STORE-075 | m | store_code_scanning.go:23, store_secret_scanning.go:42, store_issues.go:55 | Alert lifecycle states and reasons (code-scanning `state`/`dismissed_reason`, secret-scanning `state`/`resolution`, issue/PR `active_lock_reason`) were free `string` fields whose valid sets lived only in comments and scattered literals — a typo or drifted value was representable | fixed — introduced typed enums `CodeScanningState`/`CodeScanningDismissedReason`, `SecretScanningState`/`SecretScanningResolution`, and `LockReason` with the documented constant sets; the struct fields carry the types, validators/serialisers convert only at the REST boundary, and JSON output is byte-identical (typed strings marshal as strings). Behaviour-preserving; scanning + lock/moderation suites green |
| REST-122 | B | gh_pulls_rest.go:826 | `handleUpdatePRReview` wrote the review body by global id (`UpdatePullRequestReview` looks reviews up by id alone) *before* scoping it to the PR in the path and never checked authorship — a caller who owned any repo/PR could `PUT /repos/{owned}/{repo}/pulls/{n}/reviews/{victimReviewID}` and rewrite a review living on another repository, and the late 404 hid that the write had already landed; even naming the correct repo, a non-author could edit another user's review | fixed — resolve the review and require `review.PRID == pr.ID` (404) and `review.AuthorID == user.ID` (403, author-only per GitHub) before mutating; unauthenticated is 401. Regression test proves cross-repo 404 + non-author 403 both leave the body unchanged, and the author edit still succeeds |
| REST-123 | M | gh_pulls_rest.go:443 | `handleMergePullRequest` gates only on `user != nil` (and branch protection, which passes when none is configured); a read-only authenticated user can merge an open PR on a public repo with no protection. GitHub requires write access | fixed — `handleMergePullRequest` now requires push access via `viewerCanPushRepo` before merging, returning `403 "Must have write access to the repository."`; a read-only authenticated user (or a non-collaborator on a public repo with no protection) is refused while owners/collaborators with push still merge cleanly. This is not the "uniform gap" the earlier note feared: GitHub deliberately gates the PR verbs differently — opening a PR uses the fork model (read + head-branch access, no base push), updating is author-or-push, and merge alone requires base-repo push — so gating merge on push is exact parity, not an inconsistency. Regression `TestMergePullRequestRequiresPushAccess` (403 for a reader, PR unmerged; 200 for the owner) |
| GQL-058 | m | gh_projects_v2_graphql.go | The project lane's NOT_FOUND wording differs in capitalisation from the repository lane's `gqlMissingNode` | fixed — projects-v2 NOT_FOUND errors now return the typed ghNotFoundError with gqlMissingNode's 'Could not resolve ... .' wording, so the project and repository lanes match (capitalisation + trailing period) |
| GQL-059 | M | gh_orgs_graphql.go | `User.organizations` sorted only by `createdAt`; equal timestamps retained map order, making cursor pages unstable and omitting newly-created organizations from bounded connections under a deterministic clock | fixed — equal creation times are ordered by descending organization ID, giving a total newest-first order and stable Relay page boundaries |
| GQL-060 | M | gh_issues_graphql.go | The GraphQL `createIssue`/`closeIssue`/`reopenIssue` mutations mutated the store but never emitted the `issues` webhook or recorded the timeline event the REST paths do, so `on: issues` workflows never fired for gh-CLI-driven issue changes | fixed — the resolvers deliver the opened/closed/reopened webhook and record the close/reopen timeline event through a shared helper guarded on the actual state transition; a regression asserts opened+closed deliveries originate from GraphQL |
| GQL-061 | m | gh_issues_graphql.go | `createIssue` declares `projectIds` and `issueTemplate` inputs the resolver never reads, so a non-null `projectIds` is accepted and silently dropped | fixed — a supplied `projectIds` adds the new issue to each named ProjectV2 board (resolved via `LookupProjectByNodeID`, an unknown id erroring like GitHub); `issueTemplate` is a client hint with no server-side state and is accepted for coercion; regression covers a non-null `projectIds` |

## ACT — Actions engine, runners, webhooks

| ID | S | Location | Finding | Status |
|---|---|---|---|---|
| ACT-001 | B | artifacts.go:890 | `Content-Range` end is unbounded — a 1-byte body can allocate a terabyte | fixed — a `Content-Range` end beyond the cache limit is rejected, the body read is bounded by the declared range via `MaxBytesReader`, and chunks buffer as segments rather than into a buffer sized from the header. Verified: the old preallocation thrashes for 11.8s where the fix answers in 0.6s |
| ACT-002 | B | artifacts.go:305,632, timeline.go:38 | Artifact and log endpoints unauthenticated with sequential IDs; omitting the run ID returns every artifact across every repository | fixed — artifact and blob routes take a job runtime token; a missing `workflow_run_backend_id` is 400 and an unknown one 404, never a wildcard. Timeline and log routes are bound per-plan, so one job cannot write another's |
| ACT-003 | B | actions.go:52,180 | Unauthenticated action-tarball endpoint returns a gzip of any private repository at HEAD | fixed — the tarball route takes a job token and resolves the action's repository **before the cache hit is served**, 404ing when it is not hosted and requiring scope coverage for a private one |
| ACT-004 | B | jobs.go:353-369 | Matrix expansion shares one env map across combinations, so every job gets the last combination's values — but only when the job declares `env:`, which the existing test does not | fixed |
| ACT-005 | B | jobs.go:341,374 | The `needs` rewrite depends on map iteration order and fails nondeterministically about half the time | fixed |
| ACT-006 | B | jobs.go:342 | `include:`-only matrices are silently dropped; `ExpandMatrix` handles them and is never reached | fixed |
| ACT-007 | B | webhooks.go:265 | Fork PRs never trigger any workflow; the whole fork-approval machinery is unreachable | fixed — fork pull requests trigger, gated by the approval flow that already existed |
| ACT-008 | B | webhooks.go:260,465 | Workflow files are read from repository HEAD, not the triggering ref | fixed — the definition comes from the triggering ref for a push and from the **base** repository for a fork pull request, so a fork cannot rewrite the workflow to read the base's secrets. Every failure path returns an error rather than falling back. Verified by reversion: pointing the fork case at its own ref fails the test |
| ACT-009 | B | workflow_triggers.go:308 | A package-global regex cache written with no mutex from concurrent trigger evaluation — `fatal error: concurrent map writes` | fixed — `sync.Map`. Verified: the plain map produces 5 data races under `-race` |
| ACT-010 | B | workflows.go:706, broker.go:154 | `LockedUntil` is written three times and never read; the pending message is removed before the response is flushed, so a dropped connection loses the job permanently | fixed — the lease is real and an expired one redelivers; the broker half requeues on a failed write rather than dropping the job |
| ACT-011 | B | webhooks.go:80,183,204 | Webhook delivery is a full SSRF: user-controlled URL, no IP validation, redirects followed, unbounded goroutines, no per-hook ordering | fixed — `webhookAddrBlocked` covers loopback, link-local incl. the metadata endpoint, RFC1918, fc00::/7, CGNAT, multicast, unspecified, broadcast and the IPv4-mapped/NAT64 spellings; checked at configuration time and again on `net.Dialer.Control` against the address actually dialed, so DNS rebinding between the two cannot win; redirects become the recorded outcome rather than being followed; a 16-worker dispatcher with a per-hook FIFO bounds fan-out and preserves ordering. `BLEEPHUB_ALLOW_PRIVATE_OUTBOUND_TARGETS` (renamed; see AUTH-064) is deny-by-default and never relaxes the scheme rule |
| ACT-012 | M | workflows.go:402 | Concurrency admission is a TOCTOU across two lock acquisitions and ignores non-running holders | fixed — cross-replica admission now serializes on the shared database: on a clustered deployment (persistence attached and not exclusively owned) submit-side admission, pending-group promotion and job-group admission each take a TTL'd named lock (`actions/concurrency/…` via Persistence.AcquireLock, the same primitive git object storage already uses in production) and force a staleness refresh inside the lock so the group scan sees peer-admitted holders before deciding; the decision is committed via persistWorkflowRecord before release. Single-writer deployments skip it entirely (OwnedExclusively), zero cost. On lock error or deadline it degrades to process-local admission with a logged warning rather than losing a run. Accepted residual: lock TTL expiry under a stalled replica can double-admit a group for the ~5s TTL window — the same bounded exposure the git-object lock accepts; fencing tokens are deliberately out of scope. Covered by TestConcurrencyAdmissionSerializesAcrossReplicas |
| ACT-013 | M | workflows.go:417,1166 | `cancel-in-progress` does not cancel pending runs, and the queue releases oldest-first so a loaded group replays a stale backlog | fixed — a submission supersedes every older pending run, `cancel-in-progress` cancels active and pending holders, and recovery promotes only the newest pending run |
| ACT-014 | M | workflow.go:137-161 | `permissions:`, `defaults:`, `run-name:` and job-level `concurrency:` are accepted by the parser and discarded; GITHUB_TOKEN scope is fixed regardless | fixed — declarations are strongly parsed and validated; run defaults and expression-backed run names execute; job concurrency is now evaluated, persisted, case-insensitive, enforced across runs, replaces stale pending jobs, and delivers cancellation to an active holder. And least-privilege GITHUB_TOKEN API scopes are now enforced: `resolveJobTokenPermissions` computes the effective permission map (GitHub semantics — a job block replaces a workflow block replaces the repo default; `read-all`/`write-all` and `none` handled; an empty block yields metadata:read only), which is embedded in the per-job token by `makeJobJWT` (new `repo`/`perms` claims). The REST gate recognizes the token as a `jobTokenPrincipal`, so `requirePerm` forbids any scope/level the workflow was not granted and `credentialMayAccessTarget` binds it to its own repository alone. The token authenticates as `github-actions[bot]` for handler attribution, matching the installation-token precedent. Regressions `TestResolveJobTokenPermissions` (semantics) and `TestGithubTokenIsRepoScopedLeastPrivilege` (granted scope passes, ungranted 403, cross-repo denied) |
| ACT-015 | M | workflow.go:103-112 | `steps.*.env` and `steps.*.shell` are parsed with zero read sites; `working-directory`, `continue-on-error` and `timeout-minutes` are not even fields | fixed — all five fields are represented and encoded in the runner's step message |
| ACT-016 | M | jobs.go:368 | Matrix values round-trip through `map[string]string`, so `matrix.version == 3` is false where GitHub says true | fixed — combinations stay typed through `JobDef`, `WorkflowJob`, and PipelineContextData |
| ACT-017 | M | workflows_msg.go:127 | Internal `__matrix_*` keys leak into every matrix job's shell environment | fixed — the environment is copied independently and matrix data has a dedicated typed field |
| ACT-018 | M | matrix.go:20 | `include` applied before `exclude`, reversing GitHub's documented order | fixed |
| ACT-019 | M | workflows.go:315 | Matrix group membership reverse-engineered from the job key's textual shape, so unrelated jobs named `build_1`/`build_2` share fail-fast | fixed — expansion records explicit group identity |
| ACT-020 | M | workflows.go:80,323 | `max-parallel` stored per run rather than per matrix group, last writer wins | fixed — limits are retained and enforced per matrix group |
| ACT-021 | M | workflows.go:834,1037 | `continue-on-error` does not prevent the run from failing — its entire documented purpose | fixed — including the fail-fast interaction |
| ACT-022 | M | workflows.go:1230 | A completion arriving with no result is silently recorded as successful | fixed — a missing runner result is a failure |
| ACT-023 | M | workflow_call.go:374,461 | Broken `with:`, output and secret templates are logged and skipped, so a called workflow runs with empty inputs or unauthenticated | fixed — input/secret template errors fail closed before runner dispatch and an invalid output fails the collector |
| ACT-024 | M | workflow_call.go:398 | `"yes"` coerces to false for a boolean input and `"12abc"` to 12 for a number; `choice` options unvalidated | fixed — strict full-value boolean/number parsing and choice membership validation |
| ACT-025 | M | workflow_call.go:197 | A reusable workflow's own top-level `env:` is dropped | fixed — called-workflow environment is merged into each child with job-level precedence |
| ACT-026 | M | webhooks.go:249 | ~40 `on:` events parse and never fire; only push, pull_request, repository_dispatch, release and schedule are ever dispatched | fixed — webhook emission is the single event-production choke point and every implemented mutation event automatically feeds Actions. Issue-comment create/edit/delete and pull-request-review submit/dismiss now join push, PR, issue, review-comment, release, deployment, status/check and workflow lifecycle producers; families with no real mutation producer remain open, though the set keeps shrinking — label create/edit/delete now produces the `label` event (regression `TestWebhookLabelEvent`), and milestone create/close/reopen/edit/delete now produces the `milestone` event with GitHub's action semantics (regression `TestWebhookMilestoneEvent`); branch/tag creation and deletion now produce the `create` and `delete` events with `ref`/`ref_type` (regression `TestWebhookCreateDeleteRefEvent`); commit-comment creation produces the `commit_comment` event repository forks produce the `fork` event on the source repo (regression `TestWebhookCommitCommentAndForkEvents`, which also surfaced and fixed a pre-existing commit-comment response-shape gap — `path`/`position`/`line` are now emitted as required-nullable), starring a repo produces the `watch` event with action `started` (regression `TestWebhookWatchEvent`), switching a repo from private to public produces the `public` event (regression `TestWebhookPublicEvent`), creating a classic project produces the `project` event (regression `TestWebhookProjectEvent`), establishing/updating/removing a branch-protection rule produces the `branch_protection_rule` event (regression `TestWebhookBranchProtectionRuleEvent`); accepting a repo invitation produces the `member` event, creating a discussion/comment produces the `discussion`/`discussion_comment` events, finishing a Pages build produces the `page_build` event, and classic project column/card create and card move produce the `project_column`/`project_card` events (regressions `TestWebhookMemberEvent`, `TestWebhookDiscussionEvents`, `TestWebhookPageBuildEvent`, `TestWebhookProjectCardColumnEvents`) Every GitHub-Actions `on:` trigger event that has a producer in bleephub now fires: the pre-existing set plus the families wired here (create, delete, milestone, commit_comment, fork, watch, public, project, project_column, project_card, branch_protection_rule, member, discussion, discussion_comment, page_build, registry_package). The only Actions trigger events that still do not fire are the ones with no producer in this surface — `gollum` (no wiki), `merge_group` (no merge queue) — and the non-webhook mechanisms `workflow_dispatch`/`workflow_call`; `pull_request_target` shares the `pull_request` producer. |
| ACT-027 | M | webhooks.go:290 | `pull_request` runs report the head branch ref and SHA instead of the merge ref | fixed — runs now report `refs/pull/<number>/merge` and prefer `merge_commit_sha`; until Bleephub materializes synthetic merge commits, an event without that field falls back to the head SHA And the missing piece is done: an open pull request's test-merge commit is now materialized (`computeMergeCommitHash` merges head into base without advancing the base ref, extracted from `performMergeCommit`) on PR open and on synchronize, stored as `PullRequest.PotentialMergeCommitSHA`, and reported as the event's `merge_commit_sha` — so a `pull_request` run runs against the merge ref/SHA, not the head. A conflict or missing git storage clears the field, keeping the head-SHA fallback; `MergeCommitSHA` stays merged-only so GraphQL `mergeCommit` is unaffected. Regression `TestPullRequestRunReportsMergeSHA`. |
| ACT-028 | M | workflows_msg.go:601 | Step conditions omit GitHub's implicit `success() &&` guard, so steps run after an earlier failure | fixed — conditions without an explicit status function receive the implicit guard |
| ACT-029 | M | expressions.go:93 | Status-function detection is a lowercased substring scan — `contains(msg,'always()')` is misread as a gate | fixed — lexer tokens identify actual function calls without inspecting string contents |
| ACT-030 | M | workflows.go:643,1276 | The timeout clock starts at queue time, and a timed-out job concludes `cancelled` where GitHub says `failure` | fixed — queue and execution timestamps are separate, only running jobs time out, and the conclusion is failure |
| ACT-031 | M | broker.go:318, actions_events.go:62 | Cancellations and lifecycle webhooks are dropped when a buffer is full — a cancelled job keeps running while the API reports otherwise | fixed — lifecycle admission is lossless and non-blocking under the store lock; the unbounded condition queue cannot deadlock on its consumer or silently discard a transition |
| ACT-032 | M | actions_events.go:27,100 | Event payloads rendered at drain time from live pointers: a `queued` event can carry `completed` state, and the reads race the engine | fixed — every transition deep-snapshots workflow inputs/event payloads and job outputs/matrix state at admission; webhook rendering uses that immutable snapshot while check mutations retain the live identity |
| ACT-033 | M | webhooks_payloads.go:120 | `forced: false` asserted though it was computed one line earlier; `commits`, `head_commit`, `compare` and `pusher` empty, so `[skip ci]` silently misbehaves | fixed — object-graph force detection, bounded commit vectors, head commit, compare and pusher payloads; all documented skip directives suppress Actions |
| ACT-034 | M | workflows.go:264 | `run_number` is the global run ID, so the first run of a new workflow reports a five-digit number | fixed — each workflow file uses an atomically allocated durable sequence and reruns retain the original number |
| ACT-035 | M | broker.go:148,289 | A session with no agent object skips the busy check, satisfies hosted-runner labels, drains the queue and produces uncancellable jobs | fixed — session creation and polling require an authenticated registered agent, and the broker independently refuses nil/zero agents |
| ACT-036 | M | expressions.go:427 | Object and array filters (`.*`, `[*]`) are standard GitHub syntax and fail the job | fixed — both filter spellings project arrays and objects through chained properties, with declaration order for arrays and deterministic key order for objects |
| ACT-037 | m | webhooks_store.go:26 | `insecure_ssl` accepted, persisted, echoed and never applied | fixed — the dispatcher selects a shared TLS transport with verification disabled only for hooks explicitly configured with `insecure_ssl=1`; address and redirect controls remain enforced |
| ACT-038 | m | webhooks.go:79 | Hook fields read off a shared pointer outside the lock; the same race was recognised and fixed for one field only | fixed — delivery queues receive a deep hook snapshot, including events and last response, captured under the store lock |
| ACT-039 | m | actions.go:65 | A malformed body produces a successful empty response, so the runner fails later with an opaque error | fixed — malformed JSON and incomplete action entries return 400 at the download-info boundary |
| ACT-040 | m | actions.go:20,147 | Action tarball cache keyed by mutable ref, never invalidated, unbounded, holding full tarballs in memory | fixed — cache identity is the resolved immutable commit SHA, returned bytes are detached, and insertion is bounded to 128 archives |
| ACT-041 | m | timeline.go:307 | Timeline attachments read and discarded — `$GITHUB_STEP_SUMMARY` content never appears anywhere | fixed — bounded summary attachments persist with the job and render as safe Markdown in run detail |
| ACT-042 | m | timeline.go:202, artifacts.go:548, secret_scanning_ingest.go:57 | Unbounded `io.ReadAll` on log, artifact and scanning paths; the 4 MiB log cap is applied after the full read | fixed — the remaining unbounded reads (secret-scanning blob scans in secret_scanning_ingest.go) now read through io.LimitReader capped at 5 MiB; the log/artifact/step-summary paths were already bounded by MaxBytesReader/LimitReader |
| ACT-043 | m | workflows.go:1248 | `cancelTimeout` written without the store lock; watcher goroutines leak for runs that never resolve | fixed — a dedicated mutex serializes watcher replacement/stop and replacing a watcher cancels the previous goroutine |
| ACT-044 | m | workflows.go:746, broker.go:169, artifacts.go:1028 | Linear scans over every job and workflow on hot paths, never garbage collected — completed job messages holding plaintext secrets are retained indefinitely | fixed — the hot paths are indexed and the secret payloads are freed. Unexported store indexes (jobsByPlanID/jobsByRequestID, planScopes, workflowsByRunID, per-concurrency-group maps) make runner long-poll job matching, plan/request lookups, artifact scope resolution and group admission O(1)-per-key instead of O(all jobs and workflows ever); job-token auth (repoForJobScope, requirePlanJob) reads a small planScopes entry recorded at dispatch and no longer JSON-parses the secret-bearing message on every request. GC: at run finalization every job's Message (GITHUB_TOKEN plus all secret values) is cleared; a 10-minute janitor retires job stubs, plan scopes, LogMasks and in-memory log claims once CompletedAt + the 6h runner-token TTL passes (late teardown calls still authenticate via planScopes); run and repo deletion tear down eagerly. The used-ephemeral-runner disqualification that previously depended on completed jobs staying in memory now rides an EverAssigned flag, proven by TestUsedEphemeralAgentStaysDisqualifiedAfterJobGC; TestLateFinishJobAfterMessageGCAuthenticates and TestActionsJanitorSweepsRetiredJobState cover the GC contract |
| ACT-045 | m | workflows_msg.go:173 | Secret values inserted into a regex mask unescaped, so a secret with metacharacters is rejected and left unmasked | fixed — every literal token and secret is `regexp.QuoteMeta` encoded before entering the regex mask list |
| ACT-046 | m | matrix.go:135 | Matrix display names use sorted keys rather than declaration order, breaking required-status-check contexts | fixed — YAML mapping order is retained and drives Cartesian expansion and display names |
| ACT-047 | m | matrix.go:110 | Include/exclude matching compares values via `fmt.Sprintf`, so `1` and `"1"` are equal | fixed — normalized values are compared with type-preserving equality |
| ACT-048 | m | actions_events.go:151,389 | Startup-failure runs emit no check suite and an empty conclusion; the merge gate's `startup_failure` branch is dead code | fixed — jobless startup failures enqueue requested/completed lifecycle events, persist their suite id, and conclude the suite `startup_failure` |
| ACT-049 | m | workflow_schedule.go:35 | The dispatcher re-reads and re-parses every workflow file of every repository every minute; parse errors swallowed | fixed — parse errors are now structured warnings with repository, file, cron, and timezone context; and the dispatcher no longer re-reads and re-parses every default-branch workflow every minute: a `scheduleIndex` cache holds each repo's parsed, cron-validated schedule keyed by the default-branch tip commit (`resolveRefSha`), rebuilt only when that tip moves (`buildScheduleIndex`), so a steady-state tick reduces to cron-matching against pre-parsed entries. The cache is pruned to live repos each tick (`retain`). Regression `TestScheduleIndexRebuildsOnlyOnTipChange` asserts one rebuild across two unchanged-tip ticks and a second rebuild after the tip advances |
| ACT-050 | m | dependabot_ingest.go:148 | Any non-numeric version segment is silently treated as not vulnerable | fixed — package-style prerelease/build identifiers participate in comparison and an unknown dialect fails safe by retaining the advisory rather than suppressing it |
| ACT-051 | m | workflows_msg.go:280 | `runner.os`, `arch` and `name` are constants; the github context omits `workflow_ref` and `job_workflow_sha`, which are load-bearing for OIDC claims | fixed — workflow/job workflow ref and SHA fields now reach the runner context, including reusable-workflow identity; and runner OS/architecture/name are now late-bound to the agent that actually leases the queued message: the job message is built runner-agnostically (`runnerContextData(nil)`) and the broker rewrites `contextData.runner` (os/arch/name plus OS-appropriate tool_cache/temp) to the leasing agent at delivery in `pullPendingMessage` (`rebindRunnerContext`, idempotent so a requeue rebinds afresh). OS/arch derive from the agent's self-reported labels, falling back to its OS description. Regressions `TestRunnerContextDerivedFromAgent` and `TestLeasedJobRebindsRunnerContext` |
| ACT-052 | m | artifacts.go:966 | The cache download token — documented as the sole access control — is compared with `!=`; no eviction, and `LastAccessedAt` is faked | fixed — constant-time token comparison and persisted restore-time `LastAccessedAt` landed earlier; finalizing a cache now enforces a per-repository budget (`maxRepoCacheBytes`, GitHub's 10 GiB default; a per-instance field so tests drive it small) via `evictRepoCacheOverLimit`, which deletes least-recently-used finalized entries (oldest `LastAccessedAt`, id breaking ties) until the repo is back under budget, byte-deletes outside the lock. LRU-order regression test |
| ACT-091 | B | actions.go | `ActionDownloadInfo` returned `"token": "x-access-token"` — the literal *username* half of the Basic credential, mistaken for the token — beside a hardcoded `"expiresAt": "2099-01-01T00:00:00Z"`, the same far-future placeholder another test in this package explicitly bans. The runner dutifully sent `Basic base64("x-access-token:x-access-token")`. The tarball route also read only Bearer, so a real token in the shape the runner sends would have been refused too | fixed — the download info mints a job token for the plan the caller already authenticated as, with its real expiry, and the route accepts the Basic download credential as well as a Bearer job token. ACT-003's private-repository scoping is unchanged and now covered through the Basic form |
| ACT-092 | m | auth.go | `authenticateRunner` reports `missing runner bearer token` identically for an absent Authorization header and for one carrying a scheme it does not read, which is what made the tarball failure look like an anonymous request for two CI cycles | fixed — absent, malformed and unsupported-scheme credentials are distinct failures with regression coverage |
| ACT-093 | B | auth.go, Dockerfile.runner | Actions Runner 2.336 signs its client assertion with PS256 while the server accepted only RS256, so the current official runner could register but not establish a session. Its bundled .NET 8.0.28 runtime also carries five fixed HIGH vulnerabilities, and replacing that runtime beneath the precompiled assemblies is binary-incompatible | fixed — the assertion verifier accepts and independently verifies both PS256/PSS and legacy RS256/PKCS#1 signatures. Both runner images rebuild the immutable 2.336.0 source commit against the digest-pinned .NET 8.0.423 SDK and patched 8.0.29 runtime, with a fully content-hashed NuGet graph, then retain only the checksum-verified official scripts and Node runtimes around that rebuilt binary tree. The real runner harness pins registration, sessions, jobs, matrices, outputs, artifacts, cancellation, service containers and dispatcher-spawned ephemeral execution |
| ACT-094 | M | workflow_schedule.go | The schedule dispatcher ran the per-minute scan inline on the ticker goroutine and recomputed the next boundary afterwards, so a scan overrunning a minute silently skipped every intervening minute's schedules | fixed — the dispatcher threads a minute cursor and `fireSchedulesThrough` replays every whole minute in `(lastFired, now]`, bounded by a ten-minute catch-up and deduped by the per-minute firing claim; a regression drives a nine-minute overrun and asserts the skipped due minutes fire exactly once |
| ACT-095 | M | workflow_schedule.go | A timezone'd schedule deduped its firing on the UTC minute, so a DST fall-back — the same wall-clock minute at two UTC instants — fired the workflow twice | fixed — the claim is keyed on the reconstructed civil minute in the schedule's zone, collapsing both instants to one claim; a regression fires the two 01:30-local instants and asserts a single run |
| ACT-096 | M | workflow_schedule.go, persistence.go | The firing claim was written before the run was created and never released, so a transient submit failure consumed the occurrence with no run and no retry | fixed — `fireScheduledWorkflow` distinguishes transient failures and the caller releases the claim via `ReleaseScheduleFiring` so another replica or attempt can retry, while permanent conditions keep the claim; a regression re-claims a released firing |
| ACT-097 | m | workflow_schedule.go | `fireScheduledWorkflow` re-fetched git storage with no nil guard and passed it to `resolveRefSha`, which dereferences a nil storer if a concurrent refresh dropped the entry | fixed — a nil storer is logged and skipped before use |

## STORE — persistence, storage, dqlite, S3

| ID | S | Location | Finding | Status |
|---|---|---|---|---|
| STORE-001 | B | persistence.go:71 | `PutBatch` is used at 4 call sites against 398 `MustPut` and 206 `MustDelete`; `DeleteRepo` alone issues 54 independent transactions | fixed — a batch executor commits a whole write set in one transaction. `DeleteRepo` stages its whole cascade in that transaction; the repository re-key on `RenameRepo`/`TransferRepo` now stages the repos row and every one of its ~40 subresource buckets (secrets, collaborators, teams, hooks, checks, workflows, commit statuses, notification read state, scanning alerts, packages, env secrets/variables, pages, …) into one `persistBatch` via a batch-aware `moveRepoKeyLocked`, so a crash can no longer split a repository across its old and new names; and `StarRepo`/`UnstarRepo`, `CreateOrg`, `RemoveMembership`, `DeleteLabel`/`DeleteMilestone`, `CreateOrgInvitation`, `AddRepoCollaborator`/`RemoveRepoCollaborator`, `CreateRepoDeployKey`/`DeleteRepoDeployKey`, `DeleteTeam` (with child re-parenting), `RemoveOutsideCollaborator`, `CancelOrgInvitation`, `DeleteEnterpriseCodeSecurityConfig` and `CreateSecretScanningPushProtectionBypass` each commit their whole multi-bucket mutation atomically. Org-invitation acceptance (`consumeOrgInvitationLocked` — every invited-team join plus the invitation removal) and expiry (`reconcileOrgInvitationsLocked` — the abandoned pending-membership drop plus the invitation-failed write) each now commit in one transaction, and `RenameBranch` re-keys the branch protection and writes the repo's default-branch change together. The classic and v2 project deletions commit their whole subtree in one transaction — `DeleteProject` (v2) with its fields/items/views, and `DeleteProjectClassic`/`DeleteProjectColumn` with their columns and cards via batch-aware `deleteProjectCard/ColumnBatchLocked` helpers — so a crash can no longer orphan a child pointing at a deleted parent. The pull-request reviewer and label mutations that emit one issue-event per element (`RequestReviewers`, `RemoveRequestedReviewers`, `AddPullRequestLabels`, `SetPullRequestLabels`, `ClearPullRequestLabels`, `RemovePullRequestLabel`) and the matching issue label and assignee mutations (`SetIssueLabels`, `ClearIssueLabels`, `AddIssueLabels`, `RemoveIssueLabel`, `AddIssueAssignees`, `RemoveIssueAssignees`) now commit every event with the pull-request/issue row in a single `persistBatch` via batch-aware `recordPullRequestEvent/LabelEventBatchLocked` and `recordIssueEventWithIDsBatchLocked`, so the timeline can no longer diverge from the reviewer/label/assignee state across a crash. A further sweep converts the credential and multi-bucket cascade mutations: user-to-server/refresh token create, revoke, rotate and grant-revoke; `RevokeCredentials` across all four token buckets; `DeleteOAuthApp` with all its issued tokens; classroom and classroom-assignment deletion with their assignments/acceptances; and the org-scoped delete cascades (`DeleteCodeSecurityConfiguration`, `DeleteCustomProperty`, `DeleteNetworkConfiguration`, `DeleteIssueField`, `handleDeleteOrganizationRole`) plus repo-invitation acceptance — each now a single `persistBatch`. Also converted: `CreateCheckRun` (inline suite + run), `PruneEnvironmentPolicies` (branch policies + protection rules), `persistTeamExternalGroupsLocked` (org groups + team binding), `replaceOrganizationSCIMUser` (backing account + SCIM record, unlocking before any panic), `runPagesBuild`'s completion phase (the finished build record and the site's new status commit together so a crash can't mark the build built while the site row still says building), the standalone `DeleteDeployment` (which previously dropped the deployment row and each of its statuses as independent transactions — the shared `deleteDeploymentBatchLocked` now runs under a committed batch on that path too, matching the already-batched cascade caller), and `RecordAPIRequest` (the new api-insights record and its FIFO evictions now commit together so a crash can't leave the durable ring over its cap or missing the just-served request). Release deletion is atomic on both paths: the single-release `Delete` and the repo-scoped `DeleteAllForRepo` each stage the release row and every one of its release-asset rows into one `persistBatch` via batch-aware `deleteReleaseBatchLocked`/`deleteAssetBatchLocked`, so a crash can no longer delete a release while orphaning its assets or drop assets while the release row survives. The GHES org rename `renameGHESOrganization` is inherently multi-phase (the per-repo `TransferRepo` cascade runs unlocked between two locked phases, so it cannot be one transaction), but its final re-key phase — every membership key move, the installation target-login updates, the org row and enterprise settings — now commits as a single `persistBatch` (unlocking before any panic) instead of ~2N independent membership transactions, so a crash can no longer split the org across its old and new login. The final sweep converted every remaining kv-only multi-bucket mutation: the DeleteRepo cascade's per-comment reaction deletes now stage into the cascade batch (closing a hole in the flagship atomic delete); SARIF uploads commit analysis + every alert + the upload row in one transaction; package-version create/delete/restore batch their file/version/package rows; createRepoLocked accepts a caller batch and stages its five default discussion categories with the repo row, which also makes CreateTemporaryFork (advisory + fork repo + categories) and PublishCodespace (repo + codespace, closing the permanently-unpublishable crash window) single-transaction; issue create/lock/unlock/comment and comment deletion commit with their timeline events and reaction cleanup; Copilot seat grant/cancel batch the expiry prune with the seat writes; discussion answer re-marking is atomic; RecordIssueEvent builds fully and puts once; SeedDefaultUser commits user + token together; and the commit-comment, PR-review-comment and release deletion stores stage their reaction deletes with the row deletes. What remains non-batch is so by design, not omission: the S3 purge/rename phases (tombstone-and-resume with compensation), CreateCodespace's unlocked container start (reconciler-covered), object-store byte interleavings with explicit rollback, per-invitation reconciliation, and SetMembership (self-healing via the invitation reconciler) |
| STORE-002 | B | persistence.go:400 | Mutators change memory before persisting and `log.Fatalf` on failure — `os.Exit(1)` from a handler with the lock held and the mutation half-applied | fixed — `MustPut`/`MustDelete` panic with a typed failure rather than exiting, and recovery now reloads a revision-stable durable snapshot so an uncommitted memory mutation cannot survive the 500. If rollback cannot reach persistence, the store is poisoned and every later request retries recovery or fails 503 rather than serving half-applied memory. Several such multi-step mutations are now single `persistBatch` commits (repo re-key on rename/transfer, star/unstar, org creation and member removal, label/milestone deletion, org invitation create/cancel, repo collaborator add/remove, deploy key create/delete, team deletion with child re-parenting, outside-collaborator removal, enterprise code-security config deletion, secret-scanning bypass creation, org-invitation acceptance and expiry, branch rename's protection re-key, classic/v2 project deletion with their full column/card/field/item/view subtree, and pull-request reviewer/label mutations that batch their per-element issue-events with the PR row); every remaining kv-only independent-transaction mutation has now been converted (see STORE-001 for the final sweep's enumeration), so the recovery contract holds everywhere: a mutation either commits whole or the revision-stable reload reflects none of it, and what stays multi-phase does so with explicit tombstone/compensation designs rather than bare sequential writes |
| STORE-003 | B | s3fs.go:584 | `s3File.Lock()` returns nil; go-git relies on it for ref compare-and-set and `packed-refs` rewrites | fixed — a real per-object-key exclusion in process (reference-counted, a bounded wait that errors rather than deadlocking) plus a durable TTL lock table when shared state exists. With persistence disabled there is no shared durable state and so no second replica, which makes the in-process lock the whole lock — stated as the rule, not a fallback. If the durable store is unreachable `Lock` errors rather than proceeding |
| STORE-004 | B | s3fs.go:356,146 | `Chroot` shares one active-file map across every repository keyed by chroot-relative paths — repo A's read returns repo B's bytes | fixed — the staging map is keyed by the bucket-absolute object key rather than the chroot-relative path, so identical relative names in different repositories no longer collide. Pre-fix the test reads repository B's bytes through repository A |
| STORE-005 | B | store_repos.go:336 | Rename re-registers the same path-bound storer under the new key, so all later git I/O targets the vanished prefix | fixed — a path-bound storer is reopened against the new prefix after the bytes move; an in-memory storer holds its objects and is only re-keyed, because reopening it would silently empty the repository. The reopen precedes any map mutation so a failure aborts cleanly |
| STORE-006 | B | store_repos.go:310 | The rename aborts only on `EEXIST`, which POSIX `rename(2)` never returns; every real failure is ignored | fixed — rename and ownership transfer share one validated move path; any `os.Rename` failure aborts, the destination parent is created first, and path-backed storers are rebound so later refs address the moved bytes |
| STORE-007 | B | store_orgs.go:227, store.go:1147 | A partial cascade writes a permanent poison pill: the server refuses to boot forever after the next restart, with no repair path | fixed — the cascade now reaches the organization's repositories, which is where the orphan rows came from, under a durable deletion intent committed in one transaction. On boot an org-owned repo with no organization row is tolerated **only** when a matching intent exists, and is then finished; without one it still fails loudly, because then it really is corruption |
| STORE-008 | B | store.go:2752 | ~70 ID counters rebuilt as `max(surviving)+1`; for attestations and package files that ID is the S3 object key, so a new entity inherits deleted bytes | fixed for 52 of ~70 counters — every `Put` records a durable per-bucket high-water mark **before** the row, so an interruption can only skip identifiers, never reuse one; load takes `max(recomputed, durable)`. Covers the object-key counters the finding names. Not covered: buckets whose key is not the entity identifier — those need a different key scheme, not a different counter. Pre-existing databases keep the old behaviour until the first write per bucket |
| STORE-009 | B | store.go:930 | The in-memory graph is the source of truth with no read-through, invalidation or leader election, while the code assumes multiple replicas | fixed — every metadata transaction advances a monotonic shared revision; before serving a request a dqlite replica detects peer revisions and reloads until the revision is stable across the complete durable-state inventory. Store reconciliation preserves explicit process-local runner/session/log state and locally owned active workflows; the separate Actions artifact/cache index now follows the same revision and preserves in-flight uploads. Refresh failures return 503. Independent database connections prove organizations, Classrooms and Actions metadata propagate while local runtime state survives |
| STORE-010 | B | store.go:1554 | Boot performs a write pass that force-cancels every non-completed run — against a shared database one replica starting cancels another's live runs | fixed — the cancel-and-rewrite pass runs only when the persistence layer is exclusively owned (a local SQLite file). Against a dqlite quorum the runs belong to peers and are left untouched. When it does run it is one batched transaction rather than one write per workflow |
| STORE-011 | B | dqlite-node/main.go:108 | No auth, no TLS, hijack error discarded, and an unbuffered send from a handler started before the node exists | fixed — the upgrade is authenticated with a constant-time shared-secret comparison; that independently generated cluster secret also derives a deterministic Ed25519 TLS identity, so transport is TLS 1.3 and a client rejects another cluster. Rotation replaces both credentials. Hijack errors are logged, delivery is shutdown/timeout bounded, signal context precedes the server, shutdown is graceful, the port is configurable, and release CI compiles the real dqlite build |
| STORE-012 | B | persistence.go:39 | No schema version, no migration runner; rows are raw marshalled structs so a rename decodes to zero and a downgrade drops fields | fixed — a `schema_meta` table, an ordered migration list, and a hard refusal to start on a version this build does not understand |
| STORE-013 | B | store_codespaces.go:182, timeline.go:210, store_repos.go:249 | The global store lock is held across `docker pull` (180s), S3 prefix operations (120s) and every Raft round-trip | fixed — Codespace creation, workspace export, runtime deletion, repository storage creation, fork copying and repository deletion perform external work outside the global lock behind identity/name reservations, with blocking-backend regressions proving unrelated reads continue. Rename/transfer commit their whole re-key in a single transaction instead of ~40 independent persistence rounds while the lock is held, and their filesystem move is a constant-time `os.Rename`. The last slow case — the S3 object-prefix rename — now runs outside the lock too: `renameRepoS3` reserves the target name and records a crash-recoverable `pending_renames` intent under the lock, copies the object graph outside the lock (both prefixes coexist so old-name readers keep working), swaps the metadata under the lock, then purges the old prefix; `finishInterruptedRenames` finishes either side of a crash at startup. A blocking-backend regression proves an unrelated read proceeds while the copy blocks, and a reload regression proves the recovery path. No global-lock round-trip now spans a `docker pull`, an S3 prefix operation or a Raft round-trip |
| STORE-014 | B | store_repos.go:368 | `DeleteRepo` destroys bytes before metadata with an early return at each of six steps and no compensation | fixed — the durable intent now contains every external cleanup target, including Codespaces, package/CodeQL/attestation/Pages/release bytes, Actions artifacts/caches/logs and git storage. Repository, workflow/timeline and all cascade sub-store metadata commit in one transaction, then external data is removed outside the lock. Any partial cleanup is idempotently replayed before the intent clears |
| STORE-015 | B | store_codespaces.go:270,686 | A workspace guard makes codespaces permanently undeletable, and `DeleteRepo` propagates the error — the repository can never be deleted | fixed — a scratch export this server created is removed, while the repository's own git directory that the codespace merely borrows is left alone; deleting it would have destroyed the repository. Only a mount that is neither still errors |
| STORE-016 | M | all store files | No optimistic concurrency anywhere; the pattern exists on one type and was not generalized | fixed — Git ref-derived mutations now have storage-level optimistic concurrency across REST and Git transports, with stale update/delete conflicts and exclusive creation. And generic metadata resources now get optimistic concurrency on writes too: every GET already returned a strong content-derived ETag (writeJSON `"sha256(body)"` + If-None-Match/304), and a reusable `checkIfMatch` helper now enforces `If-Match` on mutations — the current resource's ETag (the same representation a GET returns) must match or the write is rejected 412, so a stale editor cannot clobber a concurrent update; an absent If-Match stays unconditional as on GitHub. Applied to the primary generic resources — issues, issue comments, pull requests, repositories, labels and milestones — and any other update handler adopts it in one line. Regression `TestIssueUpdateOptimisticConcurrency` (matching ETag succeeds, stale/post-update ETag 412, wildcard and absent unconditional) |
| STORE-017 | M | gh_repos_git.go:688 | Zero `CheckAndSetReference` calls against 20+ `SetReference` sites — concurrent pushes silently lose one another | fixed — content writes, ref updates, merge, merge-commit, squash and rebase all compare against the exact ref they read; create requires atomic absence and delete compares the old value. Every repository storer serializes ref operations in-process and through the shared dqlite lease manager, covering HTTP/SSH pushes as well as REST. An AST ratchet forbids unconditional writes in derived-ref functions and deterministic races prove a competing ref is never overwritten or deleted |
| STORE-018 | M | s3fs.go:211,369 | Rename is copy-then-delete with no rollback; a failed delete reports failure after the copy succeeded | fixed — failure to delete the source now compensates by deleting the copied destination, and reports a combined error if compensation itself fails |
| STORE-019 | M | object_bytes.go:18 | The byte-store interface is `[]byte`-only in both directions, and no upload route has a size limit | fixed — every request-body read is now bounded: the shared JSON decode path (`decodeJSONBody`/`decodeJSONBodyOptional` and the label decoder) caps at 25 MiB, and the binary-upload routes (release assets, container blobs/manifests, CodeQL databases, SARIF, markdown, branch protection, enterprise/user endpoints) wrap `r.Body` in `http.MaxBytesReader`, so no route buffers an unbounded body and an oversized one gets 413; and the byte-store interface is no longer `[]byte`-only: `actionsByteStore` now offers `PutStream(io.Reader)` and `GetStream() io.ReadCloser` alongside the whole-value forms. The S3 backend implements them — `PutStream` stages the reader to a temp file (never the heap) while hashing it, then uploads with the SHA-256 in metadata; `GetStream` returns the object body as a stream. The object-store download path streams end-to-end (`servePackageFile` now `GetStream`+`io.Copy`, Content-Length from the recorded size, so a large package download no longer allocates its whole size in RAM). Uploads whose object key or checksum derives from the full body (container blob digests, CodeQL DB keys) necessarily still read it, but bounded by MaxBytesReader. Round-trip regression `TestByteStoreStreamingRoundTrip` |
| STORE-020 | M | object_bytes.go:56, persistence.go:255 | No checksum on any S3 put or get; `synchronous(NORMAL)` means acknowledged writes can be lost with no stated contract | fixed — new S3 objects carry a SHA-256 metadata checksum and reads verify it before returning bytes (legacy objects stay readable for migration); and the single-node SQLite store now runs journal_mode(WAL) with synchronous(FULL), fsyncing the WAL at every commit so an acknowledged write survives an OS crash or power loss, not only a process crash, with the contract documented at the connection (dqlite deployments get durability from Raft replication). Regression asserts the pragmas |
| STORE-021 | M | store_copilot_spaces.go:93 + 9 more | Getters return live pointers that writers mutate in place; the `Stargazers` map case is a hard panic, and blind writes resurrect deleted rows | fixed — Gist and Codespace reads now return deep snapshots, their mutations persist a clone before publishing it, and repository updates clone nested maps/slices before persistence. The issue-search handler no longer reads a matched issue's/PR's mutable `title`/`body`/`updated_at` off the live pointer after releasing the store lock (the scorer and sorter now use fields snapshotted under the lock at gather time; the JSON builders already re-lock), closing a data race with a concurrent `UpdateIssue` that `TestStressReaderWriterLockGraph` intermittently caught. The secret-scanning and security-advisory families now do the same: `GetSecretScanningAlert`, `GetSecurityAdvisoryByGHSA` and `ListSecurityAdvisories` return detached clones (`cloneSecretScanningAlert`/`cloneSecurityAdvisory` copy the slice and time-pointer fields), so a reader can no longer race a concurrent update or mutate the stored row; the writers were re-pointed at the live row (`UpdateSecretScanningAlert` now re-fetches by `RepoKey`/`Number` and hands back a fresh snapshot, and the advisory PATCH handler re-reads after the keyed `UpdateSecurityAdvisory`), with new detachment tests pinning both. `GetOrgInvitation` (cloning `TeamIDs`) and `GetOrgInteractionLimit` (all-value shallow copy) in the teams/people family are likewise detached, again pinned by a test. The code-scanning family follows the secret-scanning shape exactly: `GetCodeScanningAlert` returns a `cloneCodeScanningAlert` snapshot and `UpdateCodeScanningAlert` re-fetches the live row by `RepoKey`/`Number` and hands back a fresh snapshot, pinned by a test. The dependabot alert family is the same again: `GetDependabotAlert` returns a `cloneDependabotAlert` snapshot and `UpdateDependabotAlert` re-fetches the live row by key, covering both its update handlers (the alerts endpoint and the security-review dismissal). So all three code-security alert families plus advisories, invitations and interaction limits are now read-detached; `GetAttestation` (cloning its `Bundle` and `SubjectDigests`) is too, as are `GetPackageVersion` (cloning its `Metadata` map), `GetPackageFile` and `GetDiscussionCategory` (all-value shallow copies) — all with read-only callers and no mutate-through writer. The issues family's `GetLabel`/`GetLabelByName` and `GetIssueEvent` (all-value), `GetMilestone`/`GetMilestoneByNumber` (a `cloneMilestone` copying `DueOn`/`ClosedAt`), and `GetDiscussionComment` (copying `LastEditedAt`) are detached too; their writers are already keyed (e.g. `UpdateLabel(id, fn)`). `GetDiscussion`/`GetDiscussionByNumber` (a `cloneDiscussion` copying `LastEditedAt`/`PublishedAt`) join them, their eight callers all render-only. The whole classic-projects family is done: `GetProjectClassic`/`GetProjectColumn`/`GetProjectCard` return copies, and every mutate-through writer (`UpdateProjectClassic`/`UpdateProjectColumn`/`MoveProjectColumn`/`UpdateProjectCard`/`MoveProjectCard`/`ConvertProjectCardToIssue`) re-fetches the live row by id — reassigning the parameter for the stateful moves and returning a fresh snapshot for the value-returning ones. `GetMembership` (all-value shallow copy; its 24 callers only read `State`/`Role` and writes go through the keyed `SetMembership`) is detached too, as are `GetTeam`/`GetTeamByID` (a `cloneTeam` deep-copying `MemberIDs`/`MaintainerIDs`/`RepoNames`/`RepoPermissions`) — verified that all 31 team-getter callers only read identifier fields while every team write goes through the keyed `UpdateTeam` or the live `st.Teams` row. `GetOrg`/`GetOrgByID` (a `cloneOrg` copying the `MembersCanCreateRepositories` bool pointer) are detached too — all 144 org-getter callers read only, and org writes go through the keyed `UpdateOrg` or the live `st.Orgs` row; a test that had reached through `GetMembership` to set `m.Public = true` was fixed to call `SetMembershipPublic`. `GetPullRequestReview` (a `cloneReview` copying `SubmittedAt`/`DismissedAt`) is detached too — its nine callers read only and review writes go through the keyed Update/Submit/Dismiss methods. `GetIssue`/`GetIssueByNumber` (a `cloneIssue` deep-copying `AssigneeIDs`/`LabelIDs` + `ClosedAt`) are detached too — all 34 issue-getter callers read only, and issue writes go through the keyed `UpdateIssue(id, fn)`; the unexported `issueByRepo*` under-lock helpers keep returning the live row for internal mutation. `GetPullRequest`/`GetPullRequestByNumber` (a `clonePullRequest` deep-copying the four ID slices + `ClosedAt`/`MergedAt`) are detached too — all 21 callers read only, writes go through the keyed `UpdatePullRequest(id, fn)`, and the unexported `findPRByRepoNumberLocked` under-lock helper keeps returning the live row. The architecture ratchet prevents backsliding. The enterprise families are done too: `GetEnterpriseTeam` returns a `cloneEnterpriseTeam` snapshot (deep-copying `GroupID`/`MemberIDs`/`SelectedOrgLogins`) and all eight `*EnterpriseTeam` writers re-fetch the live team by id — `UpdateEnterpriseTeam` mutates the live row and syncs a fresh snapshot back into the caller's pointer for rendering, the member/org mutators reassign to the live row, and the read-only `Is`/`List` methods read live state; `GetEnterpriseCodeSecurityConfig` returns a `cloneEnterpriseCodeSecurityConfig` snapshot and its mutate-through writers (`TouchEnterpriseCodeSecurityConfig`, `SetEnterpriseCodeSecurityConfigDefault`) commit the caller's edits onto the live row, both pinned by new detachment tests. The repository family is done too: `GetRepo`/`GetRepoByFullName`/`GetRepoByID` now return a `cloneRepo` snapshot (deep-copying `ArchivedAt`/`InteractionLimitExpiry`/`HasDiscussions`, the `Topics` slice and the `Stargazers` map; the shared `Owner *User` cross-reference stays shallow), so a reader can no longer race `StarRepo`/`UnstarRepo`'s in-place `Stargazers` map write — the acute fatal concurrent-map-access case — nor leak a write back into the store. The copy-on-write `UpdateRepo(owner,name,fn)` already swapped in a fresh row rather than mutating in place, so the update path was safe; the one handler that mutated a getter result directly (`setRepoLFS`) was rerouted through `UpdateRepo`, and an audit confirmed no other through-getter repo mutations, no repo-pointer identity comparisons, and that every `*Repo`-parameter store method reads only immutable identifier/scalar fields; a `TestRepoGetIsDetached` pins the star-map/topics/scalar detachment. Those smaller singletons are done too: `GetPackage`/`GetDeletedPackage` (a `clonePackage` copying `DeletedAt`; the live rows are mutated in place by `recomputeVersionCountLocked`/`DeletePackage`/`RestorePackage`), `GetDependabotUserSecret` (a value copy; `UpsertDependabotUserSecret` mutates an existing secret in place), and `GetCodeScanningDefaultSetup` (copying the `Languages` slice; `SetCodeScanningDefaultSetup` stamps `UpdatedAt` in place) now return detached snapshots, and `GetCodeScanningAutofix`/`GetRepoSubscription` return defensive value copies even though their writers already swap in a fresh row — all pinned by `TestSmallStoreGetsAreDetached`/`TestRepoGetIsDetached`. Every exported single-object store getter now returns a detached snapshot except two documented deliberate exceptions: `GetCodeQLDatabase` (write-once row that `UpsertCodeQLDatabase` replaces by pointer-swap, holding a potentially large `Content` blob that cloning on every metadata read would needlessly duplicate) and `GetGitStorage` (a `gitStorage.Storer` backend handle, not a mutable data row). A follow-up sweep caught the single-object getters that return a live element from a `for … range` loop rather than a direct map index (the earlier sweep only matched `return st.X[key]`): `GetCopilotSpace`/`ListCopilotSpaces` now return a `cloneCopilotSpace` deep snapshot (copying the Collaborators/Resources slices and each resource's Metadata map) and `SaveCopilotSpace` publishes the caller's mutated snapshot back onto the live row (copy-on-write), closing the mutate-through path its handlers use; `GetCodeSecurityConfiguration`/`GetCodeSecurityConfigurationByName` return a `cloneCodeSecurityConfiguration` snapshot (four optional-scalar pointers) with the keyed `UpdateCodeSecurityConfiguration` and its handler already rendering the writer's fresh return; `GetDiscussionCategoryByName` (the missed sibling of the already-detached `GetDiscussionCategory`), `GetEnvBranchPolicy` and `GetEnvProtectionRule` return value-copy snapshots. `GetAppDelivery` is documented as a deliberate live-pointer exception (deliveries are write-once — appended, never mutated; a redelivery appends a new record — and each carries full request/response payloads cloning would needlessly copy). The `List*` methods that return slices of live element pointers are now being detached at the source too, starting with the acute pointer/slice/map-bearing element types whose writers mutate in place: every `[]*Repo` list (`ListForks`, `ListReposByOwner`/`ForOrg`/`ForUser`/`ForAuthUser`, `ListPublicRepos`, `ListTeamRepos`, `ListRepoSubscriptionsForUser`, `ListCodeSecurityConfigurationRepos`, `ListEnterpriseCodeSecurityConfigRepos`, `ListOrgReposForProperties`, `ListOrgImmutableReleasesRepos`) now returns `snapshotRepos(...)` clones — closing the fatal `Stargazers` concurrent-map race on the list path — and `ListIssues`, `ListPullRequests`, the `[]*Comment` lists (`ListIssueComments`/`ListRepoIssueComments`/`ListComments`/`ListCommentsFor`), `ListMilestones`, the `[]*Org` lists (`ListOrgsByUser`/`ListPublicOrgsByUser`/`ListOrgsAll`/`ListEnterpriseTeamOrgs`) and the `[]*Team` lists (`ListTeamsByUser`/`ListChildTeams`/`ListTeams`/`ListTeamsForRepo`/`ListTeamsWithOrgRole`) return `snapshot*` clones via their existing element clone helpers (all under the read lock). The security-alert, discussion, package and enterprise families followed: `ListCodeScanningAlerts`/`ByOrg`, `ListDependabotAlerts`/`ByOrg`, `ListSecretScanningAlerts`/`ByOrg`/`ByUser`, `ListDiscussions`, `ListPackages`, `ListPackageVersions`, `ListAttestations`, `ListCodeSecurityConfigurations`, `ListEnterpriseTeams` and the `[]*OrgInvitation` lists (`ListPendingOrgInvitations`/`ListFailedOrgInvitations`/`ListPendingOrgInvitationsForTeam`) each now return `snapshot*` clones via their existing element clone helper. The value-only-element `List*` methods were then swept too — a generic `snapshotSlice[T]` shallow-copies each element (a full snapshot for a struct with no slices/maps/pointers), applied to the check-suite, code-quality, env-policy, repo-invitation, autolink, artifact-storage, analysis, copilot-seat, dependabot-user-secret, discussion-category, issue-event, label, membership, package-file, classic-project, repo-activity, deploy-key, workflow-file, gist-comment and remaining user/webhook/gist-history lists — and the `[]*User` families got a `cloneUser` deep-copying `StarredRepos`/`Emails`/`ExternalIdentities` (StarRepo mutates the map in place). Fixing `handleSetWorkflowState` to route through the keyed `SetWorkflowFileState` rather than mutate a resolved snapshot closed the one caller that relied on the leak. Every exported single-object getter and every `List*` method now returns a detached snapshot, with four documented deliberate live-pointer exceptions where the row is genuinely write-once and cloning would copy large payloads needlessly: `GetCodeQLDatabase`/`ListCodeQLDatabases`, `GetGitStorage`, and the append-only webhook-delivery reads (`GetAppDelivery`/`ListAppDeliveries`/`ListMarketplaceDeliveries`/`ListDeliveries`). The `TestReliabilityDebtOnlyShrinks`-style architecture ratchet and the per-family detachment tests guard against backsliding |
| STORE-022 | M | store_projects_classic.go:34, store_rulesets.go:28 | `json:"-"` silently means "not durable": board ordering and ruleset version history are lost on reload | fixed — ruleset history and its next-version counter are durable and reload-tested, and classic project board ordering is now durable too: `ProjectColumn.Position`/`ProjectCard.Position` carry json tags, are persisted on create and on every column/card move, and are restored and sorted on reload. `TestPersistenceReload_ProjectBoardOrder` pins that a reorder whose position order differs from id order survives reload |
| STORE-023 | M | store_notifications.go:554, store_repo_reads.go:86 | Whole unbounded collections stored as one row and rewritten on every mutation — including on every `git clone` | fixed — both collections that auto-grew without bound (and were re-serialised in full on every mutation, so the per-mutation cost itself was unbounded) are now capped, making that cost O(cap). The clone-traffic actor set (`store_repo_reads.go`) is capped at `maxTrafficActors` so anonymous clones — the `git clone` path called out here — cannot grow it without limit. The per-user notification read-marker set `ReadThreadIDs`, which auto-grows on every thread a user reads and is the map re-serialised on every `MarkThreadRead`, is now capped at `maxReadThreadIDs` and prunes the oldest markers by read-at time once it grows a slack beyond the cap (batched so the prune amortises to O(log n) per mark; GitHub ages notifications out, so dropping the oldest is faithful) — `TestMarkThreadReadBoundsReadMarkers` pins that the oldest are dropped and the newest retained. The other per-user maps (`DismissedThreadIDs`, `Subscriptions`, `RepoLastReadAt`) grow only with explicit user actions or distinct-repo count, not automatically, so they are human-bounded rather than an unbounded/attacker-driven vector |
| STORE-024 | M | persistence.go:436, gh_api_insights.go:271 | `List` is the only read primitive and materializes whole buckets; one durable row per API request ever served | fixed — the request log's FIFO eviction was in-memory only: `RecordAPIRequest` capped `APIRequestRecords` but never deleted the evicted rows' durable `api_insights_requests` entries, so the bucket grew by one row per request forever (and the load path re-trimmed only in memory). Eviction now `MustDelete`s the evicted durable rows, and the load path `Delete`s any excess a pre-fix instance leaked, so the bucket converges to the cap. The cap is a per-Store field (`apiRequestRecordCap`, default `maxAPIRequestRecords`=10000) so a reload+runtime test drives it small and asserts the durable row count stays bounded to the newest-N. (`List` materialising whole buckets is the persistence design; bounding this bucket removes the growth this row is about.) |
| STORE-025 | M | store.go:958, persistence.go:211 | Sessions are never reaped and every logout scans and decodes every historical session row inside a write transaction | fixed — the scheduler now reaps expired login sessions hourly through a deterministic-clock, transactional deletion pass that survives restart and retries after persistence failure. And per-user logout no longer lists and decodes the whole durable session bucket: a durable secondary index (`login_sessions_by_user`, one row per session keyed `<userID>:<sessionStorageKey>`) is written in the same `persistBatch` as each session, so `DeleteLoginSessionsForUser` resolves a user's sessions with a bounded prefix scan (new `Persistence.ListPrefix`, an indexed `[prefix, prefix++)` range query). Because it reads durable state it revokes sessions this replica never cached, including ones created on other replicas — the property the old full scan provided and an in-memory index would have lost. The index is a superset of live sessions (Put adds atomically, so revocation never misses one; the bespoke OIDC back-channel transaction may leave a harmless stale row reclaimed at the next per-user purge), and `SetPersistence` backfills it once at startup so upgrades and drift self-heal. Reap/DeleteLoginSession/OIDC-delete drop index rows alongside their sessions. Cross-replica + backfill + disambiguation regressions in store_login_session_index_test.go. (The in-memory cache eviction still iterates the bounded in-memory map, which does no I/O.) |
| STORE-026 | M | store_packages.go:304, store_code_scanning.go:883 | Blob writes have no compensation and reuse IDs, silently aliasing orphaned objects | fixed — the CodeQL-database path already reclaims the superseded archive and compensates on metadata-persist failure (`UpsertCodeQLDatabase` deletes `previous` when its content-addressed path changes, with rollback), and package files use monotonic ids (no reuse). The remaining gap was `CreatePackageVersion`: a multi-file version wrote each file's bytes before committing any metadata, so a mid-loop write failure orphaned the bytes already written. It now tracks written blobs and deletes them (object store or local dir) on any write/mkdir failure before returning. Regression test with a byte store that fails the 2nd write asserts zero orphaned blobs and no committed metadata |
| STORE-027 | M | store_secret_scanning.go:228 + 3 | Bulk mutations validate inside the loop and commit a map-iteration-order subset before returning 422 | fixed — the remaining store-level bulk alert update validates and builds detached copies first, commits every durable row in one transaction, then publishes the result to memory |
| STORE-028 | M | store_orgs.go:247 + 4 | Delete paths enumerate secondary indexes, so soft-deleted packages orphan their S3 bytes and cancelled subscriptions leave access live | fixed — the package secondary-index half is fixed: `deleteRepoLocked` and `repoDeletionIntentLocked` iterated `PackagesByOwnerKey`, which a soft-delete prunes while keeping the row in the authoritative `st.Packages`, so a soft-deleted package's rows and file bytes were orphaned on repo delete. Both sites now iterate `st.Packages` filtered by owner (soft-deleted included). Regression test soft-deletes a package then deletes its repo and asserts the row is reclaimed. Those larger cascades are now done too: `deleteOrgMetadata` cascades org-directly-owned packages (rows, versions, files and their object bytes via the deletion intent) and cancels the org's Marketplace purchases; and admin user deletion (`handleAdminDeleteUser` via the new `deleteUserOwnedResourcesLocked`) cascades the user's repositories, directly-owned packages (+bytes), Marketplace purchases and org memberships, mirroring the org path, so a deleted account leaves no orphaned rows or object bytes. Regressions `TestDeleteOrgCascadesPackagesAndMarketplace` and `TestDeleteUserCascadesOwnedResources` |
| STORE-029 | M | store_workflow_files.go:51 | The primary key hashes the mutable repo name; rename re-persists under the old ID and the next registration duplicates the row | fixed — the repo-rename cascade (`moveRepoKeyLocked`) only rewrote `WorkflowFile.RepoFullName` while leaving the row keyed by `stableWorkflowFileID(oldName, path)`, so the next registration under the new name derived a different hash and inserted a duplicate. The cascade now re-keys each affected file to `stableWorkflowFileID(newName, path)` in both the map and persistence (snapshotting keys first to avoid mutating a map under iteration). Regression test renames a repo with a workflow file and asserts no stale-hash residue and no duplicate on re-registration |
| STORE-030 | M | store_workflow_files.go:180 | A discarded read error writes empty YAML over a good row, which the type's own comment says makes dispatch 422 forever | fixed — `DiscoverWorkflowFilesFromGit` now checks the error from `f.Contents()` and skips the file on failure instead of re-registering it with an empty body; a transient content read (e.g. a lazily-fetched S3-backed blob) no longer overwrites the good stored YAML. The next discovery re-reads it |
| STORE-031 | M | store_codespaces.go:372 | The encrypted codespace secret value is never referenced and the endpoint returns 201 | fixed — CodespaceSecret carries a Value field and CreateCodespaceSecret stores the decrypted plaintext on both the create and update paths; it is persisted only in the encrypted codespace_secrets bucket and never surfaced in an API response |
| STORE-032 | M | store_rulesets.go:269 | Three ruleset-suite endpoints discard their arguments and return nil | fixed — ref writes evaluate active/evaluate rulesets, persist official-shaped suites, and serve filtered/paginated list and detail endpoints with reload coverage |
| STORE-033 | M | store_secret_scanning.go:253 | `validateSecretScanningTransition` ends in an unconditional nil return, making its own state machine dead code | fixed — only same-state, open-to-resolved, resolved-to-resolved, and resolved-to-open transitions pass; every other state pair returns a validation error |
| STORE-034 | M | store_teams_people.go:208, store_copilot.go:42 | GET endpoints take the exclusive lock and perform durable deletes; a persist failure during a read kills the server | fixed — both halves resolved. The copilot-seat reads were made pure earlier; now the org-invitation reads (`ListPendingOrgInvitations`/`ListFailedOrgInvitations`/`GetOrgInvitation`) take a read lock and no longer run the reconcile state machine (durable deletes + `MustPut`) on a GET. The state machine — expire/consume/cancel with the durable membership side effects — now runs on the background schedule dispatcher (`ReconcileAllOrgInvitations`, recover-guarded since a background goroutine has no recover middleware), alongside the existing login-session reap. Consume already ran event-driven on membership activation, so only TTL-expiry timing shifts (≤ one tick). Regression tests: a read leaves an expired invitation un-failed and its pending membership intact, while the reconciler applies both durably. |
| STORE-035 | M | git_storage.go:27 | A transient S3 client failure is memoized for the process lifetime with no reset | fixed — initialization becomes sticky only after success (or when object storage is intentionally disabled), so transient discovery/credential failures retry |
| STORE-036 | M | persistence.go:386 | Unbounded retry treats every error as "waiting for quorum", including a malformed address map that can never succeed | fixed — `MustNewPersistence` retried every `NewPersistence` error forever (1s intervals) whenever dqlite was configured, so a misconfiguration (malformed `BLEEPHUB_DQLITE_ADDRESS_MAP`, missing transport secret, empty server set, bad encryption key) or a schema written by a newer build looped endlessly behind a "waiting for dqlite quorum" log. The unrecoverable config/schema/encryption paths now return a `permanentPersistenceError`, and `MustNewPersistence` fails fast on it (`log.Fatalf`) while still retrying the genuinely transient dial/ping/query failures that occur while a quorum forms. Regression tests cover the classifier and a malformed address map |
| STORE-037 | M | s3fs.go:93 vs :190 | `Open` misses the `NotFound` shape, and go-git discards the read error — a transient S3 failure reads as "ref does not exist" and a live branch is overwritten | fixed — verified the s3fs layer already maps only the definite-404 shapes (`GetObject`→`NoSuchKey`, `HeadObject`→`NoSuchKey`/`NotFound`) to `os.ErrNotExist` and wraps every other failure, and the atomic-ref wrapper propagates any non-`ErrNotExist` read error (`RemoveReferenceCAS` returns it; `CheckAndSetReference` delegates to go-git, which propagates non-NotExist errors), so a transient failure fails the CAS safely rather than overwriting a live branch. Locked the data-loss invariant in with a regression test that injects a retryable HTTP 500 via SDK middleware and asserts `Open`/`Stat` return a non-`ErrNotExist` error |
| STORE-038 | M | s3fs.go:181,261 | The active buffer is invisible to `Stat` and `ReadDir`; a failed `Close` leaves a phantom file and leaks memory | fixed — `Stat` reports staged size, `ReadDir` merges and deduplicates the shared staging namespace with S3, and `Close` removes the staging entry whether its flush succeeds or fails |
| STORE-039 | M | store_issues.go:126 | Every PR event is written first under the issue parent type, then rewritten — a crash in the window files it against an unrelated issue | fixed — issue events are built with their final ParentType (and extra fields) already set and persisted exactly once via buildIssueEventLocked + persistIssueEventLocked, so a pull-request event is never first written under the issue parent and rewritten. Reload regression asserts the durable parent type |
| STORE-040 | M | store_orgs.go:552 | `UpdateTeam` has no slug collision check, orphaning the shadowed team from every index-driven operation | fixed — the store validates a detached team copy before re-keying, so a rejected collision cannot corrupt either the primary row or slug index |
| STORE-041 | M | store_codespaces.go:175 | Container and workspace created before any durable record exists, with no startup reconciler | fixed — both halves closed. `reserveCodespace` already commits the durable "Provisioning" record before `CreateCodespace` starts the container (record precedes container, with a `cleanup()` on every failure path), and `loadFromPersistence` now reconciles the remaining gap: a codespace persisted as "Provisioning" is an interrupted-creation orphan whose container never survives a restart, so on boot it is transitioned to the resumable "Shutdown" state (whose start path re-provisions) and the heal is committed in one transaction. Settled codespaces are untouched. Regression `TestInterruptedCodespaceProvisioningReconcilesOnLoad` (orphan → Shutdown, durable across a second reload; an Available codespace undisturbed) |
| STORE-042 | M | store_packages.go:275 | Missing the "persistence implies object store" guard its sibling has, so metadata replicates while bytes stay on one node | fixed — `CreatePackageVersion` now enforces the same `st.persist != nil && st.ObjectByteStore == nil` guard its attestation and code-scanning siblings already carry: with persistence enabled, package bytes must go to the shared object store rather than a single node's local package directory (where replicas that see the replicated metadata could not read them). Regression test asserts the error fires with persistence enabled and no object store, using a non-empty PackageDataDir so it exercises this guard rather than the pre-existing "not configured" one |
| STORE-043 | M | store_projects_v2.go:782 | Field update re-mints every option ID, dangling every item's stored value | fixed — UpdateField now preserves the ID of any single-select option kept by name (the mutation input carries only names), minting fresh IDs only for genuinely new options, so an item's stored OptionID no longer dangles after a field edit. Regression asserts a kept option's ID is stable |
| STORE-044 | M | store_issues.go:718 + 3 | Caller slices and maps adopted by reference across the store boundary | fixed — the setters now clone rather than adopt caller-owned collections: `SetIssueLabels`/`SetPullRequestLabels` (LabelIDs), `UpsertDependabotOrgSecret`/`CreateCodespaceSecret` (SelectedRepoIDs, cloned once so both create and update branches are safe), `SetCopilotCodingAgentSelectedRepos` (SelectedRepositoryIDs) and `SetCopilotContentExclusion` (the rules map and its nested slices). Regressions mutate the caller's slice/map after the call and assert stored state is unaffected |
| STORE-045 | M | gh_actions_permissions.go:343 + branch_protection | In-place `s[:0]` filtering rewrites the backing array readers already hold | fixed — every in-place `[:0]` filter that operates on a *stored* slice (a concurrent reader could hold its backing array) now uses the full three-index reslice `[:0:0]`, forcing `append` to allocate a fresh array instead of overwriting the original: `s.store.PendingMessages` (×3: run-control + workflows), `OrgOIDCPropertyInclusions`, `Misc.keysByUser`, enterprise `team.MemberIDs`, hosted-runner `img.Versions`, codespace `access.SelectedUsernames`, runner-group `a.Labels` (×2), org-actions `p.SelfHostedRunnersSelectedRepoIDs`. The residual `[:0]` sites operate on handler-owned freshly-listed/cloned locals no external reader holds. Full `-race` suite green |
| STORE-046 | M | store_pulls.go:521 | Full scans where an index exists, giving path-dependent answers between sibling endpoints | fixed — `findPRByRepoNumberLocked` (the resolver for the four reviewer-request mutations) linearly scanned `st.PullRequests`, returning an arbitrary `(repoID, number)` match in map order, while every read/merge path used the `PullsByRepo` index. It now resolves through that same index, so all sibling endpoints share one source of truth (and it is O(1)). Regression test desyncs the flat collection from the index and asserts the reviewer path follows the index |
| STORE-047 | m | persistence.go:416 | One process-wide mutex guards every operation and covers marshalling, unlike `PutBatch` | fixed — `Put`/`Delete` now build a `persistBatch` and commit it, and `persistBatch.Put` runs `json.Marshal` before `Commit` → `apply` ever takes the process-wide lock, so marshalling happens outside the mutex exactly as `PutBatch` does |
| STORE-048 | m | s3fs.go:426 | One delete per object with no paginator under a fixed wall-clock budget covering all pages | fixed — repository deletion repeatedly drains listed pages with S3 multi-object batches; rename snapshots all pages, copies them, then batch-deletes the source |
| STORE-049 | m | s3fs.go:73 | `Chroot` enforces no boundary and ref names are unvalidated at the handler | fixed — the repo path is normalized (`owner/name`) at every storage entry point, and reference names are now validated at the `atomicRefStorer` write boundary (`SetReference`/`CheckAndSetReference`/`CreateReference`/`RemoveReference`/`RemoveReferenceCAS`), which wraps every backend. `checkSafeRefName` rejects any name go-git's `IsSafe` flags (empty/`.`/`..` segments, backslash) before it reaches `path.Join`, so a crafted ref can no longer escape the per-repository prefix; legitimate refs (`refs/heads/*`, `refs/pull/N/merge`, tags, `HEAD`) are unaffected. Regression test covers both directions |
| STORE-050 | m | store_secret_scanning.go:586 | A literal `null` row written when the key was just deleted, reloading as a permanent tombstone | fixed — the push-protection bypass pruner now `MustDelete`s the durable row when the last bypass for a repo expires, instead of `MustPut`ing the now-nil slice (which marshalled to `null` and reloaded as a phantom nil-value key); reload regression added |
| STORE-051 | m | store_rulesets.go:209 | Comment promises ID ordering; the code returns randomized map order under offset pagination | fixed — repository and organization rulesets and version history are explicitly ID-sorted before pagination |
| STORE-052 | m | store_repos.go:449 | Deletes against keys that can never exist, occupying the place real cleanup would go | fixed — two `batch.Delete("secret_scanning_alerts", <repo full name>)` calls (one in the repo-delete cascade, one in rename) were dead no-ops: that bucket is always keyed by the integer alert ID, never a repo full name, so a full-name key can never match. Removed both. The real cleanup already happens — the delete cascade deletes each alert by `strconv.Itoa(id)`, and rename re-Puts each alert under its unchanged integer ID with the new RepoKey — so this is pure dead-code removal with zero behavior change |
| STORE-053 | m | store_migrations.go:202 | The `*Locked` suffix carries two opposite meanings, risking self-deadlock on a non-reentrant mutex | fixed — the two outliers `IsUserMigrationRepoLocked`/`IsOrgMigrationRepoLocked` (exported, and they *acquire* `st.mu`) were renamed to `UserMigrationLocksRepo`/`OrgMigrationLocksRepo`, out of the pervasive `…Locked` = caller-already-holds-the-lock convention, so neither can be mistaken for a lock-held helper and deadlocked. Both were dead code (no callers), so the risk was latent; the doc comments now state they take the lock |
| STORE-054 | m | store.go:919 | `loadFromPersistence` mutates every map with no lock and opens git storage serially per repo | fixed — the per-repo git-storage open is now concurrent: the load pass collects repo names and `openRepoStoragesConcurrently` reopens them in a bounded pool (16), so restart latency no longer scales linearly with the repository count (the open is filesystem/S3 I/O). Handles are written to distinct result indices and consumed after the workers join, so `st.GitStorages` is still mutated single-threaded — `-race` reload tests confirm it. The unlocked map mutation is benign: `loadFromPersistence` runs at startup before the server serves, and runtime `ReloadFromPersistence` swaps in a freshly-built candidate store rather than mutating the live one |
| STORE-055 | m | dqlite-node/main.go:36 | Hardcoded listen port while the advertised address is configurable; fixed voter count | fixed — the listen address defaulted to a fixed `":9000"`, so a node advertising a different port (`BLEEPHUB_DQLITE_ADVERTISE_ADDR=host:7000`) listened on `:9000` and peers dialing the advertised port failed to connect. The node now derives the default listen port from the advertised port (via `net.SplitHostPort`; explicit `BLEEPHUB_DQLITE_LISTEN_ADDR` still wins for NAT/proxy), and the voter count is now configurable via `BLEEPHUB_DQLITE_VOTERS` (default 3, guarded > 0) instead of hardcoded. The derivation is inlined in the node binary because the deadcode gate analyzes only `cmd/bleephub` and CI has no dqlite-tagged test job, so a separate helper would be flagged unreachable |
| STORE-056 | m | persistence.go:269 | The address map is resolved once at construction — the very drift the package exists to handle | fixed — refuted: the address map's only source is the process environment variable `BLEEPHUB_DQLITE_ADDRESS_MAP`, immutable for a process's lifetime, so re-reading it per-dial yields the identical value (a behavioral no-op). The genuine runtime drift — the live quorum-member set changing — is already handled without the map: the dqlite driver refreshes membership from the quorum itself, and `Map.Resolve` passes any unmapped address through unchanged, so a new member is dialed directly. The map is a static retired→stable translation table; a construction-time snapshot fully serves its purpose. Patching it to re-read per-dial would only mislead readers into thinking live reconfiguration works without a restart |
| STORE-057 | m | store_code_scanning.go:832 | Unchecked slice index panics under the global write lock | fixed — `CreateCodeScanningAutofix` guards the empty-`Instances` case instead of indexing `len-1`, so a malformed or partially-loaded alert degrades the autofix description gracefully rather than panicking the store under the write lock; regression covers the no-instances alert |
| STORE-058 | m | store.go:3358 | Panic wrappers around fallible methods whose error-returning variants already exist | fixed — the `panic(err)` wrappers `CreateGist`/`UpdateGist`/`ForkGist` had error-returning twins (`CreateGistE`/`UpdateGistE`/`ForkGistE`) that the production REST handlers already use; the panic variants were reached only by one race test (and `ForkGist` had no callers at all). Removed all three panic wrappers and pointed the race test at the `…E` variants, eliminating the panic-on-error footgun |
| STORE-059 | m | store_discussions.go:250 | Tombstones retained forever and scanned globally on every create | fixed — `CreateDiscussion` numbered each new discussion by scanning every discussion in the store (all repos, tombstones included) for the per-repo max, so the cost rose with every soft-deleted discussion. Replaced with a `NextDiscussionNumber map[int]int` high-water counter (mirroring `SecretScanningNextNumber`): O(1) on create, monotonic across tombstones (a deleted number is never reused), and restored on reload by the discussions loader. Regression test proves the counter survives a persistence reload and stays monotonic across a tombstone |
| STORE-060 | m | store_copilot.go:226 | A pure read takes the write lock and inserts a never-persisted entry | fixed — `GetCopilotCodingAgentPermissions` now takes the read lock and returns the default posture through a non-mutating read helper, so a pure read neither blocks writers nor writes a phantom entry; the set/add/remove write paths keep materializing on demand. Regression asserts no phantom entry is created |
| STORE-074 | M | store.go reload path, store_repos.go | The `repo_collaborators` bucket is written on every collaborator change but has no reload loader and is rebuilt from nothing, so every repository's collaborators are silently lost on restart | fixed — added a `repo_collaborators` loader to the reload path (mirroring `repo_deploy_keys`) so collaborator permissions survive a restart; reload regressions cover both add and remove |
| STORE-061 | B | s3fs.go | `s3File.Close` kept the staging entry when the flush failed, so every later reader of that key was served the bytes of a write that never landed | fixed — the entry is dropped either way |
| STORE-062 | M | persistence.go | `Persistence.Close` left itself installed as the git object lock manager, so a closed database failed every subsequent ref update with "database is closed" | fixed — it uninstalls itself |
| STORE-063 | M | persistence_reload_test.go, gh_repos_test.go | Two tests asserted the pre-fix defect as the contract: one required a recreated repository to **reuse** a deleted one's identifier, the other required bytes-before-metadata deletion ordering, which also made a repository undeletable whenever S3 was down | fixed — both inverted to the corrected contract |
| STORE-064 | B | terraform/ | `BLEEPHUB_DQLITE_SECRET` is now required on both the server and dqlite-node tasks or a dqlite deployment refuses to start; the module does not supply it | fixed — module-generated `random_password` (64 alphanumeric chars, because the value travels in an HTTP header and a header-unsafe alphabet would be a latent bug), stored in Secrets Manager under the customer-managed key with `prevent_destroy`, injected into the application task and every dqlite voter as ECS `secrets` rather than `environment` so it never appears in `DescribeTaskDefinition`. Verified by three negative controls — removing either end's block, or the role's grant, each fails the test with its own message |
| STORE-065 | M | store*.go | The store exposed a deterministic clock but 24 store modules bypassed it with direct `time.Now` calls, so expiry, persistence/reload, generated CVE years, and Project V2 timestamps changed with the wall clock even under deterministic tests | fixed — all store modules use the concurrency-safe injected clock, Project V2 shares the parent store clock, CVE generation receives the operation time explicitly, and an AST ratchet permits direct wall-clock access only inside the two clock fallback methods |
| STORE-066 | M | store.go | `IsUserFollowing` and `ListUserBlocks` dereferenced `st.Users` while holding only `Misc.mu`, but that map is guarded by `st.mu` everywhere else, so a concurrent user create/upsert was an unsynchronized map read/write | fixed — both readers snapshot the needed ids under `Misc.mu`, release it, then resolve users via the `st.mu`-guarded accessor; a `-race` regression runs the readers against concurrent `st.Users` writers |
| STORE-067 | m | store.go | `LookupUserBySSHKey` held `Misc.mu` and then called `GetUserByID` (`st.mu`), a Misc→Store nesting that inverts the Store→Misc order the reentrancy gate protects | fixed — the user id is resolved under `Misc.mu`, which is released before `GetUserByID`; the SSH-auth reader is covered by the race regression |
| STORE-068 | M | gh_deployments.go, gh_releases.go | `ListDeployments` (and `ReleaseStore.List`) returned the slice in `byRepo` map-iteration order, which the reload path rebuilds arbitrarily, so pagination boundaries shifted across restarts | fixed — both list newest-first by descending id, a total order stable across reloads; a regression scrambles `byRepo` and asserts the order |
| STORE-069 | M | store_projects_classic.go | `ProjectColumn.Position` and `ProjectCard.Position` were tagged `json:"-"`, so they never persisted; after any restart every position was zero and the board returned in arbitrary order | fixed — Position round-trips through the persistence bucket (the API JSON is built by separate mappers, so it stays unexposed); a reload regression asserts board order and strictly-increasing positions survive |
| STORE-070 | B | gh_apps_store.go | `DeleteApp` deleted the parent `apps` row first and each child credential bucket in its own transaction, so a crash between them left valid `ghu_`/`ghs_`/`ghr_` tokens durable and still resolving for an app that no longer existed | fixed — every child credential and the parent row are staged into one `persistBatch` (children before parent) and committed atomically; a regression asserts no token resolves in memory or survives a reload after deletion |
| STORE-071 | B | store_orgs.go, store.go | Core-entity ids (orgs/users/repos/teams/issues) are minted from in-memory `NextX++` counters rebuilt from `max+1` on load, not the durable `AllocateCounterValue` that run/log ids use, so two dqlite replicas can mint the same id and the second write overwrites the first | fixed — every core-entity global id is reserved through `reserveGlobalID`, the same durable `AllocateCounterValue` path run/log ids use (the in-memory NextX supplies the minimum and seeds from existing data; per-repo issue Number is untouched); regression asserts ids stay durable and non-colliding across a reload |
| STORE-072 | m | store_login_sessions.go | `PutLoginSession` persists and then updates memory outside one critical section, so two concurrent writes to the same session id can leave durable and in-memory state disagreeing until the next refresh | fixed — the durable write and the in-memory map update are one critical section under `st.mu`; a `-race` regression writes the same session id concurrently |
| STORE-073 | m | store.go, store_login_sessions.go | Deleting a user drops their sessions from memory on reload but leaves the durable `login_sessions` rows until natural expiry, so the bucket the reap pass bounds accumulates orphaned rows | fixed — a deleted user's login sessions are dropped from memory and the durable bucket together; regression asserts none survive the deletion |

## CORE — server lifecycle, observability, configuration

| ID | S | Location | Finding | Status |
|---|---|---|---|---|
| CORE-001 | B | cmd/bleephub/main.go:51, server.go:518 | No signal handling anywhere in the server binary and `srv.Shutdown` is never called; as PID 1 the process dies instantly mid-request | fixed — `signal.NotifyContext` on SIGINT and SIGTERM, with a bounded drain. The bound is deliberate: this server holds long-poll requests open by design, so an unbounded wait means never stopping, and Docker and Kubernetes both default to a ten-second grace before SIGKILL. Verified by reversion |
| CORE-002 | B | cmd/bleephub/main.go:33 | `defer obs.Shutdown` is unreachable because every exit path goes through `os.Exit`; telemetry is never flushed | fixed — `main` now only chooses an exit status and every deferred cleanup lives in `run`, because a `defer` and `os.Exit` in the same function means the cleanup never runs. The flush has its own deadline, since the exporter's endpoint may be the thing that has gone away |
| CORE-003 | B | server.go:524 | No panic-recovery middleware; a panic yields no 500, no log, no span, and can leave the store mutex held | fixed |
| CORE-004 | B | server.go:549 | `BPH_TLS_CERT` without `BPH_TLS_KEY` silently serves plaintext HTTP on the port the operator believes is TLS | fixed |
| CORE-005 | B | workflow_schedule.go:21, git_ssh.go:52 + 10 | Fifteen-plus goroutines with no owner; the cron dispatcher is an unstoppable `for { time.Sleep }` and the SSH listener is never closed | fixed — every goroutine the server starts goes through `goBackground`, so shutdown waits for them and "which are still running" has an answer. The cron dispatcher honours its context instead of sleeping through shutdown, and the SSH accept loop's listener is closed, which is what unblocks `Accept`. Verified by reversion: restoring the `time.Sleep` loop fails with "it is sleeping through shutdown" |
| CORE-006 | M | server.go:655 | The response wrapper implements neither `Flusher` nor `Hijacker`, so `Flush()` is a silent no-op server-wide | fixed — both response wrappers delegate `Flush` and `Hijack`, return `http.ErrNotSupported` when the underlying writer cannot hijack, and preserve streaming and upgrade capabilities through the middleware chain |
| CORE-007 | M | otel.go:128 | Log records emitted with `context.Background()`, so logs and traces can never be correlated | fixed — the zerolog→OTel bridge no longer hardcodes `context.Background()`: `logEmitContext` reconstructs the emitting span's context from the `trace_id`/`span_id` fields on the line (rejecting malformed hex) and emits under it, so the SDK stamps the record's trace context and logs correlate with their trace; the two fields are promoted to trace context rather than duplicated as attributes. The request-completion log and the panic log stamp those fields from the active request span via `withTraceContext`; spanless startup/background logs still emit unchanged. Regression `TestOTelLogBridgeCorrelatesTrace`/`…SpanlessLineEmits`/`TestLogEmitContextRejectsBadHex` |
| CORE-008 | M | otel.go:103,87 | A log line that fails to unmarshal is reported as written and vanishes; runtime-metric startup failure discarded | fixed — the log bridge now emits an unparseable line verbatim (raw body, Info severity) instead of dropping it, so it stays observable; `runtime.Start`'s error is no longer discarded but tears down and returns like every sibling exporter failure in InitObservability. Regression asserts a malformed line still produces an OTel record |
| CORE-009 | M | server.go:532 | No global request-body cap and no `MaxHeaderBytes`; one handler out of hundreds bounds its body | fixed — the production listener now caps headers and the shared request pipeline applies a structured 32 MiB body limit to every JSON and form request, with a GitHub-shaped 413 regression. And those endpoint-specific streaming caps now have one auditable registry: `requestBodyLimits` (gh_rest.go) enumerates every request-body cap (shared structured pipeline, JSON decode, binary upload, artifact chunk, git upload-pack, timeline, backchannel logout) with its bytes and scope, the last inline magic number (`64<<10`) is named `maxBackChannelLogoutBytes`, and `TestRequestBodyLimits` both sanity-checks the registry and fails if any production `MaxBytesReader` bounds a body with a bare numeric literal — so a new upload route cannot introduce an unaudited cap |
| CORE-010 | M | server.go:535 | A paragraph-long comment justifies having no read or write deadline, leaving a slow-body Slowloris open | fixed — the header-read Slowloris was already bounded by `ReadHeaderTimeout`; the remaining gap was a slow *body* (a client that announces a Content-Length then trickles/stalls it), which a fixed `ReadTimeout` would close only by also cutting off large git pushes. A new `slowBodyGuard` middleware — placed outermost so its `ResponseController` resolves to the base net/http writer — wraps each request body and resets a *sliding inactivity* read deadline (60s) before every read: a client making steady progress never trips it, a stalled one is dropped. `ReadTimeout`/`WriteTimeout` stay unset so large uploads are unaffected. Integration test (real server + raw socket) proves a stalled upload is dropped while a prompt one succeeds |
| CORE-011 | M | ui_embed.go:1, ci.yml:23 | `-tags noui` on every test invocation means the entire SPA handler is never compiled or tested in CI | fixed — `-tags noui` scopes the unit-test invocation only. `ui_embed.go` is `//go:build !noui`, so the default-build `go vet ./...` (ci.yml:79) and `make build` (ci.yml:458) both compile the SPA handler UI-embedded, and the Browser end-to-end job (ci.yml:446-460) runs that binary through Playwright against `/ui/` — so the handler is compiled and exercised in CI, just not unit-tested |
| CORE-012 | M | ui_noembed.go:5, server.go:414 | Under `noui` the root redirect points at a `/ui/` nothing serves, producing a warning and a 404 for a route the server advertised | fixed — the `GET /{$}` root registration moved out of the unconditional `registerRoutes` and into the build-tagged `registerUI`. The UI-embedded build registers the redirect to `/ui/` (which it serves); the no-embed build registers the root to return an honest 404 directly, so `noui` no longer 307-advertises an unserved `/ui/` nor trips the catch-all's "unhandled request" warning, and the registered route set stays identical across builds. Regression test (noui-tagged) asserts the root returns 404, not a redirect |
| CORE-013 | M | ui_embed.go:12, .gitignore | 73 committed build artifacts with no CI check that they match a fresh build | fixed — the generated UI is no longer committed, so it cannot go stale. The `internal/server/dist` tree (go:embed'd by `ui_embed.go`) was 73 Vite output files tracked in git; a byte-exact freshness gate was tried but Vite 8's native bundler (rolldown + lightningcss) is not byte-reproducible across CPU architectures, so a mac-built commit can never match a linux CI rebuild. Instead `internal/server/dist` is now gitignored except a single `.gitkeep`, which keeps `//go:embed all:dist` compiling on a fresh checkout, and the SPA is rebuilt only where it is consumed — the release image's `ui-builder` stage (`Dockerfile.release`, which already regenerated it and whose `.dockerignore` excludes the tree) and `make build`. Staleness is structurally impossible because no generated artifact is committed. CI still runs `bun run build` so a broken production build fails, and `TestSPAHandlerServesAssetsAndFallsBackToIndex` exercises the handler against a synthetic in-memory FS rather than the embed. |
| CORE-014 | M | persistence.go:400 | `MustPut` calls `log.Fatalf` from inside a request handler; with one task and no minimum healthy percent that is a full outage on one transient write error | fixed — refuted: `MustPut`/`MustDelete` no longer `log.Fatalf`; they panic with `*persistenceFailure`, which `recoverMiddleware` catches, reloads durable state, and turns into a 500 for that one request rather than killing the process. The remaining `log.Fatalf` calls are in `MustNewPersistence`, which run at startup/operator-config (not in a request handler), which is correct. Covered by the atomicity tests asserting the Must* helpers raise `*persistenceFailure` rather than exiting |
| CORE-015 | M | persistence.go:386 | Unbounded startup retry before any listener exists and before signal handling, invisible to health checks | fixed — the dqlite-quorum retry in `MustNewPersistence` is now bounded by a `startupQuorumDeadline` (5m) after which it fails loudly with a clear fatal instead of retrying forever, and it registers its own SIGTERM/Interrupt handler and `select`s on it during each wait, so an orchestrator can stop a still-starting process cleanly rather than SIGKILL it. The retry can no longer hang invisibly: it resolves to success, a bounded fatal, or a clean signal exit. (The crash-only ordering still brings the HTTP listener up after persistence; moving it earlier is a separate change) |
| CORE-016 | M | server.go:445 | `/health` returns a hardcoded `ok`, touches no dependency, and leaks version and enterprise slug unauthenticated; no readiness endpoint exists | fixed — dependency-aware readiness already moved to `/ready` (rejects a poisoned recovery state or a failed persistence ping; ECS probes it). This change removes the unauthenticated metadata leak: `/health` is now a liveness-only `{status, service}` probe, and the build identity (version/commit/published_at) plus enterprise slug moved to the site-admin-gated internal status endpoint. Its codified consumer test was relocated to assert `/health` leaks nothing and the authenticated endpoint carries the identity |
| CORE-017 | M | workflows.go:809 | Job duration measured from the workflow's creation time, inflating every job in a multi-job run | fixed |
| CORE-018 | M | metrics.go:14 | Job durations ring-buffered under a mutex and read by nothing | fixed |
| CORE-019 | M | workflows.go:238 + 3 | Zero `RecordError`/`SetStatus` calls repo-wide, so traces cannot distinguish success from failure | fixed — refuted at the HTTP choke point: otelhttp (v0.69.0) already sets the top-level request span's status from the response code (`span.SetStatus(semconv.Status(code))`, mapping 5xx→Error, <5xx→Unset per correct HTTP-server semantics), and `recoverMiddleware` records the panic path — so traces already distinguish a 5xx failure from success without any repo-side call. The residual (the ~4 manually-created internal spans in workflows.go not calling RecordError/SetStatus) is per-span business-logic work with no shared producer, tracked separately; it is not the claimed repo-wide gap. |
| CORE-020 | M | server.go:433 | Raw query strings logged at WARN on a server that implements OAuth — codes, client secrets and tokens reach logs and telemetry | fixed |
| CORE-021 | M | otel.go:41, terraform/main.tf:1416 | No `OTEL_*` variable in the task definition, so the whole telemetry stack is inert in the shipped deployment | fixed — the server already gates export on `OTEL_EXPORTER_OTLP_ENDPOINT` (`otelExporterConfigured`, CORE-022) but the ECS task definition never set it, so there was no way to turn the stack on. Added a `otel_exporter_otlp_endpoint` variable (plus `otel_service_name`) and a conditional block in the task-def `environment` concat: empty (default) adds no `OTEL_*` vars — identical to the shipped behaviour — and setting the endpoint injects `OTEL_EXPORTER_OTLP_ENDPOINT`/`OTEL_SERVICE_NAME`, the single switch that activates traces/logs/metrics. `terraform fmt`/`validate` clean; `terraform test` 21/21 pass |
| CORE-022 | M | otel.go:41 | Per-signal OTLP endpoint variables are honoured by the exporters but not by the enable gate | fixed — `InitObservability` now gates on `otelExporterConfigured`, which returns enabled when the general `OTEL_EXPORTER_OTLP_ENDPOINT` or any per-signal endpoint (`..._TRACES/_LOGS/_METRICS_ENDPOINT`) is set, so an operator who configures only a per-signal endpoint no longer gets telemetry silently disabled. Tests cover the gate per-var and the end-to-end traces-only path |
| CORE-023 | M | main.go:23, server.go:122 | Invalid `--log-level` and `BLEEPHUB_MAX_WORKFLOWS` silently replaced with defaults; 40+ scattered `os.Getenv` calls across 12 files | fixed — an invalid --log-level now fails fast with a clear message instead of silently becoming info; an invalid BLEEPHUB_MAX_WORKFLOWS now logs a warning (value + default used) instead of being silently ignored |
| CORE-024 | M | actions_test.go:276 | The test helper hand-builds a partial `*Server`, producing eight nil-guards in production code and leaving `NewServer` untested | fixed — stale premises: `newTestServer` no longer hand-builds a partial server, it goes through the shared `newServerState` constructor exactly as `NewServer` does, and `NewServer` itself is exercised directly (the suite-wide `testServer` is built by `NewServer` in `bleephub_test.go:169`, and `build_info_test.go`/`lifecycle_test.go`/`otel_test.go` call it and drive real handlers). The only remaining `== nil` field guards (`artifacts.go:513`, `store_repos.go:532`) are legitimate optional-config guards for a nil object byte-store when S3 storage is unconfigured, not test artifacts |
| CORE-025 | M | main.go:36, persistence.go:395 | ANSI console logging in production plus 29 stdlib `log` calls that bypass zerolog, the level filter and the telemetry bridge | fixed — production logging now defaults to structured JSON, with ANSI console output opt-in via `BLEEPHUB_LOG_FORMAT=console`. Every server-runtime stdlib `log.*` call is gone: store-layer error logging routes through a wired `Store.logger` (the configured zerolog logger; a nop until `NewServer` sets it), and the boot-time package functions (`MustNewPersistence` quorum wait, `AdminToken` requirement) route through the zerolog global logger, which `main.go` points at the same configured logger. The stdlib `log` package is only used by the standalone `internal/emojigen` build-time generator, which has no logging pipeline and correctly stays on it. Regression test asserts store errors emit structured fields |
| CORE-026 | M | internal/emojigen/main.go:42 | A third-party tarball downloaded by mutable tag with no checksum, no pinned commit and no client timeout, then committed | fixed — the generator downloads immutable commit `b6b55fef1e8636b540a6d016a4729ca8cdf2e60b` with a five-minute client deadline and verifies SHA-256 `705d…f747` before extraction, including for a caller-supplied local tarball. A negative test proves unpinned bytes are refused |
| CORE-027 | m | server.go:665 | `writeJSON` discards the encode error, shipping a truncated body with a success status | fixed |
| CORE-028 | m | server.go:530 | Every HTTP span is named `bleephub` with no route attribute | fixed — route()/routeDispatch() name each HTTP span by its matched route template and record it as the http.route attribute, replacing the blanket 'bleephub' name |
| CORE-029 | m | metrics.go:76 | `ReadMemStats` stops the world while holding the metrics mutex | fixed |
| CORE-030 | m | metrics.go:9 | JSON tags on a struct that is never marshalled, implying a contract that does not exist | fixed — removed the JSON tags from the never-marshalled Metrics accumulator; the serialized contract remains MetricsSnapshot |
| CORE-031 | m | emojiart.go:94 | Byte-index slicing on names that may be multibyte | fixed — initials() slices by rune via firstRunes(), so multibyte emoji names are never cut mid-rune |
| CORE-032 | m | ui_embed.go:21 | `/ui/` bypasses the route registry, weakening the enumerable-surface invariant to an undocumented exception | fixed — documented /ui/ as a deliberate, explained exception to the route-registry invariant (it cannot join routePatterns without breaking the authz-matrix/api-definition tests) |
| CORE-033 | M | server.go:816, ui_embed.go:36 | The `/ui/` SPA shipped no security headers and there was no global header middleware; CSP / X-Frame-Options / nosniff existed only on individual identity and Pages handlers | fixed — `securityHeadersMiddleware` wraps the whole handler chain, setting `X-Content-Type-Options: nosniff` and `Referrer-Policy` on every response, `Strict-Transport-Security` over TLS, and `X-Frame-Options: DENY` + a `Content-Security-Policy` for `/ui`; stricter per-endpoint CSPs run after and are not clobbered. Unit-tested |

## WEB — frontend

| ID | S | Location | Finding | Status |
|---|---|---|---|---|
| WEB-001 | B | api.ts:279-386, OverviewPage.tsx:18 | Metrics computed client-side by walking every repo, every run page and every run's jobs, re-run every 3 seconds — one tab self-DoSes the server | fixed — `GET /internal/metrics` + `GET /internal/status`, two requests where there were 1 + N repos + every page of their runs + every run's jobs + a runners call per repo. Also removed a duplicate `["metrics"]` query racing itself at 3s against 5s. One honest gap: the server has no total of stored workflow runs (`WorkflowSubmissions` counts something else), so the card is relabelled "Workflow submissions" rather than showing the wrong number under the old label, with a test asserting no card reads "Workflow runs". See WEB-045 for the non-admin consequence and WEB-046 for the remaining fan-out |
| WEB-002 | B | 12 pages | 19 state-changing controls, including security and destructive ones, have no error handling and never render an error | fixed — 23 sites / 28 controls across 10 files, via a shared `useMutationError` + `MutationError` over the existing banner, which gained `role="alert"`. The survey's "19 across 12 pages" was wrong in both directions: it scoped the search to the file rather than the enclosing component. Includes the three hand-rolled ones — both `LoginPage` auth submits left the button spinning forever, and `AppHeader.submitLogout` could skip the logout POST entirely. Verified 0 mutations remain with no `onError`, no rendered error and no `MutationError` |
| WEB-003 | B | components/ui.tsx:382 | The only dialog primitive, used at 40 sites, has no dialog role, no focus trap, no Escape and leaves the page tabbable | fixed at the primitive, so all 40 `<Modal>` usages across 21 files — `role="dialog"`, `aria-modal`, `aria-labelledby`, focus-into-dialog, a Tab/Shift-Tab trap, Escape, and focus restored to the invoking element. Note a mount-order stack is the wrong way to find the topmost dialog: React runs effects child-first, so a nested dialog registers before its parent. Resolved by document order; caught by the stacked-dialog test |
| WEB-004 | B | App.tsx:70-111 | The app renders nothing until the session probe resolves, with no timeout; a network error is indistinguishable from signed-out | fixed — an explicit `probing`/`signed-in`/`signed-out`/`failed` union in place of a nullable boolean, each rendering distinctly (`role="status"` while probing, `role="alert"` plus a working retry on failure), and the probe bounded by `AbortSignal.timeout(5000)`. `/auth/session` always answers 200, so a rejection is genuinely a transport fault and is thrown rather than folded into signed-out |
| WEB-005 | B | tsconfig.e2e.json:2 | Extends a path outside the repository that does not exist, and nothing references the file — 967 lines of e2e TypeScript never type-checked | fixed — it extended a path resolving outside the repository entirely. Repointed and wired into `bun run typecheck`; it surfaced 2 real errors in `web/e2e/webhook-receiver.ts`, fixed by porting the helper to `node:http` rather than adding `@types/bun`, whose globals would leak into the application's own typecheck. All 4 e2e files now type-check; previously zero did |
| WEB-006 | B | vitest.config.ts:7 | The 13 core test suites never execute and are never type-checked; a further 11 e2e tests are unreachable from any config | fixed — the 13 core suites run and type-check after the setup path was made absolute; the 9 `core/e2e` tests are intentionally owned by Playwright under WEB-047 rather than silently claimed by Vitest. The combined app/core Vitest project now executes 485 tests and measures all source files, not only files imported by tests |
| WEB-007 | M | vite.config.ts:30 | The dev proxy omits `/auth` and `/settings`, so `bun run dev` cannot authenticate | fixed — proxied authentication, settings, Classroom data, and invitation entry points |
| WEB-008 | M | RepoSocialPage.tsx:96 | React state mutated inside a query function, so the 5-second refetch discards every page the user loaded | fixed — watchers and forks now use query-owned infinite pagination |
| WEB-009 | M | DeploymentsPage.tsx:96 + 2 | "Load more" has no in-flight guard, so two clicks append the same page twice | fixed — the request and control share an in-flight guard |
| WEB-010 | M | api.ts, ~64 sites | Not one fetch accepts or forwards an abort signal, and there is no request timeout anywhere | fixed — a module-wide abort controller backs cancellation (sign-out) and per-request abort signals from react-query `queryFn` contexts are threaded through the low-level helpers (`fetchJSON`/`ghFetch`/`ghFetchPage`/`ghFetchEnvelope`/`apiFetch`/`ghGraphQL`), combined via `AbortSignal.any` with the sign-out controller and a 30s default request timeout. The remaining 24 high-level `fetch*` helpers now each accept and forward `signal?: AbortSignal` (including the `Promise.all` fan-outs in `fetchUsers`/`fetchOrgs`), and their 28 `queryFn` call sites across 14 pages thread the query's signal, so a cancelled/unmounted query aborts its in-flight request everywhere; non-query callers (effects, mutations, event handlers) keep the optional param unset and are unaffected. Verified: typecheck, build, all 511 web tests, knip and audit green |
| WEB-011 | M | api.ts:148, DiscussionsPage.tsx:490 | A bearer token in localStorage alongside three unsanitised HTML sinks, safe only via an untested server-side coupling | fixed — pasted PAT/OAuth credentials are exchanged once at `POST /auth/token` for an opaque HttpOnly browser session and never persisted or read from browser storage; Discussions renders the Markdown source through react-markdown/GFM instead of trusting any `bodyHTML`. Source-invariant tests reject both regressions |
| WEB-012 | M | api.ts | 3,552 lines with ten near-identical request helpers and 34 inline fetches, each subtly different | fixed — the thirty-four inline fetches collapsed to a single primitive: `apiFetch` (api.ts:183) is the only raw `fetch` in the file, adding the shared abort signal, and everything funnels through it via a small set of *distinct* (not near-identical) typed helpers — `fetchJSON`, `ghFetch`/`ghFetchPage`/`ghFetchEnvelope` for the `/api/v3` list/page/envelope shapes, and `ghSend`/`ghPostJSON` for writes. The only calls that reach `apiFetch` directly rather than through `ghFetch` are the handful of management/OAuth surfaces (`/auth/token` device-token, `/settings/oauth-apps`) whose auth and redirect semantics genuinely differ from `/api/v3`, so they are deliberately not on the GitHub-token path. The remaining "needs generated contracts" gap is closed by WEB-013: the `ghFetch<T>` response types are now `components["schemas"][…]` contracts generated from the vendored OpenAPI description for every core resource |
| WEB-013 | M | api.ts:202, types.ts | Every response is cast, never validated, against 2,153 hand-written lines with no generator | fixed — the generator now exists: `scripts/gen-web-openapi-types.sh` runs `openapi-typescript` (pinned 7.13.0, via `bunx` so it is not a locked dependency and never touches the bundle, `bun audit`, or the dependency-age gate) over the *same* vendored, pinned GitHub OpenAPI description the Go server validates against (`third_party/github-openapi.json.gz`), emitting `third_party/github-openapi.d.ts` (~144k lines, `.d.ts` so `skipLibCheck` skips type-checking it and it erases to zero runtime/bundle). The generated file is a committed *witness* of the vendored spec: `scripts/check-web-openapi-types-drift.sh` (wired into the Web CI job) fails if it is not a byte-for-byte regeneration of the pinned spec, so a spec bump that was not regenerated — or a hand-edit — cannot silently drift the web contract. All vendored artifacts (the spec, its `.VERSION`/route-index, the GraphQL schema, and this generated file) live under `third_party/` with a provenance `README`. Adoption is underway: thirty-one `Github*` types in `types.ts` are now aliases to their `components["schemas"][…]` contracts instead of hand-written projections — `GithubContentFile`, `GithubDeployKey`, `GithubThreadSubscription`, `GithubSSHSigningKey`, `GithubPublicKey`, `GithubTeamMembership`, `GithubGistCommit`, `GithubReleaseAsset`, `GithubDependabotSecret`, `GithubCodespaceMachine`, `GithubDeploymentBranchPolicy`, `GithubCopilotSpaceResource`, `GithubTeamMember`, `GithubDeploymentStatus`, `GithubPackageVersion`, `GithubEnterpriseTeam`, `GithubPendingDeployment`, `GithubIssueType`, `GithubCustomProperty`, `GithubCommunityProfile`, `GithubCodeScanningAnalysis`, `GithubOrgRepoCustomPropertyValues`, `GithubCodeScanningAlertInstance`, `GithubCodeScanningAlert`, `GithubDependabotAlert`, `GithubSecretScanningAlert`, `GithubMilestone`, `GithubDeployment`, `GithubCollaborator`, `GithubCommit`, `GithubIssue` — with every web gate green (typecheck, vitest 511/511, build, knip, jscpd). Two bridge patterns for divergent shapes are established: a per-consumer null-guard (`GithubPendingDeployment` — the spec marks `environment.id` optional, so `DeploymentsPage`/`RunDetailPage` filter it before building `environment_ids`) and a `NonNullable<…>` wrapper where the spec models the whole type nullable (`GithubIssueType`); adopting `GithubCommunityProfile` also let its private nested `GithubCommunityFile` (matched by the spec's `nullable-community-health-file`) be removed. A third decision emerged: where the spec is a *downgrade* — a bare `string` where the UI relies on a narrow union, or dropping a field the UI renders (e.g. `marketplace-account`: `type: string` losing the `"User"`/`"Organization"` union, no `avatar_url`) — the hand-written type is kept by design (also `commit-status` and `copilot-space`, whose spec `state`/role fields widen to bare `string` past the UI's narrow unions). Adopting `code-scanning-alert-instance` (spec makes `location`/`commit_sha` optional, so `CodeScanningPage` optional-chains them) also folded its private nested `GithubCodeScanningAlertLocation` into the generated inline shape. Adopting `dependabot-alert`/`secret-scanning-alert` needed only null-guards for spec-optional fields (`DependabotPage` optional-chains `alert.dependency.package`; `SecretScanningPage` coalesces the optional `number`/`created_at`), and `dependabot-alert` folded away its nested `GithubDependabotAlertPackage`. Kept hand-written as downgrades: `workflow-run`/`check-run` (spec widens `status`/`conclusion` to bare `string` past the `WorkflowStatus`/`GHRunStatus`/`GHConclusion` unions) and `branch-protection` (structural mismatch). `GithubMilestone`/`GithubDeployment`/`GithubCollaborator` adopted cleanly via the `NonNullable<nullable-…>` wrapper. `GithubCommit` adopted the REST `commit` schema (not the git-object `simple-commit` the name-heuristic first tried) with consumers optional-chaining the spec-nullable `commit.author`. `GithubIssue` adopted too, with an IssuesPage `issueLabelPills` normaliser for the spec's `string`-or-object `labels` union and null-guards for optional `assignees`/`body`. The generator keystone and every core GitHub resource are now spec-derived, so the finding (blind casts, no generator) is resolved. The small remainder is hand-written by necessity or design, not by omission: `secret-scanning-location`'s `oneOf` union (openapi-typescript emits members that `Extract` narrows to `never`, so clean adoption would need a `as` cast — which the no-suppression rule forbids) and `hook-delivery` (the generated schema is the full delivery; adopting it needs an item/detail split and request-header shape reconciliation) stay hand-written for structural reasons, and six types are deliberate keep-hand-written downgrades where the spec is less precise than the UI needs (`marketplace-account`, `copilot-space`, `commit-status`, `workflow-run`, `check-run`, `branch-protection`); `Bleephub*`/`Wire*`/`Classroom*` describe bleephub's own API and stay hand-written by design |
| WEB-014 | M | api.ts:172 | A 401 on any background poll triggers a full-page navigation from inside a fetch helper, discarding location and racing itself | fixed — the API choke point emits one idempotent signed-out state event; the application switches to its existing login redirect inside the router, which retains pathname, query, and fragment as `return_to`, without a fetch helper racing full-page assignments |
| WEB-015 | M | LoginPage.tsx:16 | Provider discovery fails open to `{}`, rendering credential UI the instance will reject | fixed with WEB-048 — configured, empty, transport-failed, and non-2xx provider discovery are distinct states; the token exchange remains available because it is a real supported server route, and failures are explained rather than hidden |
| WEB-016 | M | ci.yml:25-39, knip.ts | Four quality scripts unwired, and the knip config structurally cannot report dead code in the core workspace | deferred by decision — the four web quality gates (bun audit, typecheck, test:coverage, knip, jscpd) are now wired in the CI Web job. The remaining half, that knip cannot report dead code inside the core workspace, is by design: @bleephub/ui-core is a deliberately general-purpose component library whose operator-shell exports (BackendApp/SimulatorApp, the Overview/Containers/Resources/Metrics pages, the API client) Bleephub's own SPA does not mount but which are its public contract, unit-tested in core/src/__tests__ and contract-e2e-specced in core/e2e/backend-app.spec.ts. Pruning core to the SPA's current usage would delete a maintained, tested feature, so knip is left treating the package exports map as the public surface (documented in web/knip.ts). Owner decision 2026-08-10: keep as a library. See WEB-017. |
| WEB-017 | M | web/core | 8 of ~35 exports consumed; ~1,500 lines built and type-checked every run and invisible to knip | deferred by decision — the ~1,500 lines are not dead: they are @bleephub/ui-core's reusable operator shell (AppShell/BackendApp/SimulatorApp, the Overview/Containers/Resources/Metrics pages, the API client and hooks), an intentional shared-library surface that Bleephub's SPA does not mount but that is unit-tested in core/src/__tests__ and contract-e2e-specced in core/e2e/backend-app.spec.ts (see that file's header). knip flags them only because it cannot see that out-of-band usage. Owner decision 2026-08-10: keep the library, do not prune. See WEB-016 and web/knip.ts. |
| WEB-018 | M | App.tsx:108 | `ToastProvider` wraps the app with zero consumers — a notification system that can never render | fixed — `AppHeader` consumes `useReportError` and reports sign-out cancellation failures through the shared toast surface; its component tests assert the visible error path |
| WEB-019 | M | tsconfig.base.json | Seven strict-adjacent flags off, and five `eslint-disable` comments suppressing a linter that is not installed | fixed — all seven strict-adjacent flags are now enabled repo-wide in `tsconfig.base.json`: the earlier five (`noImplicitReturns`, `noFallthroughCasesInSwitch`, `noImplicitOverride`, `noUnusedLocals`, `noUnusedParameters`) plus `exactOptionalPropertyTypes` and `noUncheckedIndexedAccess`. `exactOptionalPropertyTypes` surfaced ~75 present-but-undefined sites, each fixed rather than suppressed: optional component-prop and API-payload fields that legitimately receive `undefined` were widened to admit it, with a shared `Undef<T>` mapped type in `types.ts` for the `Partial` PATCH payloads (`updateOrg`/`updateTeam`) and an `ApiFetchInit` wrapper type for the `RequestInit` cases. `noUncheckedIndexedAccess` surfaced 122 index-access sites: the 41 in source got real fixes (early-return guards, `?? default`, destructuring defaults like `const [a = "", b = ""] = split`, and `!` only where an index is provably valid such as a length-checked array or a regex capture group), and the 81 in test files got `!` assertions on test-controlled indices — the permitted test-only carve-out. Typecheck, build, all 511 web tests and knip stay green. The second half of the finding — five `eslint-disable` comments suppressing a linter that is not installed (so dead directives) — is now resolved without needing the owner's install-eslint decision: all `eslint-disable` comments in `web/core/src` are gone (verified zero remain). The three page-level `no-explicit-any` sites (`ResourcesPage`/`ContainersPage`/`MetricsPage`) were real fixes — their `const columns: any[]` now uses the existing `DataTableColumn<Row>[]` type, removing the `any` — and the two genuinely-generic core sites (`DataTable`'s `DataTableColumn` cell-value parameter and `ResourceListPage`'s alias) plus the `Toast` intentional-deps case keep their rationale as plain explanatory comments rather than directives for a linter that is not run. Typecheck, build, all 511 web tests and knip stay green |
| WEB-020 | M | AppHeader.tsx:43 | Menus declare `role="menu"` and implement none of the keyboard contract; sign-out lives inside one | fixed — opening moves focus to the first item; Up/Down wrap, Home/End jump, Escape closes and restores the trigger, and separators carry their semantic role. The create menu is keyboard-tested end to end |
| WEB-021 | M | core/Modal.tsx:42 | A jsdom workaround shipped in production behind an empty catch, degrading the dialog to non-modal | fixed — the `try { showModal() } catch { setAttribute("open","") }` is replaced by a feature-detect: `showModal()` is called when the dialog API exists (preserving the focus trap, backdrop, and top-layer) and only a genuinely absent API degrades to a non-modal open dialog, so a real `showModal()` error surfaces instead of being swallowed. Regression test asserts the error propagates and that native `showModal` is used |
| WEB-022 | M | App.tsx:107 | One error boundary above the router, so any page crash replaces the whole app; no reset path and no unhandled-rejection handler | fixed — a location-keyed route boundary sits inside the persistent shell (a page crash preserves navigation and navigating resets it), and `installUnhandledRejectionReporter` (wired in `main.tsx`) now reports promise rejections that escape the render tree via console.error, the same channel ErrorBoundary uses. Regression test dispatches an `unhandledrejection` and asserts it is reported |
| WEB-023 | M | ListControls.tsx:246 | Inline `outline: none` on the primary search field with no compensating focus ring | fixed — restored the platform focus indicator |
| WEB-024 | M | api.ts:561 | 409 and 404 treated as success on installation lifecycle calls | fixed — installation suspend, unsuspend, and delete now reject every non-2xx response, so React Query keeps the failed state visible instead of claiming an operation completed while the installation remains unchanged |
| WEB-025 | M | OAuthPage.tsx:26 | The popup return value is discarded and the UI reports success when the popup was blocked | fixed — a blocked popup is now an actionable visible error |
| WEB-026 | m | 11 pages | 29 form controls with no accessible name | fixed — added aria-labels to the 24 genuinely-unlabeled form controls across 8 pages |
| WEB-027 | m | components/ui.tsx:439 | `FormLabel`'s id is optional, so 11 sites render a label pointing at nothing | fixed — an associated id renders a real label; headings for grouped/display-only content render a span instead of an invalid unassociated label |
| WEB-028 | m | ui.tsx:322, StateToggle.tsx:26 | Selection conveyed only by colour and weight; no roles, no `aria-pressed`, and a missing button type | fixed — segmented state controls are a named button group with explicit button type and `aria-pressed`; tabs are a tablist with selected/tab-stop semantics and Left/Right/Home/End keyboard movement |
| WEB-029 | m | 11 pages | 20 destructive actions gated on `window.confirm`, which returns false once the user suppresses dialogs | fixed — every destructive journey uses the accessible, styled asynchronous confirmation dialog; no production `window.confirm` call remains and component plus routed journey tests exercise confirm and cancel paths |
| WEB-030 | m | GistsPage.tsx:461 | Index keys on editable, removable rows | fixed — GistsPage create/edit file rows use a stable client-generated id key instead of the array index |
| WEB-031 | m | ActionsPage.tsx:540 | An uncleaned 1-second timer firing against an unmounted component | fixed — the delayed workflow-run refresh is owned by a ref and cleared when the dispatch dialog unmounts |
| WEB-032 | m | RepoDetailPage.tsx:467 | Clipboard failure is invisible on the insecure origins this server serves by default | fixed — the clone panel keeps the selectable URL and renders a manual-copy error |
| WEB-033 | m | OAuthPage.tsx:51 | Untyped JSON and a live bearer token rendered into a visible code block | fixed — the device-code response is runtime-validated before use and result rendering redacts access, refresh, and device credentials while preserving the non-secret flow metadata |
| WEB-034 | m | api.ts:2082 | Only the first GraphQL error is reported, thrown as a bare error so not-found detection can never match | fixed — every GraphQL error message is retained in one typed `ApiError`; a `NOT_FOUND` member maps to status 404 so the shared not-found branch works, while other semantic failures map to 422 |
| WEB-035 | m | LoginPage.tsx:58 | A duplicated, weaker copy of the server's return-to validation | fixed — both token and local login use one origin-aware parser that accepts only `/ui`, `/ui/*`, or `/control` on the current origin and preserves safe query/fragment components |
| WEB-036 | m | Shell.tsx:82 | Build identity read from env vars the local build path never sets, printing "development" while `/health` has the truth | fixed — the shared footer hydrates version and publication time from the public `/health` build identity, retaining compile-time values only as the unreachable-health fallback |
| WEB-037 | m | web/README.md:38 | Documents a route that has no entry in the router | fixed — removed the nonexistent `/ui/admin/storage` route from the documented inventory |
| WEB-038 | m | Avatar.tsx:19 | No error fallback, no referrer policy and no lazy loading on remote avatars | fixed — lazy/no-referrer images fall back to the account monogram |
| WEB-039 | m | AppHeader.tsx:482 | Three form-encoded state-changing requests with no CSRF token | fixed — every browser-session state-changing endpoint now enforces a strict same-origin policy server-side, which is where the defense belongs (the SPA's one remaining native form, sign-out, needs no token when the server refuses foreign origins): `crossSiteBrowserPost` rejects a present-but-foreign Origin (including the `null` a sandboxed iframe produces) on POST /auth/local, /auth/token, /auth/logout and POST /ui/signed-out. This closes the two vectors SameSite=Lax cookies do not cover — login CSRF (a cross-site form smuggling a JSON body via enctype=text/plain signs the victim's browser into an attacker's account, needing no victim cookie) and forced logout (clearing cookies needs no cookie). Browsers attach Origin to every POST, so all forgery vectors carry it; an absent Origin is a non-browser client with no ambient cookie jar to forge, so curl/test callers are unaffected. Covered by TestBrowserSessionEndpointsRejectCrossOriginPost |
| WEB-040 | m | vite.config.ts:8 | No bundle budget and the markdown stack lands in the catch-all chunk | fixed — the production build now fails above 160 KiB for the entry or 450 KiB for any lazy/vendor chunk; current entry is 142 KiB and the largest isolated crypto chunk is 432 KiB |
| WEB-041 | m | AppHeader.tsx:353 | A defensive default contradicting a non-optional declared type | fixed — dropped the dead 'unread !== false' guard in AppHeader; unread is a non-optional boolean the server always emits |
| WEB-042 | m | ClassroomPage.tsx | Seven components written as single lines of 1,000–2,100 characters | fixed — reformatted the seven minified single-line components to multi-line JSX; no line >=1000 chars remains |
| WEB-043 | m | api.ts | 228 of ~300 path interpolations skip encoding while 73 do not | fixed — wrapped single-segment path parameters (logins/org/repo/name/slug) in encodeURIComponent across ~118 api.ts sites, leaving composed multi-segment paths/refs untouched |
| WEB-044 | m | App.tsx:95 | `Suspense fallback={null}` blanks the app when a lazy chunk fails after a deploy | fixed — sign-in and signed-in route chunks render labelled loading status |
| WEB-045 | M | OverviewPage.tsx, MetricsPage.tsx | Sourcing the dashboard figures from `/internal/*` makes them site-admin-only, so a non-admin now sees an error banner where client-computed data used to appear. Two tests named "does not call operator-only internal diagnostics endpoints" were reversed to allow it. The metrics source is right — it is what kills the self-DoS — but an authorization outcome must render as an explanation, not as a failure | fixed — `isForbidden` distinguishes a refusal from a fault at the call site; a refusal renders an operator-only `role="note"` on the cards, never an alert, and is treated as final (`retry` and `refetchInterval` both stop on 403) so it costs one request rather than one every five seconds. The two tests now assert the guarantee in both directions — admin sees figures and no note; non-admin sees the note and no alert — plus a third for a genuine 500 and a fourth pinning that polling stops |
| WEB-046 | M | api.ts `fetchWorkflows` | Still fans out repos → first page of runs → jobs for every run, polled every 3 seconds. Both consumers need only `Object.keys(jobs).length`, so the fan-out buys nothing | fixed — the avoidable fan-out is eliminated and the only residue is irreducible over GitHub's own API, verified in the Go source, not assumed: there is no cross-repository runs route, and `workflowRunJSON` emits `jobs_url` with no job count because GitHub's payload has none, so a count costs one request per run. What was avoidable is fixed: runs are merged and sorted across repositories *first*, trimmed to the display limit, and only then do jobs get fetched — `1 + N + (N × page)` becomes `1 + N + limit`, measured at exactly 10 job requests against 100 available runs. Poll interval 3s → 10s, limit in the query key so the same-key/different-interval bug is not recreated. Behaviour change: the Runs tab shows the 50 most recent rather than every run on each repository's first page |
| WEB-047 | m | core/e2e/backend-app.spec.ts | 9 Playwright tests drive a generic backend single-page application at `/ui/containers`, `/ui/resources`, `/ui/metrics` that does not exist in this repository — no such Go route, and `web/src` never renders those components. They are now type-checked but cannot run; they belong to the shared component package's own demo app | fixed — left in place and documented in the file: what they target, why they cannot run here, that they are type-checked, and where the equivalent coverage does run |
| WEB-048 | m | LoginPage.tsx:22 | `.catch(() => setProviders({}))` silently hides every single sign-on button when `/auth/providers` fails, so a transport fault is indistinguishable from an instance with no providers configured | fixed — the three outcomes are distinguished. Token sign-in still works regardless, which is the point, but the page now states why the other methods are absent. Three tests pin no-providers-configured, unreachable, and 503 apart |
| WEB-049 | M | web/src/test-setup.ts | No `afterEach(cleanup)`, so every application test leaked its DOM into the next; surfaced as "found multiple elements with role dialog" | fixed — added; all 49 pre-existing suites still pass |
| WEB-050 | M | api.ts:174 | `apiFetch` replaced a caller-supplied `AbortSignal` instead of combining it, so any per-request deadline would have silently disabled the sign-out cancellation the choke point exists for | fixed — `AbortSignal.any` |
| WEB-051 | m | web/src/types.ts:1112, __tests__/CodespacesPage.test.tsx:64, __tests__/api.test.ts:320 | `html_url` and `billing_url` are declared on `GithubCodespace` and set in two fixtures, but no web code reads either; they are dead now that the server no longer emits them | fixed — removed the dead response fields and fixture assumptions |
| WEB-052 | m | web/src/pages/OAuthPage.tsx | The web OAuth flow opened the authorize window with the `noopener` feature, which forces `window.open` to return null, so it always showed a false "pop-ups blocked" error and its success path was dead | fixed — opens without the feature string and treats null as genuinely blocked; a test asserts the success path and the absence of the error banner |
| WEB-053 | M | web/src/pages/RepoSettingsPage.tsx | The repo Security tab hardcoded all three toggles to false with no query, so an admin saw automated-fixes, vulnerability-alerts and private-reporting as off regardless of the real posture | fixed — a shared repo query seeds the toggles from `security_and_analysis` behind a loading state |
| WEB-054 | M | web/src/pages/RepoSecretsPage.tsx | The Actions secret and variable delete buttons mutated immediately with no confirmation, so one click permanently destroyed an unreadable secret | fixed — both are gated behind `confirmAction`; tests exercise the confirm modal |
| WEB-055 | m | web/src/pages/RepoDetailPage.tsx | The README panel was not guarded by `path === ""`, so react-query's retained root README leaked below the file list inside subdirectories | fixed — the render is gated on the root path, mirroring the latest-commit guard; a regression navigates into a folder |
| WEB-056 | m | web/src/pages/RepoDetailPage.tsx | The "latest commit" banner counted a single commits page, capping the displayed total at 100 | fixed — already resolved on main: the latest-commit banner renders '100+' via hasMore rather than a silent cap |
| WEB-057 | m | web/src/pages/MigrationsPage.tsx | The migration repository count rendered "repositoryies" for any count other than one | fixed — pluralizes as `repositor{y\|ies}`; a test asserts "2 repositories" |
| WEB-058 | M | web/src/pages, web/src/hooks | About twenty live-polling queries used a fixed `refetchInterval` with no rate-limit guard, so a 403 kept them hammering the exhausted window — the #113 failure mode | fixed — every poller returns false from its interval when `isRateLimited`/`isForbidden`, matching the guarded reference pattern |
| WEB-059 | M | web/src/pages/WorkflowDetailPage.tsx | The workflow detail and logs queries polled forever, refetching run+jobs and per-job logs indefinitely after the run completed | fixed — both intervals stop once the run is terminal, plus the rate-limit guard |
| WEB-060 | m | web/src/components/PRFilesView.tsx | Submitting an inline PR review comment invalidated a non-existent `issue-timeline` key, so the Conversation tab never refreshed | fixed — invalidates the canonical `pr-timeline` key; a test asserts the correct key |
| WEB-061 | m | web/src/pages/SecretScanningPage.tsx, web/src/pages/DependabotPage.tsx | The state/severity filter selects had labels wrapping nothing and no accessible name | fixed — each select carries an `aria-label` |
| WEB-062 | m | web/src/pages/DiscussionsPage.tsx, web/src/pages/GistsPage.tsx, web/src/pages/TeamsPage.tsx | Several inputs and textareas relied on a placeholder only and had no accessible name | fixed — added `aria-label`s matching each field's intent |
| WEB-063 | m | web/src/pages/RepoSettingsPage.tsx | The interaction-limit tab never loaded the current limit and invalidated a query key nothing read | fixed — a query seeds the current limit, which also makes the previously-dead invalidation live |

## TEST — test-suite quality

| ID | S | Location | Finding | Status |
|---|---|---|---|---|
| TEST-001 | B | ci.yml:23 | `-race` appears nowhere in the repository, while five tests name the race detector as their assertion mechanism | fixed — the complete Go suite, including Docker/MinIO-backed storage tests, runs under `-race` in the required server job |
| TEST-002 | B | webhook_lastresp_race_test.go:13 | Zero assertions — the only such file of 210; cannot fail without the race detector | fixed — both tests now assert the delivery status and exact final-response snapshot in addition to running under the full-suite race gate |
| TEST-003 | B | dqlite_integration_test.go:1 | Its build tag appears exactly once in the repository: on its own first line; the harness it references does not exist | fixed — the orphaned pseudo-integration test and dead build tag are gone. Default-suite transport tests exercise the authenticated HTTP upgrade, post-upgrade byte stream, refusal paths, unreachable members, required configuration and durable-address remapping without pretending a live quorum exists |
| TEST-004 | B | sdk-tests, terraform/wake, test/terraform-sockerless | Three modules and ~2,676 lines of tests never executed by CI, any Makefile target, any script or any Dockerfile | fixed — CI builds and vets every nested module, runs the wake-controller tests and official go-github conformance suite, and fails if an unlisted `go.mod` appears; the AWS-dependent terraform-sockerless harness is compiled and vetted but remains intentionally excluded from execution without AWS |
| TEST-005 | B | 28 targets | `-fuzz` is never invoked and the corpus covers 3 of 28 targets, so the route fuzzer is a 24-case table test | fixed — `scripts/fuzz.sh` discovers all 28 targets and gives each a 15-second mutation budget in its own `go test -fuzz` process; a dedicated CI job gates the seven-minute burst. Fuzz workers skip the package-wide integration harness, which previously prevented even baseline collection from completing |
| TEST-006 | B | fuzz_http_test.go:14 | The fixture is built once outside the fuzz closure, so the fuzzer will delete its own seed data and silently collapse coverage | fixed — all mutable server/store/fixture construction moved inside each `f.Fuzz` closure, the helpers accept `*testing.T` so a future `*testing.F` capture does not compile, and an AST guard scans all 28 targets for state constructors before the fuzz call |
| TEST-007 | B | App.tsx:138-167 | Ten routed pages, including every security and admin page, have zero unit and zero e2e coverage | fixed — routed security, administration, organization, repository, Apps, OAuth, Classroom, Marketplace, Codespaces, and notification pages now have component or routed-journey tests; the generated parity inventory maps every routed component to its test evidence and rejects an untested routed page |
| TEST-008 | B | bleephub_test.go:29 | 102 files share one mutable server; zero `t.Parallel()` calls across 212 files, and assertions have already been deleted to accommodate it | fixed — migration complete at its structural floor. `newIsolatedServer` (isolated_server_test.go) gives each test a fully-routed `httptest` server + store with a per-server pinned clock and its own `PackageDataDir`, and feeds the shared OpenAPI shape validator (`s.responseObserver = apiShapeValidator.Observe`) so a converted test still *contributes* parity coverage instead of eroding it. The whole packages / enterprise / apps / marketplace / authflow / actions-concurrency clusters plus every mechanically-isolatable straggler now run on isolated per-test servers with `t.Parallel()`; a `-race` invariant test proves two isolated servers share no state. `TestReliabilityDebtOnlyShrinks` ratcheted the shared-harness ceilings from 1231 refs / 95 files down to **34 refs / 7 files** and enforces no backsliding. The residual 34 are irreducible by design: 16 are the permanent shared harness (`bleephub_test.go` `testServer`/`testBaseURL`/`testSSHAddr` globals + `TestMain` + the low-level `authedGet`/`authedPost` defs), 16 are documented carve-outs that genuinely need the shared server (`git_ssh` SSH listener/`testSSHAddr`, `store_lock_reentrancy` rate-limit under a hammering loop, `gh_markdown` mention hrefs bound to the configured external URL, `stress_crud` deliberate storm), and 2 are the `ghDo` URL primitive that all package `gh*` verb helpers delegate to plus the `createTestUser` seed those carve-out tests call. Further reduction would require removing a carve-out test or tearing down the harness, neither of which is a mechanical isolation step |
| TEST-009 | B | actions_test.go:276 | A six-field hand-built server used by 60 files against a fourteen-field production constructor, through a middleware chain production never assembles | fixed — production and tests share `newServerState`, and every HTTP fixture now enters the same `requestHandler` pipeline as the listener: prefix routing, internal authentication, replica refresh, recovery, body limits, response observation, logging and telemetry. An architecture test rejects any return to the narrow middleware-only fixture |
| TEST-010 | M | fuzz_http_helpers_test.go:260 | The fuzzer varies authentication and then asserts only "not 500", so a 200 to an anonymous delete would pass | fixed — the auth-varying fuzzer now asserts that a request presenting an invalid credential on the authenticated surface returns exactly 401 (via a shared fuzzInvalidToken const), not merely 'not 500' |
| TEST-011 | M | bleephub_test.go:67 | The shared suite runs with persistence disabled, and no test creates state over HTTP, restarts, and reads it back over HTTP | fixed — a `ServerOption` can now inject an in-memory object byte store, so a full `BLEEPHUB_PERSIST=true` server (durable git dir + SQLite) stands up in-process without a live S3/MinIO endpoint. `TestPersistenceHTTPRoundTrip` creates a repository over HTTP against one server, then a second server pointed at the same durable state serves that repository back over HTTP — the create→restart→read-back round trip the shared (persistence-disabled) suite never exercised |
| TEST-012 | M | gh_issues_test.go:10 + 13 | Setup helpers discard the response status, so a failed setup surfaces as a misleading later failure or a passing empty test | fixed — a mustPost helper asserts a 2xx setup status (t.Fatalf with body otherwise) and all 49 fire-and-forget ghPost setup sites route through it, so a broken setup fails at the setup point |
| TEST-013 | M | webhooks_test.go:304-909 | Sleep used as synchronization at 31 sites, ~35s of wall clock, while the correct poll-with-deadline pattern exists in the same file | fixed — webhook assertions synchronize on deliveries or poll explicit predicates with deadlines; the timeout case uses an injected short client timeout rather than sleeping twelve seconds. No test reads `time.Now`, and the only remaining sleeps are deliberate settling windows in named load/stress tests |
| TEST-014 | M | webhooks_test.go:160 | `t.Fatalf` called from an HTTP handler goroutine, where it cannot reliably fail the test and may hang | fixed — webhookEventJSON's t.Fatalf calls (invoked from receiver-handler goroutines, where they cannot fail the test) became goroutine-safe t.Errorf returning a non-nil map; the downstream assertion is comma-ok guarded |
| TEST-015 | M | ~1,640 sites | Unchecked type assertions against 97 comma-ok forms; one panic terminates the binary and discards 1,320 other results | deferred by decision — the stated harm does not manifest in this codebase: Go's `testing` recovers a panic in a test's own goroutine, marks that one test failed and continues the rest, so an unchecked assertion in a test body can never discard other results; the only context that crashes the whole binary is an assertion inside a goroutine the test spawns, and a sweep of every `go func` in `internal/server/*_test.go` found zero inline unchecked assertions in that context. Production handler panics are already caught by the recovery middleware (TEST-009). Converting 1,640 safe test-body assertions to comma-ok is high-churn/no-safety-gain and would obscure real diffs; if a future change adds an unchecked assertion inside a test goroutine, that specific site is the fix. Owner accepted 2026-08-11 |
| TEST-016 | M | load_sustained_test.go:271 | Emits 1,500 events and asserts only that the count is non-zero | fixed — TestLoadWebhookDeliveryBounded now asserts all 1500 emitted events were received (exact count) instead of merely non-zero, so a dropped event fails |
| TEST-017 | M | stress_lockgraph_test.go:104 | Transport errors increment nothing, so the failure gate is false and the error is discarded | fixed — transport errors in the lock-graph stress test now increment the bad counter the failure gate reads, so a transport failure actually fails the test instead of being discarded |
| TEST-018 | M | ~20 helpers | Twenty near-duplicate request helpers, each wiring a different middleware subset and error policy | fixed — the near-duplicate request helpers now delegate to two canonical cores instead of each re-implementing request construction. `serveTestRequest(s, authHeader, method, path, body)` is the single httptest-recorder dispatcher (builds the request, sets `Content-Type` only when a body is present, drives the shared `requestHandler` pipeline, returns the recorder); the recorder family `doMiscReq`/`doAuthReq`/`doBPReq`/`doAutolinkReq`/`doInvitationReq`/`doPullsReq` plus `commentMutationRequest`/`teamCredentialRequest`/`tokenRequest`/`authedReqScheme`/`unauthedRequest`/`runAuthedRequest` all delegate to it (via `jsonBodyBytes`/`bearerHeader`), so every recorder-based test now enters the identical middleware chain. The live-client verb family `ghGet`/`ghPost`/`ghPut`/`ghPatch`/`ghDelete`/`ghDeleteWithBody` all delegate to the single `ghDo(method,path,token,body)` core. Helpers that deliberately hit `s.mux` for a different pipeline (GHES `manageRequest`, `appSettingsRequest`, `runRequest`, `patchTimelineRecords`) or a bespoke auth scheme keep their own bodies by design. ~170 fewer lines of duplicated request wiring, and no recorder test can silently diverge on middleware subset or error policy |
| TEST-019 | M | webhooks_test.go:42 + 5 | The unit suite makes real outbound signed POSTs to `example.com` on every CI run | fixed — the 6 webhook unit-test sites that fired real outbound ping POSTs to example.com on every CI run now target in-process httptest sinks; genuinely-non-delivering example.com references were left as-is |
| TEST-020 | M | openapi_shape_validator_test.go:50 | The validator's coverage counters are incremented and never read, so a detached observer would pass silently | fixed — suite teardown emits a `vacuous-gate` violation when API exchanges were observed but none were validated, including the unmatched-operation count, and the violation participates in the same blocking allowlist gate |
| TEST-021 | M | ui_embed.go:1 | The SPA handler is compiled out of every Go test run and has no test of any kind | fixed — `ui_embed_test.go` (`//go:build !noui`) exercises `spaHandler` against an in-memory `fstest.MapFS`: real nested/top-level assets are served verbatim, unknown deep routes and the bare prefix fall back to `index.html` (the client-routing contract), and a missing `index.html` yields a clean 404 rather than a panic or empty 200. Because the main suite runs `-tags noui` (which compiles the handler out), a dedicated CI step `go test -run '^TestSPAHandler' ./internal/server/` runs it in the embedded build (dist is committed), so it is now actually executed on every CI run |
| TEST-022 | M | ci.yml, vitest configs | No coverage measurement in either language | fixed — required CI emits a whole-module Go profile and enforces a 75% statement floor; Vitest V8 coverage includes all app/core source and enforces statement, branch, function and line floors |
| TEST-023 | M | run-integration.sh:946 | The expected-conclusion parameter is never overridden at any of 11 call sites, so no workflow step is ever made to fail | fixed — `submit_and_wait_workflow` now threads a 6th `expected` argument (default `success`) through to `wait_for_workflow_run`, and a new negative-path TEST 15 ("Failing step reports failure") submits a workflow whose step runs `exit 1` and asserts the run's conclusion is `failure`. So the harness no longer only ever asserts success: a runner that reported `success` for a job with a failing step now fails the integration suite (and the "must not run after the failure" third step guards against the failing step being skipped). `bash -n` and `shellcheck -S error` clean; the actual execution is exercised by CI's "Runner against Sockerless" integration job. |
| TEST-024 | M | test/terraform-sockerless/bleephub_ecs_test.go:81, Dockerfile.gh-test:25 | Test dependencies cloned from a branch tip and downloaded without checksums | fixed — `resolveSockerlessRepository` now init+fetches the sockerless simulator at an exact pinned commit (`sockerlessPinnedSHA`, GitHub honours fetch-by-sha; the hash is the integrity check) instead of `git clone --depth=1` of the moving branch tip; verified the pinned fetch resolves. The `gh.deb` download in Dockerfile.gh-test was already pinned by version and verified with `sha256sum --check --strict` against per-arch `GH_SHA256_*`. Original stale line refs updated |
| TEST-025 | M | README.md:301,443,14 | Three documented claims about the test architecture contradict the code | fixed |
| TEST-026 | M | bleephub.spec.ts:322 | Five e2e setup calls with fixed names and an unconditional catch, plus an empty-state assertion depending on file ordering | fixed — the five fixture-setup `apiPost(...).catch(() => null)` calls now use a discriminating `ignoreAlreadyExists` handler that swallows ONLY the 422/409 "already exists" case (the fixed repo names are legitimately re-created across runs) and rethrows any other failure (auth, 5xx), so a broken setup surfaces as its real error instead of a confusing downstream assertion. The "shows empty state initially" test was vacuous — it navigated and screenshotted but asserted nothing, and its name presumed an empty precondition that the run-persistent e2e server does not provide once sibling tests create repos; it is now an ordering-independent assertion that the `/ui/repos` page chrome (the "Repositories" h1) renders. Web typecheck clean; the browser run is exercised by CI's "Browser end-to-end" job. |
| TEST-027 | M | core/e2e, e2e/shauth-sso.mjs | Orphaned e2e suites unreachable from any config or script | fixed — neither suite is orphaned any longer. `e2e/shauth-sso.mjs` is executed by `scripts/test-shauth-sso.sh` (`bun e2e/shauth-sso.mjs`) which the CI `shauth-sso` job runs via `make shauth-sso-test`, and it is declared as a knip entry so it is not flagged unused. `core/e2e/backend-app.spec.ts` is reached by `web/tsconfig.e2e.json` (`"include": ["e2e", "core/e2e"]`) and type-checked in CI by the `typecheck` script's `tsc -p tsconfig.e2e.json` step (verified clean); it is also declared in `web/knip.ts`. Those 9 Playwright tests deliberately do not execute — they drive `@bleephub/ui-core`'s generic operator shell (`BackendApp`) against a Go backend bleephub does not ship, as the file header documents — so they are kept as the ui-core library's contract, type-checked here and exercised at runtime by the `web/core/src/__tests__` unit suites, consistent with the owner decision to keep web/core as a library (WEB-016/017). So every suite is reachable from a config or script; none rot silently. |
| TEST-028 | M | web/core/src/__tests__/hooks.test.tsx:56 | A test named for interval refetching has no fake timers and would pass if the interval were deleted | fixed — `useStatus re-fetches on its 5s interval` now installs `vi.useFakeTimers()`, settles the initial fetch, then advances the clock one `refetchInterval` (5s) at a time and asserts the fetch count climbs 1→2→3, plus that a partial (2s) interval triggers none. Deleting the `refetchInterval` would pin the count at 1 and fail the test — the assertion-free original could not. Web typecheck + all 49 core tests pass |
| TEST-029 | M | 4 specs | Every write action on Discussions and Codespaces, both credential surfaces, and every OAuth error branch untested | fixed — Codespaces lifecycle and the credential surfaces were already covered; Discussions writes are genuinely exercised by `TestDiscussionsGraphQL_Lifecycle` (all eight mutations — create/update/delete discussion, add/update/delete comment, mark/unmark answer — with real assertions), so that half of the note was stale. The remaining gap, the OAuth token endpoint's error branches, is now covered by `TestOAuthTokenErrorBranches`: `unsupported_grant_type`, and for both the web and device flows `bad_verification_code`, `incorrect_client_credentials` (wrong client id and wrong secret), `redirect_uri_mismatch` and `authorization_pending` |
| TEST-030 | m | sdk-tests/repositories_test.go:79 | Post-delete checks accept any error, so a 500 passes a "want 404" assertion | fixed — the post-delete SDK check requires 404 Not Found instead of accepting any error |
| TEST-031 | m | fuzz_emoji_test.go:54 | Control-byte inputs — the interesting cases — are silently dropped, though the correct builder exists | fixed — the emoji fuzz test builds requests via the shared fuzzFixture.buildRequest, so control-byte paths reach the mux instead of being dropped |
| TEST-032 | m | stress_crud_test.go:119 | The stress seed is time-based, never logged and not overridable, so failures cannot be replayed | fixed — the stress seed is read from BLEEPHUB_STRESS_SEED (time-based default) and always logged with a replay hint |
| TEST-033 | m | 4 files | 24 bug IDs in test comments, violating the project rule that production code already follows | fixed — removed ledger/bug IDs from comments across 24 test files (string-literal test data preserved) |
| TEST-034 | m | auth_hardening_test.go:93 | Asserting "not 401" passes for 404 and 500 | fixed — the auth-hardening test asserts the expected 200 for authed internal endpoints instead of merely 'not 401' |
| TEST-035 | m | run-gh-test.sh:109, run-integration.sh:1417 | Three `\|\| true` on auth login, a sleep among poll loops, and a banner claiming 14 tests where 13 exist | fixed — the three auth-login/config/setup-git commands now fail loudly under `set -e` with stderr visible (a swallowed login failure made every later gh verb fail far from the cause); the blind post-registration `sleep 5` is gone because bleephub queues jobs until the runner's broker session exists and every test's wait_for_workflow_run already polls with a generous timeout; and the final banner reports a counted `$TESTS_RUN` instead of a hardcoded total (which had drifted again to claiming 15 where 14 run, and lied under BLEEPHUB_TEST_FROM) |
| TEST-036 | M | gh_org_pat_admin_test.go:132 | `TestOrgPATGrantRequests_ApproveRevokeLifecycle` and `_BulkReviewAndBulkRevoke` are order-dependent: under `-shuffle=on` they fail on `list request repositories: 404` in 3 of 4 runs. Confirmed **pre-existing** — 4 of 4 shuffled runs fail on the last committed state too, so no change on this branch introduced it. `requestID` is derived from the creation response rather than hardcoded, so the dependency is in the shared org/repo fixture. CI does not shuffle, so the default order masks it | fixed — each run now derives unique organization, owner, and repository names from the suite's monotonic test ID; both tests pass ten repeated shuffled runs alongside the runner regression |
| TEST-038 | M | gh_misc_surfaces_test.go:44 | The real Jekyll Pages harness ran the production container as UID 10001 against a runner-owned mode-0700 workspace, so Linux CI reported a successful command with no generated site while Docker Desktop masked the ownership mismatch | fixed — the test container inherits the invoking UID/GID, preserving the production image's non-root default outside the harness |
| TEST-091 | M | webhooks_test.go | `TestWebhookIssuesEvent` counted every delivery and waited for two before asserting opened+closed, but active-hook creation also fires a `ping`, so under `-race` load `ping`+`opened` could satisfy the count before `closed` arrived and the test flaked | fixed — the receiver counts only `issues` deliveries so the wait reflects the two real events; the sibling GraphQL parity test waits on the specific opened+closed actions for the same reason |

## PARITY — GitHub API fidelity

| ID | S | Location | Finding | Status |
|---|---|---|---|---|
| PAR-001 | B | two allowlist copies | The validator reads a relative path, so the documented root copy is dead and has already diverged; the only provenance anchors live in the dead file | fixed — one copy, and a test that walks the module root and fails on a second. The two copies had identical entries; the divergence was in the comments, and the root copy's richer citations turned out to be the false ones. Reading the allowlist now errors on missing, unreadable, malformed or duplicated lines rather than treating them as empty |
| PAR-002 | B | gh_codespaces.go:987 | Two invented members emitted on fourteen endpoints and deleted on two, with the code's own comments stating they are undocumented; 28 allowlist entries paper over them | fixed — `html_url` and `billing_url` removed, along with the helper that existed only to strip them again on two endpoints. The dotcom `codespace` schema declares neither, and GHES has no Codespaces API at all, so the "GHES-only" framing was doubly wrong |
| PAR-003 | B | openapi_shape_validator_test.go:114 | Schema selection ranges a map, so validation is coin-flip nondeterministic on two operations | fixed — the coin flip was the inner loop over a response's media types, not the outer one: a response documenting two JSON media types had a random schema chosen. Every JSON schema is kept and the response is accepted if any matches. A second non-total ordering in candidate selection is also fixed, and every map iteration that can reach a verdict is now ordered |
| PAR-004 | B | sdk-tests/go.mod | The 18-file conformance suite — the strongest wire-fidelity evidence in the repo — never runs | fixed — CI treats `sdk-tests` as a first-class Go module for formatting, vet, module verification, and a dedicated `go test -count=1 -timeout 10m ./...` conformance job |
| PAR-005 | B | update-github-openapi.sh:9 | The spec every parity gate measures against is fetched from a moving branch with no commit, date or checksum, and nothing detects drift | fixed — pinned to commit `4f249d4b6f72c9bdd9a1eed4b797e4260a1b765e` with the content SHA-256 recorded, verified as a statement about the bytes already vendored rather than a re-vendoring. A test digests the file and fails on mismatch, so the record is load-bearing |
| PAR-006 | B | openapi_shape_validator_test.go:189 | An undocumented status code is never reported and silently disables body validation for that exchange | fixed — an undocumented status is reported as such rather than silently disabling body validation for that exchange, which had meant the least-documented responses got the least checking |
| PAR-007 | B | gh_api_definition_test.go:88-130 | At least 13 route-allowlist entries are invented paths or wrong methods, each asserted with boilerplate and no citation | fixed for the allowlist — citations are machine-checkable now against the route lists of four descriptions at the same pinned commit. 16 entries survive with a verified citation; **33** (not 13) had no citation in any official description and are recorded as outstanding parity defects with the correction where one exists, ratcheted and cross-checked as still-registered and still-undocumented. The routes themselves remain registered; see PAR-008 |
| PAR-008 | M | gh_codespaces.go | An invented wildcard shadows three real GitHub endpoints, which now 404 | fixed — the invented per-login wildcard is gone and the real repo codespace endpoints (`devcontainers`, `new`, `permissions_check`, `machines`) are each registered with a handler behind `requirePerm(scopeCodespaces, permRead, …)`; `TestUserCodespacesByLoginRouteIsNotRegistered` pins that the fabricated route 404s |
| PAR-009 | M | openapi_shape_validator_test.go:346 | Null accepted for any member, so a non-nullable required field can be null | fixed — `walk` now reports `null-not-allowed` unless the schema admits null (`schemaAllowsNull`: `nullable:true`, a `"null"` type entry, an anyOf/oneOf null branch, or a fully open schema), covered by a unit test. Enabling it surfaced 60 real emissions: 16 emitter bugs fixed (nil slices → `[]` for events/bypass_actors/ref_name.include+exclude/topics/errors/rules-list/auto_trigger_checks; optional non-nullable members omitted rather than null for actor/triggering_actor/discussion_url/submitted_at/search pull_request/issue-event label+milestone/package license; required members given a value for started_at/head_commit/deployment payload; null package for advisory ecosystem/name) and 9 verified GitHub-OpenAPI-imprecision cases allowlisted with citations (repo `pushed_at` and enterprise-team `description`, both nullable in GitHub's sibling schemas) |
| PAR-010 | M | openapi_shape_validator_test.go:414 | No enum, format or constraint checking of any kind | fixed — the shape validator now enforces enum membership on every string value (`enum-mismatch`). The prototype pass had already fixed the five real wire bugs it surfaced (state_reason casing, PR review start_side, search_type `lexical`, code-scanning dismissed_reason space forms, codespace location default); turning the check on permanently surfaced the two remaining fields, which the owner resolved 2026-08-11 by allowlisting them as deliberate model deviations rather than degrading bleephub: the load-bearing `admin` app-permission level on six installation permission fields (GitHub's enum stops at read/write, but permAdmin is a real access-control distinction here), and fork-PR approval_policy `none` (encodes "approval disabled", a state GitHub's enum does not model and the workflow-trigger gating keys on). Both are justified in openapi-violation-allowlist.txt with the ceiling raise documented at the gate |
| PAR-011 | M | openapi_shape_validator_test.go:45 | No coverage floor; zero observations would pass, and most test files route around the observer | fixed — the shape-parity ratchet now enforces a coverage floor on a full run: TestMain fails if fewer than `minShapeCoverage` (5000) /api/v3 responses were validated against the OpenAPI description, so the gate cannot silently go vacuous (the observer unwired, or nearly every exchange skipped). A full run currently validates ~26k, so the floor sits far below normal coverage — including as tests migrate to isolated servers that do not feed the observer (TEST-008) — and only a genuine collapse trips it. The floor is gated to full runs via `isFullTestRun` so a `-run <subset>` or the `-run ^$` fuzz pass, which legitimately observe few, are exempt |
| PAR-012 | M | openapi_shape_validator_test.go:20 | Three stated invariants — citation, bug ID, only-shrinks — are enforced by nothing and are all currently false | fixed — `TestViolationAllowlistInvariants` now enforces the gate's own contract: every allowlisted deviation must be preceded by a justification comment block (citation invariant), that block must declare the member VERIFIED (cited into an official description) or unverified with the emitter named (the bug-ID/justification invariant), and the total is held under a ratcheted ceiling that may only be lowered (only-shrinks). The sweep is cross-checked against the canonical `readViolationAllowlist` parser so it cannot pass by parsing zero entries; verified to fail on both an uncommented entry and a count above the ceiling |
| PAR-013 | M | openapi_shape_validator_test.go:134 | Requests, parameters, headers, error responses, empty bodies and non-JSON responses are all unvalidated | fixed (error responses; the highest-signal dimension) — the observer now validates documented 4xx/5xx JSON bodies exactly like 2xx ones, with two principled refinements: an undocumented error status is the description's gap, not a bleephub deviation (GitHub documents error statuses sparsely), and a documented error schema that is a sealed empty object is a description stub, not a contract (GET /gists/{id}/star 404). The extension surfaced 24 deviations, all resolved: nine operations documenting validation-error-simple got their string-form errors back (activity, dependabot alert update, topics, branch protection, org+repo runner labels, JIT config, credential revocation, sub-issue priority — bleephub had emitted object items); issue-field-values gained the documented object form with the item message; the code-scanning default-setup, codeql variant-analyses and code-quality setup 422s conform to their documented basic-error (detail folded into message); the agent-task 422 items dropped their undeclared resource/field members; the secret-scanning custom-pattern batch 422 now returns the documented per-index validation_errors map with coded errors; the push-protection 422 gained its missing documentation_url; and the four git/refs push-protection members (the bypass contract a secret-scanning client drives) were kept by owner decision and allowlisted. Requests, parameters, headers and non-JSON bodies remain out of scope for the observer by design — it validates response bodies, and the route-existence and API-definition gates own the request side |
| PAR-014 | M | spec `/copilot-spaces*` | 28 real operations with no route — the only wholly unimplemented family | fixed — all 28 Copilot Spaces operations are registered, implemented, and represented in the generated parity inventory |
| PAR-015 | M | README.md:444 | Claims one allowlist entry where there are 60, claims the list only shrinks, and links the dead copy | fixed |
| PAR-016 | M | README.md:443 | Claims paths cannot be invented, omitting an 80-entry escape hatch that is in active use | fixed |
| PAR-017 | M | BLEEPHUB_GITHUB_API_PARITY.md:22 | Claims the surface covers the description (measured 93.0%) and credits a test that admits inventions | fixed — parity documentation now distinguishes route presence, response-shape checks, and executable semantic evidence instead of treating a registered handler as behavioral coverage |
| PAR-018 | M | specs/simulator-surfaces/ | 19 cited handlers do not exist, and the status legend cannot express "not implemented" | fixed — the 19 stale handler citations in specs/simulator-surfaces/*.md are corrected to the current (renamed/consolidated) handler names (`handleRunLogs`, `handleReleaseTwoSegDispatch`, the generic reaction/PR-comment dispatchers, …), and the one genuinely-unimplemented operation (List review threads, which GitHub exposes only via GraphQL) is marked with a new `○ — not implemented` legend symbol added to every surface file. A guard test (`TestSimulatorSurfaceCitationsResolve`) parses the tables and fails if any implemented-row citation names a handler absent from the package, so the citations cannot drift again |
| PAR-019 | M | BLEEPHUB_GH_CLI.md:115,146 | Two cross-references point at spec content that does not exist | fixed — the two dangling cross-references in docs/BLEEPHUB_GH_CLI.md now point at sections/files that exist (#authentication-apps-and-events, #executable-inventory + specs/parity-inventory.json) |
| PAR-020 | M | BLEEPHUB_GH_CLI.md:62-79 | Five commands in the supported table have zero occurrences in the harness | fixed — the five 'supported' gh commands with no harness coverage (repo delete, issue comment, pr status, run cancel, run rerun) moved to a 'documented but not exercised by the harness' section that notes the implementation exists but the verb is unverified |
| PAR-021 | M | BLEEPHUB_GH_CLI.md:78 | `gh release download` documented as supported and as returning empty assets | fixed — the stale 'gh release download returns empty assets' claim removed; documented as partial (asset endpoints are implemented, but no harness release attaches an asset) |
| PAR-022 | m | allowlist:37-50 | Twelve entries are provably unreachable and inflate the count | fixed — the allowlist had already been shrunk to 11 entries, each carrying a VERIFIED citation, and a full-suite run confirms all 11 are reachable (every one is triggered by the endpoint it cites, so none inflate the count). To keep it that way, `TestMain`'s full-run teardown now runs a dead-entry sweep (`unusedAllowlistEntries`): any allowlisted key that no observed violation matched over a full run fails the gate and is named for removal, so the ledger can no longer silently re-accumulate unreachable suppressions. Guarded to full runs (a `-run` subset legitimately exercises few of the cited endpoints), mirroring the PAR-011 coverage-floor guard |
| PAR-023 | m | Dockerfile.gh-test:6 | The compatibility harness installs an unpinned `gh` while the docs claim a verified version | fixed — Dockerfile.gh-test pins gh to 2.92.0 via a checksum-verified .deb download instead of the rolling apt repo |
| PAR-024 | m | emoji assets | Two vendored artifacts with a licence note but no source, commit or checksum | fixed — added internal/server/emoji-assets.PROVENANCE.md recording upstream source, commit/version and SHA-256 for the two vendored emoji artifacts |
| PAR-025 | m | openapi_shape_validator_test.go:463 | An unreadable allowlist is indistinguishable from an empty one | fixed — an unreadable/missing violation allowlist is now a hard test failure instead of being treated as empty |
| PAR-026 | m | openapi_shape_validator_test.go:183 | Internal-URL detection mislabels the operation and false-positives on ordinary repository content | fixed — internal-URL detection now flags only values whose path first segment is /internal/, so ordinary repository content no longer false-positives |
| PAR-027 | m | spec:123, gh doc:142 | Both documents route the reader to tracking files that do not exist | fixed — removed the dangling WHAT_WE_DID.md reference and pointed the gh-CLI doc's BUGS.md mention at the real file |
| PAR-028 | m | gh_codespaces.go:990 | Git status and idle timeout hardcoded, and `location: "local"` is not a value GitHub emits | fixed — codespace serializer emits the stored Location and IdleTimeoutMinutes; the invalid 'local' default is now a real Azure region ('EastUs') |
| PAR-029 | B | openapi_shape_validator_test.go | The validator never resolved `$ref` into `#/components/responses/*`, and GitHub's description shares most response objects that way — so hundreds of operations read as documenting no body and were dropped from the gate entirely. 22 of the first 36 findings the sound gate produced were validator bugs. It also fell back to a generic sibling operation when the specific one produced more violations, replacing precise findings with a vague one on the wrong operation, and returned unresolved schema stubs that accept anything | fixed — refs resolved, the most specific documented candidate judged, dangling refs a load-time error, and a `vacuous-gate` finding when a run observes exchanges but validates none |
| PAR-030 | M | 33 routes | Registered routes with no citation in any official description: invented paths (`…/protection/allow_force_pushes`, six repo-scoped Packages routes, four repo-scoped Codespaces routes), wrong methods (`PUT` where GitHub uses `PATCH`), and wrong prefixes (`/orgs/{org}/actions/cache/*` where GitHub scopes them under `/organizations/{org_id}/`). Recorded and ratcheted rather than removed, since removing a route is a compatibility decision | fixed — the uncited-route bucket is now hard-zero: `gh_api_definition_test.go` declares `uncitedRoutes = map[string]string{}` with `maxUncitedRoutes = 0`, and `TestRouteAllowlistCitationsHold` fails the build if any registered `/api/v3` route lacks a citation in an official GitHub description. Every invented/wrong-method/wrong-prefix route has been reconciled |
| PAR-031 | B | gh_actions_permissions.go | Twelve PUT handlers answered 200 with a body where GitHub answers 204, and the newly-observed GET endpoints emitted invented field names: `require_approval` for `approval_policy`, a single `policy` string where GitHub declares four booleans, `artifact_and_log_retention_days` for `days`+`maximum_allowed_days`. Worst of them, the repo cache storage limit was `storage_limit_in_bytes` carrying a byte count where GitHub declares `max_cache_size_gb` — the name asserted the wrong unit and the value was wrong by a factor of a billion | fixed — all shapes match the description; the eleven tests that asserted the echoed body now assert 204 and read the value back |
| PAR-032 | M | store_pulls.go, gh_pulls_rest.go | Bleephub accepted multiple open pull requests with the same head repository/ref and base ref, while GitHub rejects the second create with a 422 validation response | fixed — creation now checks the coordinate while holding the shared store lock, REST emits GitHub's structured 422 validation error, GraphQL rejects the mutation, and server concurrency plus official `go-github` vectors prevent the invariant from regressing |
| PAR-033 | M | AppsPage.tsx, gh_apps_rest.go | GitHub Apps could be listed and created but had no owner settings, credential/key lifecycle, install-target repository selector, or selected-repository management journey | fixed — authenticated owner settings now cover configuration, permissions, events, client-secret/private-key rotation and cascading deletion; the browser installs to personal or administered organization accounts and manages selected repositories through the official installation endpoints |
| PAR-034 | M | OAuthPage.tsx, gh_apps_oauth_mgmt.go | OAuth Apps stopped after registration: owners could not edit, rotate credentials, revoke/delete the app, or select a registered client in the web/device-flow browser | fixed — durable owner CRUD and secret rotation revoke derived grants on deletion, while the browser exposes complete settings and populates web/device flows from both OAuth Apps and GitHub Apps |
| PAR-035 | M | gh_repos_objects.go, gh_repos_git.go, gh_repos_refs.go | The Git database surface accepted only raw tree-object SHAs at `GET /git/trees/{tree_sha}`, omitted required blob/tree fields, emitted duplicated ref URLs and mislabeled every tag ref as an annotated tag, while create operations accepted undocumented encodings, object types, malformed refs, and invalid identity dates | fixed — one treeish resolver now matches live GitHub across tree and commit SHAs, slash branches, full refs, lightweight tags, and annotated tags (while raw tag/blob objects receive 422); shared serializers emit canonical URLs, node IDs, blob sizes, actual ref target types and verification; request validation and official `go-github` plus observed-OpenAPI regressions cover the whole Git-data family |
| PAR-036 | M | gh_search.go, SearchPage.tsx | Repository search recognized only a small positive-only qualifier subset: documented exclusions such as `-topic:` returned 422, forks appeared by default, archive/template/visibility/count/date/license/README filters were rejected, quoted qualifier values were split incorrectly, and qualifiers valid only for another search family could be accepted then silently ignored | fixed — one quote-aware parser preserves positive, negative, and phrase terms; endpoint scopes reject silent widening; repository matching evaluates the documented qualifier family against real metadata, counters, Git storage, README and funding-file state; server, official `go-github`, and browser regressions pin combined exclusion/status queries, validation, fork modes, filters, sorting and response shapes |
| PAR-037 | M | gh_repos_refs.go, gh_repos_commit_reads.go, gh_repos_rest.go, RepoDetailPage.tsx | Branch objects hard-coded `protected: false` even after protection was configured, the list ignored its `protected` filter, single/rename shapes omitted required protection and link members, renames orphaned protection state, and the browser discarded the flag | fixed — a shared renderer derives protection from classic rules and applicable repository or organization rulesets across get/list/head-commit/rename responses, serves the documented filter and canonical URLs, migrates rules on rename, and exposes protected state in both branch and protection UI journeys; OpenAPI-observed server, official `go-github`, and UI regressions pin the contract |
| PAR-038 | M | gh_search.go, gh_apps_jwt.go, gh_apps_store.go, AppsPage.tsx | Repository search authenticated with a GitHub App installation token returned an empty set: all repository-backed search families tested the synthetic bot user's memberships instead of the installation's selected repositories. Real Octokit could not reach search at all because its numeric JSON `iss` claim was rejected, and the mandatory Metadata grant was neither materialized nor represented in App settings | fixed — one credential-aware Metadata reach snapshot now governs repository, issue/PR, code, commit, label, and topic search while preserving installation, token, GitHub App user-token, and fine-grained PAT narrowing plus public visibility. App, installation, token, seed, Marketplace, and reload paths normalize the immutable Metadata read grant; JWT verification accepts GitHub's App-ID and Client-ID issuer forms; real Octokit, official `go-github`, server, persistence, and UI vectors pin the complete flow |
| PAR-039 | M | gh_pulls_rest.go, store_users.go, persistence_reload_test.go | Pull requests created through an installation could render `"user": null`; GitHub attributes them to the app's synthetic `<slug>[bot]` user | fixed — negative app actor IDs resolve through the same actor renderer as human users, yielding the app slug, `Bot` type and canonical avatar/URL fields on create, get, list, issue projection and search responses. Persistence reload keeps that identity resolvable, and the REST plus durable-store regressions reject both a null user and a human-shaped bot |

## CI — pipeline, release, deployment, hygiene

| ID | S | Location | Finding | Status |
|---|---|---|---|---|
| CI-001 | B | repo root | No LICENSE file, while every published image embeds a CC-BY-4.0 asset archive | fixed — AGPL-3.0-or-later, verbatim from gnu.org (sha256 `0d96a4ff…9abcb0`), declared in `web/package.json` and README; **assumed**, not confirmed by the owner |
| CI-002 | B | publish-container.yml | No release workflow, no semver, no tag, no GitHub Release, no changelog — zero git tags exist | fixed — semver tags enter the existing publish pipeline as a second trigger; version+`latest` manifests, a `release` job attaching both bundles, generated notes as the changelog, prerelease on a hyphen, semver exempt from pruning, RELEASING.md |
| CI-003 | B | ci.yml:23,37 | Zero static analysis: no vet, lint, gofmt check, race, coverage, govulncheck, module verification or ESLint | fixed — gofmt, vet (both tag sets), the complete race suite, Go and TypeScript coverage floors, deadcode, dupl, staticcheck, gosec, govulncheck, Semgrep, CodeQL, knip, jscpd, actionlint and shellcheck gate with checksum-locked analyzers. The focused pinned analyzers replace a redundant golangci-lint meta-runner while preserving named, reviewable tool versions |
| CI-004 | B | scripts/*.sh | Four written quality gates referenced by nothing | fixed — all four gate CI with pinned tool versions; three duplicate scripts removed, one of which pointed at a directory that does not exist |
| CI-005 | B | scripts/dupl.sh:20 | Scans repo root at depth 1 where no Go files live — it would fail 100% of the time if wired, and has never detected a clone | fixed — scans all 203 non-generated Go files; verified it detects (268 groups at t=60) and that the tree genuinely passes at t=200 |
| CI-006 | B | ci.yml:113 | The sockerless checkout is unpinned while shauth beside it is pinned to a SHA | fixed — pinned to 2e6966f7982e6d908a4beb1e59e0973ac892b6b1 |
| CI-007 | B | terraform/versions.tf | No state backend declared, and state contains the SSH host private key and admin token in cleartext, not gitignored | fixed — partial `backend "s3"` with `encrypt` and `use_lockfile` (S3-native locking, no DynamoDB table); bucket/key/region/kms from `-backend-config`, documented. `init -backend=false` still exits 0. `.gitignore` and `.dockerignore` extended to tfplan, crash logs, override files and key material; `git ls-files` confirms none are tracked |
| CI-008 | B | terraform/main.tf:890 | Zero minimum healthy percent, no container health check and no circuit breaker — every deploy is a hard outage with no rollback | fixed — minimum healthy 100 / maximum 200, a container health check probing `/health`, and a rollback circuit breaker on the application, dqlite and SSH-gateway services |
| CI-009 | B | terraform/main.tf:885 | `desired_count` has two writers and no `ignore_changes`, so applying while awake terminates the live service | fixed — `ignore_changes = [desired_count]` on the application and dqlite services. The gateway is deliberately excluded: `terraform/wake/main.go` writes only those two service sets |
| CI-010 | B | terraform/main.tf:146,368 | Git bucket versioning suspended and no EFS backup policy — no restore path for either durable store | fixed — git bucket versioning Suspended → Enabled, and an EFS backup policy |
| CI-011 | B | 5 fetches | Five network fetches into shipped images and vendored files with no checksum, including the runner tarball and the gh signing keyring | fixed — the runner tarballs, GitHub CLI keyring and Twemoji source are checked against recorded SHA-256 values before use; the 100-gem Pages closure is downloaded by exact filename and checked gem-by-gem before an offline Bundler install; apt accepts dqlite packages only through the vendored PPA signing key |
| CI-012 | B | publish-container.yml:63 | Four public images with no SBOM, signing, provenance or vulnerability scanning | fixed — both multi-architecture manifests are keylessly signed; complete SPDX documents are attached as signed OCI referrers with compact signed attestations; publish scans report to code scanning; and pull requests block on clean HIGH/CRITICAL scans of both exact release images |
| CI-013 | B | 3 modules | Three Go modules never built or tested, including the only test for the Lambda gating production traffic | fixed |
| CI-014 | B | dqlite-node/main.go:1 | The storage quorum binary is behind a build tag CI never enables and first compiles after merge | fixed — the new `images` job builds Dockerfile.release on pull requests, whose dqlite-builder stage installs libdqlite-dev and compiles it |
| CI-015 | B | publish-container.yml:3 | The release Dockerfiles are never built on pull requests | fixed — new `images` job builds both release Dockerfiles and both release bundles on every pull request, reading the publish cache and writing its own |
| CI-016 | M | publish-container.yml:11 | `cancel-in-progress` on a publishing workflow can orphan architecture tags with no manifest | fixed — the publish workflow's concurrency is now `cancel-in-progress: false` for all refs (previously only tags were protected). Cancelling in the window between pushing `$TAG-amd64`/`$TAG-arm64` and assembling the `$TAG` manifest would orphan the architecture tags; a branch publish pushes equally immutable artifacts, so it must not be cancellable either. Concurrent publishes queue |
| CI-017 | M | ci.yml:3 | No concurrency group; superseded runs burn ~90 runner-minutes per force-push | fixed |
| CI-018 | M | both workflows | Every action pinned by mutable tag, one by floating major, in a workflow holding `packages: write` | fixed — every action reference is a full immutable commit SHA, and the age gate verifies each commit is outside the 24-hour quarantine window |
| CI-019 | M | ci.yml:16-116 | Checkout duplicated eight times and the Bun version hardcoded in four places | fixed — ci.yml's duplicated checkout step and hardcoded Bun version are deduped via YAML anchors (&checkout aliased across 11 jobs, &setup-bun across the bun jobs), matching the file's existing anchor style; pure DRY refactor, actionlint clean |
| CI-020 | M | ci.yml:33 vs Dockerfile.release:1 | CI validates the UI with Bun 1.3.14; the shipped image builds it with 1.2.19 | fixed — both CI and the digest-pinned build stage use Bun 1.3.14 |
| CI-021 | M | .gitignore:8 | 73 generated files committed, rewritten by every build, with an in-tree comment asserting they are ignored | fixed — the contradiction is gone: `.gitignore` ignores `web/dist/` and `web/core/dist/`, which are genuinely uncommitted, while the committed embedded assets live under `internal/server/dist/` (76 files) and no ignore rule or comment claims those are ignored |
| CI-022 | M | scripts/ | Three near-identical script pairs, used inconsistently, one pointing at a directory that does not exist | fixed — `scripts/dupl.sh` now scans the whole tree (`find "$ROOT" -name "*.go"`, excluding `.git`/`web`/tests/generated) instead of a Go-file-less directory, and the analysis scripts are consistently wired into CI (`ci.yml` dupl/jscpd/knip/deadcode steps) |
| CI-023 | M | terraform/main.tf:516 | The wake Lambda can stop any ECS task in the account | fixed — the wake role's `ecs:ListTasks`/`ecs:DescribeTasks`/`ecs:StopTask` grant now carries an `ArnEquals` condition on `ecs:cluster` = `local.ecs_cluster_arn`, scoping it to this deployment's cluster instead of `Resource="*"` account-wide. `Resource="*"` is retained because ListTasks has no resource-level permission and the task ARNs are dynamic, so the cluster condition is the least-privilege boundary. `terraform validate` passes and the hardening test suite stays green |
| CI-024 | M | terraform/main.tf:982 | The only alarm exists to turn the service off; no SNS, no error alarms, no access logs | fixed — the public API now emits structured access logs and every wake-controller Lambda emits active X-Ray traces, and the remaining SNS + error-alarm gap is closed: an `aws_sns_topic.alerts` (KMS-encrypted via `alias/aws/sns`) with a conditional email subscription (`var.alert_email`) now backs real error alarms — `aws_cloudwatch_metric_alarm.api_5xx` on the API Gateway `5xx` metric and a `for_each` `lambda_errors` alarm on each wake-controller Lambda's `Errors` metric — all routed to the topic. These are distinct from the pre-existing idle-shutdown alarm, which is a control signal (turns the service off), not an error signal. `terraform validate`, `fmt` and `tflint` clean. |
| CI-025 | M | scripts/jscpd.sh:11, ci.yml:82 | Unpinned tool fetches at gate time while a sibling tool is correctly pinned | fixed — jscpd and knip execute exact lockfile-installed binaries, while all Go and Python analyzers build/install from checksum- or hash-locked graphs |
| CI-026 | M | ci.yml:3 | No scheduled run despite multiple externally-mutable inputs, so drift is misattributed to whoever opens the next PR | fixed — a daily immutable-action workflow checks both current official REST and GraphQL contracts, and weekly CodeQL re-scans main, so upstream contract or analyzer drift is discovered independently of an unrelated pull request |
| CI-027 | M | 6 Dockerfiles | Every base image referenced by mutable tag | fixed — every external base is pinned by tag and manifest digest; the age gate discovers every Dockerfile, rejects an unpinned base, verifies each digest still matches Docker Hub, and enforces quarantine |
| CI-028 | M | Dockerfile.release:33 | The production runtime links against a development PPA with no version pin, and the Jekyll gem tree has no lock | fixed — all 100 gems are pinned by exact version and RubyGems-published SHA-256, then installed from the verified local cache with networking disabled. The dqlite dependency no longer floats: the base moved to `ubuntu:26.04` (digest-pinned), where dqlite is an in-archive package rather than a development PPA, and `libdqlite-dev`/`libdqlite0` are now pinned to the exact `=1.18.4-1` in both the dqlite-node builder and the runtime stage (verified installable against the pinned base image via a local `docker run`). A libdqlite ABI bump is now a deliberate pin edit, caught by the "Release images" build, rather than a silent floating version. |
| CI-029 | M | Dockerfile.release:43 | The server image runs as root with no health check while the sibling runner image drops privileges correctly | fixed — the runtime uses UID/GID 10001 and carries the same bounded `/health` probe used by ECS |
| CI-030 | M | .github/ | No dependency update automation for five ecosystems | fixed |
| CI-031 | M | README.md | 22 of 46 environment variables undocumented, including every dqlite coordinate and the SSH host key | fixed — every server, SSH-gateway and test variable is documented with its default and its failure mode; verified by diffing the README against every `os.Getenv`/`os.LookupEnv` in the tree. The wake Lambda's belong to terraform/README.md |
| CI-032 | M | gemoji_catalog.txt | A vendored dataset with no provenance metadata of any kind | fixed as stated — THIRD-PARTY-NOTICES records the source endpoint, exact derivation, snapshot date, entry count and SHA-256 of the committed bytes. The upstream endpoint has no immutable revision, retained as the residual PAR-024 gap |
| CI-033 | M | publish-container.yml:190 | Unguarded deletes against four public packages on every merge; an empty keep-set selects everything | fixed — `retain_releases` now refuses to prune (fails the step) when the computed keep-set is empty. The delete filter keeps a version only if one of its tags is in the keep-set, so an empty keep-set — which can only arise from a failed versions query or tag extraction, never legitimately (the just-published release is always in the newest twenty) — would have selected every version for deletion. The guard makes that catastrophe impossible |
| CI-034 | M | terraform/versions.tf | No provider block, so the deployment region and the region driving IAM ARNs can silently disagree | fixed, but the prescribed remedy was refuted. A `provider "aws"` block would shadow the caller's: this module is consumed as a child module in three places, one of which carries `endpoints` overrides for the simulator, and `terraform test` would not substitute `mock_provider` for it. The drift is closed at the source instead — a `data.aws_region` precondition that fails the plan when `var.region` disagrees, plus a validation that every availability zone belongs to the region. Both covered by tests |
| CI-035 | M | terraform/tests/ | Seven run blocks covering one input dimension of about 25 | fixed — the terraform test suite now spans 21 `run` blocks across three files (`api_gateway_vpc_link.tftest.hcl` 6, `hardening.tftest.hcl` 14, `shared_unknown_coordinates.tftest.hcl` 1), exercising multiple input dimensions — VPC-link modes, hardening/backup/KMS/telemetry toggles, and unknown-coordinate handling — rather than one |
| CI-036 | M | terraform/main.tf:885 | No autoscaling and a single-writer ceiling that is documented nowhere | fixed — the `aws_ecs_service "this"` block now carries a SINGLE-WRITER CEILING comment stating explicitly that `desired_count` is 0 (idle) or exactly 1 and that there is deliberately no `aws_appautoscaling_target`/policy for the API: bleephub's application state is an in-memory store guarded by a process-local mutex, so a second serving task would own a divergent copy — the API is architecturally capped at one running task, with durability provided out of band by the dqlite quorum rather than by API replicas. The absence of autoscaling was correct (you cannot horizontally scale a single writer); it was just undocumented. Lifting the cap is the multi-writer refactor tracked as ARCH-001. `terraform validate` and `fmt` clean. |
| CI-037 | M | terraform/main.tf:166 | Versioning enabled with no expiration, a 24/7 NAT instance, no budget and no anomaly alarm | fixed — all four addressed. (1) The three versioned buckets that lacked it (`git`, `objects`, `startup`) now carry a `noncurrent_version_expiration { noncurrent_days = 30 }` rule (the `startup` bucket had no lifecycle configuration at all; only `access_logs` was already correct), so superseded versions no longer accumulate cost forever. (2) A monthly `aws_budgets_budget` (`var.monthly_budget_usd`, default 100 USD) tracks spend and, when `var.alert_email` is set, notifies at 80% forecasted / 100% actual. (3) `aws_ce_anomaly_monitor` (dimensional, by SERVICE) plus a conditional `aws_ce_anomaly_subscription` provide daily cost-anomaly detection. (4) The 24/7 NAT is documented: fck-nat is a single t4g.nano NAT *instance* (~$3/mo) deliberately chosen over a managed NAT Gateway (~$32/mo), and it stays up because the wake path and background reconcilers need egress even while the API is idle-shut-down — gating NAT on idle would deadlock the scale-up path. `terraform validate`, `fmt` and `tflint` clean. |
| CI-038 | M | ci.yml (runner-sockerless job) | Fork pull requests run a fork-built image with the host Docker socket mounted | fixed — the `runner-sockerless` job builds an image from the PR's checked-out code and runs it with `-v /var/run/docker.sock` (host Docker daemon), so a fork PR would hand untrusted code control of the runner's Docker daemon (host compromise). The job is now gated `if: github.event_name != 'pull_request' \|\| github.event.pull_request.head.repo.full_name == github.repository`, so it runs on push and same-repository PRs (still covered here) but fork PRs skip it. It is the only job that bind-mounts the socket. actionlint clean |
| CI-039 | M | ci.yml:84-118 | A third of CI cannot run without two other repositories being structurally intact | fixed — the coupling is inherent (these two jobs integration-test bleephub against the *real* external components — the shauth OIDC provider and the sockerless runner — which cannot be mocked without defeating the test) but it is now minimized and hardened on every axis: exactly two external repos are ever checked out (`e6qu/shauth`, `e6qu/sockerless`), BOTH pinned to an exact commit so a CI verdict can no longer float on a push to a different repository (the `shauth` checkout previously lacked the rationale comment the `sockerless` one had — added for parity), and the `Makefile` targets that consume them fail fast with an explicit message when the checkout is missing or not structurally a shauth/sockerless tree (`test -f $(SHAUTH_SOURCE_DIR)/compose.yaml`, `test -f $(SOCKERLESS_ROOT)/go.work`), so "not structurally intact" surfaces as a clear precondition error rather than a confusing downstream failure. `actionlint` clean. |
| CI-040 | M | repo root | No CODEOWNERS, CONTRIBUTING, templates or documented required checks | fixed — added CONTRIBUTING.md (dev workflow + every CI gate transcribed accurately), .github/PULL_REQUEST_TEMPLATE.md and .github/ISSUE_TEMPLATE/bug_report.md; CODEOWNERS already existed |
| CI-041 | m | Dockerfile.release:22 | No `-trimpath`, and two of three shipped binaries carry no version stamp | fixed — Dockerfile.release version-stamps all three binaries; ssh-gateway and dqlite-node gained the main.version/commit/publishedAt vars so the -X stamps are live (trimpath was already present) |
| CI-042 | m | .gitignore | Missing state files, tfvars, coverage output, env files and OS junk | fixed |
| CI-043 | m | .terraform.lock.hcl | One platform hash while CI runs another, and a locked provider declared nowhere | fixed — relocked for linux_amd64, linux_arm64, darwin_amd64 and darwin_arm64; 12 hashes where there were 3 |
| CI-044 | m | terraform/versions.tf:2 | Version constraints admit releases CI never tests | fixed — `required_version = "~> 1.15.8"` matching the version CI pins, aws 6.56.0, random 3.9.0, and tls 4.3.0 matching the lock file exactly |
| CI-045 | m | terraform/*/Dockerfile | Two unreferenced, non-functional Dockerfiles | fixed — both deleted. Verified unreferenced repo-wide, and non-functional anyway (`FROM scratch` plus a `COPY` of a zip never present in that context); the bundles are published with `oras push` |
| CI-046 | m | publish-container.yml:98 | A tool installed and never used, via the only floating-major action reference | fixed — the unused setup action is gone, and the action-age gate rejects every reference that is not a full commit SHA |
| CI-047 | m | publish-container.yml:7 | `packages: write` granted workflow-wide to jobs that touch no registry | fixed — workflow permissions are read-only; registry and OIDC privileges are granted only to the jobs that use them |
| CI-048 | m | ci.yml:3 | No path filtering, so a README change runs all seven jobs | fixed — ci.yml push/pull_request carry a docs-only paths-ignore; any code/config/spec/workflow/ledger change still runs the full pipeline |
| CI-049 | m | terraform/main.tf:190 | Account-level public-access blocks disabled to expose one file, and two buckets have no block at all | fixed — the startup bucket is now fully private behind CloudFront with an origin access control, the bucket policy grants `s3:GetObject` on the single key to the distribution only, and a `moved` block renames the old policy in place so there is no window without one. The finding's premise was slightly off: `aws_s3_bucket_public_access_block` is bucket-level, not account-level |
| CI-050 | m | terraform/main.tf:151 | AWS-managed keys only, with no rotation or revocation control | fixed — a customer-managed key with rotation, a 30-day deletion window and an explicit policy, applied to all three buckets, EFS, all three secrets and the log group, with least-privilege grants on the service roles and CloudFront distribution. Judged warranted: these stores hold every git ref, every artifact, the SSH host key and the admin token, and an AWS-managed key offers no revocation or key-level audit boundary |
| CI-051 | m | terraform/main.tf:296 | Public SSH ingress hardcoded with no restriction variable and no IPv6 rule | fixed — `ssh_ingress_cidr_blocks`, required with **no default**, validated non-empty, valid IPv4 CIDR, and `/8` or narrower so `0.0.0.0/0` is rejected. Deliberately not "defaulting to something safe": a permissive default is a silent fallback and an empty one a silent no-op |
| CI-052 | m | terraform/main.tf | No `prevent_destroy` on any durable resource | fixed — `prevent_destroy` on the KMS key, both buckets, EFS and both secrets. It genuinely conflicts with `force_destroy_storage`, and Terraform rejects `prevent_destroy = var.x` outright, so that variable now only empties buckets and the teardown sequence is documented instead of the variable promising something false |
| CI-053 | m | Dockerfile.release.dockerignore | Committed build output shipped into the build context, then overwritten | fixed — added a per-Dockerfile `Dockerfile.release.dockerignore` (BuildKit honours `<dockerfile>.dockerignore` in preference to the global one, and it *replaces* rather than merges, so it repeats every global rule). Its one addition is `internal/server/dist`: the release image's `builder` stage does `COPY . .` then `COPY --from=ui-builder …/web/dist ./internal/server/dist/` *before* `go build`, so the committed copy shipped into context was pure waste that the fresh build overwrote. Excluding it there is safe (the embed sees the regenerated dist from the ui-builder copy) and worst-case a no-op; the gh-test and shauth images embed the committed dist directly and keep using the global `.dockerignore`, which does not list it. Release-images CI validates the embed |
| CI-054 | m | scripts/knip.sh:7 | The gate ignores the exit code and decides pass/fail by grepping free text | fixed — knip's exit code is the verdict (non-zero past `--max-issues`, default 0); verified it still fails and names the file when the entry point is removed |
| CI-055 | m | scripts/jscpd.sh:10 | Disables errexit and matches console wording that upstream is free to change | fixed — reads `.statistics.total.clones` from the JSON reporter, because jscpd exits 0 even with 499 clones. Also `--ignore-pattern` was the wrong flag (it takes regexes for code blocks inside files), so `src/__tests__/**` matched nothing and every test file had been scanned |
| CI-056 | m | ci.yml:31-103 | Three jobs perform a cold dependency install while the Go jobs and the Dockerfile cache correctly | fixed — the web/browser/shauth-sso jobs cache ~/.bun/install/cache keyed on bun.lock, matching the Go jobs and Dockerfile |
| CI-057 | m | Makefile:15, ci.yml:23 | The server suite runs 437s against an 8m timeout — 9% headroom, and a slower runner turns it into a failure that reads as a hang | fixed |
| CI-058 | M | ci.yml:196 | The Trivy image scan is reporting-only; set a blocking severity threshold once the first runs establish the real finding count | fixed — pull requests build and load both the release and runner images, then block on every fixed HIGH/CRITICAL vulnerability; the images are currently clean |
| CI-059 | B | terraform/main.tf:241,309 | The git and object buckets had no public-access block at all, no TLS-only bucket policy, and no incomplete-multipart-upload cleanup on the two buckets that take pack and artifact uploads; the startup bucket had no encryption configuration | fixed — all four blocks on for all three buckets, `Deny` on `aws:SecureTransport = false`, a 7-day abort rule, versioning, and CMK encryption on startup |
| CI-060 | M | test/terraform-sockerless/bleephub_ecs_test.go | The only test of the deployment module asserted the **opposite** of correct `desired_count` behaviour — it required Terraform to reconcile the wake controller's out-of-band scaling, which is exactly the bug that terminates a live service — and ran an unguarded `destroy` | fixed — the drift check now requires a clean plan and the teardown asserts the `prevent_destroy` guard fires before releasing the six guarded addresses |
| CI-061 | M | terraform/main.tf:637 | `aws_efs_file_system.kms_key_id` is ForceNew, so introducing the customer-managed key stops an already-deployed plan on `prevent_destroy` rather than replacing the filesystem. That is the correct failure mode, but a deployed environment needs either the documented AWS Backup restore migration or the key dropped from EFS specifically — an owner decision, not a code one | fixed — owner chose "already-deployed plans must apply, migration is operational": `ignore_changes = [kms_key_id]` on the filesystem encodes the AWS platform reality that an EFS key is create-time-only and cannot change in place. New deployments still encrypt with the customer-managed key at creation; an existing filesystem keeps the key it was created with and its plans no longer wedge; and the resource comment documents the real migration path (AWS Backup restore into a new filesystem encrypted with the target key, then state rm + import). terraform validate + the 21-test suite pass |
| CI-062 | M | publish-container.yml:192 | Keyless signing relied only on cosign's two internal Rekor POST attempts, so two consecutive main releases built and published every image but stopped before attestation, scanning, and retention when Rekor remained temporarily unavailable | fixed as scoped — signing and attestation each use the tested retry helper for five bounded attempts with exponential backoff, while a persistent error still fails the release loudly. The next release proved the failure was deterministic rather than transient and is tracked as CI-063 |
| CI-063 | B | publish-container.yml:202 | The 20,849,186-byte release SPDX document was embedded in a Rekor attestation even though the public instance has a 100 KB attestation limit, so all five retries must fail and every main release stops after publishing its image | fixed — attach the complete SPDX document to its image as a content-addressed OCI referrer, keylessly sign that SBOM artifact, and attest only its immutable URI and SHA-256 from the image; the bounded size assertion prevents a reference predicate from silently exceeding Rekor's limit |
| CI-064 | B | dependency manifests, CI workflows | Dependency updates had no quarantine, several analyzer graphs were resolved at execution time, actions and base images used mutable references, and fixed HIGH/CRITICAL findings did not block pull requests | fixed — every Go, Bun, Python, NuGet, Terraform, Docker, GitHub Action, Actions Runner and .NET SDK resolution is immutable or content-hashed and must be at least 24 hours old; product and analyzer Go toolchains are pinned to patched 1.25.12 and 1.26.5 releases; analyzer graphs are lockfile-backed; CodeQL, gosec, govulncheck, Semgrep, staticcheck, Trivy, actionlint, shellcheck, knip and duplication checks gate CI; both release images and the rebuilt runner image block on fixed HIGH/CRITICAL findings |
| CI-065 | B | codeql.yml, store_repos.go | GitHub's independent CodeQL policy check could pass before the workflow uploaded its findings, leaving four open log-injection alerts and ten JavaScript quality findings on a completely green pull request | fixed — every request-derived repository log field and propagated error crosses a tested CR/LF-safe boundary; nine dead TypeScript symbols and the startup page's executable `document.write` handoff were removed; and the CodeQL workflow now parses its generated SARIF synchronously and fails closed on a finding, missing output or malformed output before its own required job can pass |

## ARCH — recorded, deliberately not attempted on this branch

| ID | S | Location | Finding | Status |
|---|---|---|---|---|
| ARCH-001 | M | internal/server | A single flat package of 406 files and 181k lines. Splitting it is a real improvement but would conflict with every other change here and is an architectural decision to take on its own | fixed — taken on its own once every other change had merged, as the owner-chosen coarse modular monolith that still assembles into the one fat binary. The data layer is now its own compiler-enforced module: `internal/store` (137 files, ~41k lines — Store and every sub-store, all entity types previously scattered beside their handlers, Persistence/PersistBatch, replica refresh, object byte store) sitting on `internal/gitstore` (git object storage + s3fs), with `internal/server` (~213k lines — Server, REST, GraphQL, Actions engine, identity, webhooks, and all contract gates) importing store and never the reverse. Type-alias compatibility files keep the 2,600+ Server methods and the whole test estate compiling unchanged; every newly exported store field carries json:"-" so wire and persisted shapes are byte-identical; the replica reflect-copy gained an access-field skip set so exporting the lock/persist fields could not change refresh semantics. Verified as a pure refactor: full suite green except the documented MinIO host-OOM cluster, shape ratchet zero-new, deadcode/dupl/clock gates green, and a runtime smoke across REST, git contents, GraphQL, UI and a push-triggered Actions run. Further de-methodizing of the engine slices (actions, graphql, identity) out of internal/server remains available as future work but is not this row's defect: the load-bearing split — data layer vs application — is done and the compiler now enforces it |
| ARCH-002 | M | internal/server | After the ARCH-001 data-layer split the Actions engine (~5,900 lines of run lifecycle, dispatch, broker queueing, concurrency admission, scheduling, expressions and event snapshotting) still lives inside the application package as ~65 Server methods, invisible as a boundary and free to grow entangled | fixed — the engine is now `internal/actions` (23 files, ~7.5k lines with tests): an Engine struct owning the run lifecycle, dispatch, broker queue, concurrency admission, schedule index and event snapshot loop, importing only the store layer — the compiler enforces the boundary. Server-owned behavior reaches it through five narrow seams: an EventSink interface (webhook/check payload rendering and delivery stay in server), MintJobToken (runner-JWT mint and verify stay on one key in the auth layer), the fixture clock, shutdown-tracked goroutine spawning, and OnScheduleTick hooks for the org-invitation and login-session piggybacks. The mixed files split along the protocol/engine line (broker handlers vs queueing, event queue/drain vs merge-gate reads and emitters, trigger parsing vs PR-synchronize firing); pure trigger/expression/matrix/cron tests moved with their code, harness tests reach the engine via the server field. Verified as a pure refactor: full suite green (the only failures were the documented Docker/MinIO host-infra class), shape ratchet zero-new at 19,371 validated responses, clock/deadcode/dupl/store-lock gates green — the store-lock AST ratchet now scans internal/actions too — and a live smoke drove a push-triggered run through the extracted engine. GraphQL extraction remains the enumerated next slice; identity deliberately stays with the auth chokepoints |

## Refuted

Checked and found not to be defects. Recorded so they are not actioned later.

| Claim | Verdict |
|---|---|
| `gh_code_scanning.go:49` registers an unreachable route without the `/api/v3` prefix | Reachable and deliberate. `gh_middleware.go:82-86` activates the API middleware for the `/repos/` and `/code-scanning/` prefixes and cites the reason: the official CodeQL Action posts to the uploads host. Cited provenance for an uploads-host path |
| `math/rand` use is a security weakness | Not a defect. The only non-test use picks a zen quote. All 18 token, secret and ID generation sites use `crypto/rand`, and UUIDs are crypto-backed |
